# 밤샘 실행 계획 — 형님 취침 중 Opus 단독 진행 (2026-08-17 새벽)

> **실행자(Opus)에게**: 형님은 주무신다. 질문할 사람이 없다 — 이 문서와 참조 문서가 전부다.
> 막히면 그 유닛을 버리고 다음 유닛으로 간다. 아침 보고서(N5)는 무슨 일이 있어도 쓴다.

---

## 0. 야간 운영 수칙 — 전 유닛 공통, 어기면 사고

| # | 수칙 |
|---|---|
| 1 | **`git push` 금지. 전부 로컬 커밋만.** 운영 배포는 아침에 형님이 검토 후 직접 민다. (오늘 커밋 `2479936`·`9bb6170`·`218ddd4`도 아직 로컬이다) |
| 2 | **결제 실행 금지** (경쟁사 상품 구매 포함) · 메일 발송 금지 · 광고 집행 금지 · 외부 계정 로그인 금지 |
| 3 | **가격·slug·번들 산수 불변**: inyeon 17,900/24,900 · sangun 19,900/29,900 · 번들 26,900/54,800. **R5(3단 인상)는 문서로만 쓰고 코드에 배선하지 않는다** |
| 4 | `/api/orders/create` 계약 불변 · 한자는 장식만 · 만세력 API는 캐시 우선(한도 6,000) |
| 5 | 신규 라우트(`/jiknyeo`)는 **`feature/jiknyeo` 브랜치**. main 에는 기존 상품 개선(N1)만 |
| 6 | 유닛 하나 끝날 때마다 커밋(작게, 검증 붙여서). 유닛 도중 커밋 금지 |
| 7 | R4(베틀·천·은사)·R8(실값)은 기획서의 강력 권고 + 증거 5곳(붉은실 선점)이므로 **신규 직녀 산출물에 적용**한다. 단 산출물 헤더에 「R4·R8 반영 — 형님 X 시 이 문서만 수정」 표기. **기존 인연 랜딩의 붉은 실 디바이저는 건드리지 않는다** |
| 8 | 스크롤 연출(useInView)은 headless 검증 불가 — 실브라우저(프리뷰 탭, `all_saju-dev`)로 확인 |
| 9 | 문체: 산군=반말(`~다`/`~라`, lint-result 검사) · 인연/직녀=존대 해요체(INYEON_VOICE 금지선 `prompt.ts:108-122` 절대 유지) |
| 10 | 토큰·시간이 부족해지면 **N4 → N3 → N2 순으로 버리고**, N1과 N5는 사수한다 |

---

## N1. 인연 결과지 8장 → 10장 (main, 로컬 커밋)

**전용 지시서가 이미 있다 — 그대로 실행:**
→ `다음세션_계획_인연결과지_산군수준_2026-08-17.md` (T1~T7, 완료 정의 포함)

핵심만 재확인: 10장 × 850~1,050자 = 8,500~10,500자 · 신설 2장(걸어온 연애의 길=과거 검증)·6장(알아보는 신호 셋+하지 말 것) · 샘플 2명 생성(캐시 명식) · lint 치명 0 + 금지선 grep 0 · 랜딩 「8개 챕터·A4 3장」 표기 전량 갱신 · 375px 실렌더.

---

## N2. 직녀 카피 팩 — 문서 6종 (`직녀/` 폴더, main, 로컬 커밋)

코드 아님, 전부 문서. 기획서(`인연사주_최종기획_2026-08.md`)를 소스로, 실행목록(`경쟁사레퍼런스/00_인덱스.md` §4-B #1~52)을 재료로 쓴다.
각 문서 머리에 `> R4(베틀·은사)·R8(실값) 반영 — 형님이 X 주면 이 문서만 고친다` 한 줄.

| # | 파일 | 내용 | 수용 기준 |
|---|---|---|---|
| 2-1 | `직녀/프리셋_상대유형_40.md` + `src/config/jiknyeo-presets.seed.ts` | 상대/악연 40종 — 「OO형」+지표5+문장2 (#31 포맷, §3-8) | 40종 전부. 유형명 중복 0 · 문장 중복 0(조판 규칙). seed 는 타입 붙은 배열만(어디에도 import 하지 않는다 — 배선은 직녀 구현 단계) |
| 2-2 | `직녀/입력대본.md` | 대화형 입력 전 대사 — 6문항+자유고민+이메일. 산군 위저드(1/10~10/10)가 이미 같은 구조로 돌고 있으니 **문항 구성은 그대로, 대사만 직녀 보이스**로. 자유고민 화면 = 블러 해제 지점 명기(#46), 이메일 = 「짠 천을 어디로 보낼까요」(#45) | 화면별 대사·버튼 라벨·플레이스홀더까지 전부. 존대 해요체, 언니 아닌 직녀(§1-3 위계: 본문<대사<나레이션) |
| 2-3 | `직녀/티저_12블록_전문.md` | §3-4 B1~B12 완성 카피. B2 과거 연도 문장(매수 조건) · B4 다리문장(#35) · B8 하지말것 · 잠금=「여기까지만 짰어요」(§6-B-3) · 목차는 대사로(#48) · 의성어 단독 컷 「툭.」(#50) · 미래 연도 1문장(#52) | 블록별 완성 문장 + 이미지 슬롯 표기(글·사진 교차). §3-5 문장 규칙(형용사→장면) 적용. 산군 티저와 겹치는 문구 0 |
| 2-4 | `직녀/결제시트_v4.md` | 3단 카드 문구 전문 — **R5 가격(19,900/29,900[추천]/49,900)은 시나리오로 쓰되 「현행 배선은 17,900/26,900 유지」 명기** + 쿠폰=로그인 미끼(#49) + 마케팅 수신 기본체크 + 실값 언어(R8) | 카드 3장 문구 + 상태 분기(비로그인/로그인) + 현행가 병기표 |
| 2-5 | `직녀/일러_발주문.md` | LOCK v3 전문(기획서 §2-1 통째) + 컷리스트: 마스터 4(§2-2) + 웹툰 7컷(§3-11) + 티저 슬롯별 + 사이즈/포맷(webp 800×1000 기준, 산군 자산 규격 준용) + 수량·납품 조건. **발주처 칸과 스타일 칸(A/B/C)은 「아침 선택」으로 비워둔다.** 이건 **플랜 B 문서**다 — 본편 기본 경로는 AI 생성(2-7 시안 → 아침 스타일 확정 → J1 시드 → 레퍼런스 체인). 보류 중인 오행 캐릭터 일러와 묶음 발주 권고 한 줄 | 발주문 그대로 복사→전송 가능한 완성도 |
| **2-7** | `직녀/시안_그림채_스프레드/` (이미지 12~16장 + `비교시트.md`) | ★ 그림채 판정 **매트릭스 — 스타일 6종 × 컷 2종** (형님 지시 2026-08-17: 「좀 더 많은 샘플」).<br>**스타일**: **A** 순수 수묵담채(원안) / **B** 하이브리드=담채 배경+고밀도 인물(추천안) / **C** 고채도 세미리얼 웹툰(타이트 마감, 빨강 금지) / **D** 실사풍(산군 실사 세계관 통일축 — 이 시안에 한해 LOCK의 NOT photorealistic 조항 유예) / **E** 전통 미인도·공필 세밀화(저채도+고디테일) / **F** 셀채색 웹툰(청월당축 — 비교군).<br>**컷**: ① 얼굴 클로즈업(피드 3초 판정) ② 베틀 앞 반신(세계관 판정). 여력 시 상위 2스타일에 촛불 온기 조명 변형 +2~4장.<br>**생성 우선순위(경로 막힘 대비)**: 6스타일×클로즈업 6장 → ×반신 +6장 → 조명 변형. 공통 불변: 30대 초반 한국 여성·낮게 땋은 머리·아이보리 저고리+남색 치마·은사·빨강 0(F 포함).<br>**생성 경로 사다리**: ① `.env`에 `OPENAI_API_KEY` 존재하면(값 확인 금지, 존재만) 이미지 API로 전량 무인 → ② ChatGPT 웹을 claude-in-chrome(형님 크롬)으로 구동 — 로그인 풀려 있으면 시도 중단, **계정 설정·결제 화면은 절대 건드리지 않는다**, **이 경로는 10장 상한**(우선순위 순서로 끊는다) → ③ 막힌 칸은 **프롬프트 완성본**을 비교시트에 수록(형님 아침에 붙여넣기 한 장당 30초).<br>비교시트: 매트릭스 표(스타일×컷) + 원본과 **375px 축소본** 나란히 + 산군 컷 1장 옆에 배치 + 판정 기준 2줄(3초 안에 얼굴이 사는가 / 산군과 한 세계로 보이는가) + 스타일별 시장 근거 1줄 | 아침 보고서에서 형님이 매트릭스 보고 스타일 1개(+조명)를 찍을 수 있는 상태 |
| 2-6 | `직녀/광고카피_5본.md` | ④-C 위닝 실측 반영: ① 내게 올 사람(1등 훅 #72) ② D-DAY/무늬력(자체 지표 #78) ③ 낮은 점수(#79) ④ 3자 관점(#80) ⑤ 목차 자체(#82). 각 본 = 상단 훅 2줄 + 고정본문 결합형 | 5본 전부. 개인화 없는 소재에서 연도 단정 금지 · 가격 노출 금지 |

---

## N3. `/jiknyeo` 광고 전용 랜딩 — 조립까지 (feature/jiknyeo 브랜치)

**범위를 좁게 잡는다 — 소개 랜딩만.** 위저드·티저 개인화는 기존 인연 플로우(`SajuWizard`, productSlug=`inyeon-saju`)를 그대로 쓴다. 퍼널 엔진을 포크하지 않는다.

- 라우트: `src/app/jiknyeo/page.tsx` — **noindex(robots meta) + sitemap 제외**(R6: 광고 전용 비공개)
- 내용: N2-3 티저 12블록 중 **랜딩 성격 블록**(도입~목차~가격 앵커~CTA)을 `InyeonWebtoon.tsx` 구조를 참고해 새 컴포넌트로 조립. 직녀 보이스 + 은사(銀絲) 액센트(붉은색 금지 — LOCK v3 `NO RED ANYWHERE`)
- 이미지: 기존 `public/products/inyeon/i1~i3.webp` + 회색 플레이스홀더(발주 컷 자리, 컷 번호 라벨) — **일러 오면 갈아끼우기만**
- CTA → 기존 인연 위저드 진입(현행 가격 17,900/번들 26,900 그대로)
- 분량 앵커: N1 이 끝난 뒤의 실측 숫자로(「A4 열 장」) — N1 미완이면 이 블록은 생략하고 TODO 주석
- QA: 375px 실렌더 + 좌우 넘침 0 + useInView 연출 실브라우저 확인
- 완료 정의: 브랜치 커밋 + 스크린샷 확보(아침 보고서 첨부용)

---

## N4. 리그레션 + (스트레치) 산군 소재 2차 영상

**4-a 리그레션 (필수, main 에서):**
```bash
npx tsc --noEmit && npm run build
npx tsx scripts/lint-result.ts --temp
npx tsx scripts/verify-money-path.ts --only=A     # 캐시 생일만, API 0콜
```
전부 통과 기록. 실패 시 원인 파일만 고치고 재실행(신규 기능 추가 금지).

**4-b 산군 소재 2차 — 여유 있을 때만** (계획: `marketing/소재/_계획_소재2차_데이터반영_2026-08-16.md`):
- `marketing/tools/` 별도 package.json → `puppeteer` + `ffmpeg-static` 설치(~260MB, node_modules 안)
- 소재 B(20초, WIN3 공식 — 정지컷+자막, 판매문구 0) 먼저 → 소재 A(9초, WIN2 공식 — 훅 고정 3컷 + 결과지 실물 + 운명의 상대 카드)
- 각 9:16 + 4:5 = mp4 4개 → `marketing/소재/산군/` + 카피시트 갱신
- **설치 실패(네트워크 등) 시 미련 없이 스킵하고 N5로** — 기록만 남긴다

---

## N5. 아침 보고서 (무조건 쓴다) — `아침보고_2026-08-17.md`

형님이 눈 뜨고 5분 안에 전체를 파악하도록:

1. **완료/스킵 표** — 유닛별 결과 + 커밋 해시(전부 로컬임을 명시) + 증거(실측 숫자·스크린샷 경로)
2. **아침 5분 목록** (형님 몫, 우선순위순):
   - `git push` 여부 결정 (밤 커밋 검토 후)
   - 픽셀 ID 3종 + RESEND → Vercel → Redeploy
   - 산군 소재 ON (카피시트대로: 캠1·세트1·소재2·일2만·7일 방치)
   - **그림채 선택** (시안 매트릭스 A~F 보고 스타일 1개 찍기 — 이게 정해져야 본편 컷 생성 시작)
   - R4·R5·R8 확인 (X 있으면 해당 문서만 수정)
   - 직녀 일러 발주처 결정 → `직녀/일러_발주문.md` 복사 전송
   - 네이버 메일함 확인 (A-1 — 타이트 리드 2건 등록됨)
   - 참고 결과지 1건 구매 승인 여부 (3,900원~)
3. **밤새 발견한 문제**(있으면) + 다음 밤 후보 큐

---

## 참조 문서 지도
- N1: `다음세션_계획_인연결과지_산군수준_2026-08-17.md` ← 전용 지시서
- N2·N3: `인연사주_최종기획_2026-08.md`(§1-3·§2·§3·§4·§6-B) + `경쟁사레퍼런스/00_인덱스.md` §4-B 실행목록 + `경쟁사레퍼런스/타이트/티저_MZ범산도령_시장1위_퍼널전문.md`
- N4-b: `marketing/소재/_계획_소재2차_데이터반영_2026-08-16.md` + `00_위닝소재_전체랭킹.md`
- 저장소 상태·도구: `인수인계_2026-08-06.md` · 큰 틀: `다음세션_계획_실행1차_형님품0_2026-08-16.md`
