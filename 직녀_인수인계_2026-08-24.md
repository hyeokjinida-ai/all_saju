# 직녀 인수인계 — 2026-08-24 (결제 전 티저 검수 → 16건 수정 배포)

전날 것: `직녀_인수인계_2026-08-23.md` · 조판 규칙 원문은 그 문서와 메모리 `myeongunrok-teaser-craft`.

## 오늘 한 것 — 운영 티저(?demo=1)를 폰 390×844 로 전수 실측하고 16건 고침

### A. 망가져 보이던 것 (버그 6)
| 무엇 | 원인 | 고친 것 |
|---|---|---|
| 헤더 진행점이 「명운록 · 직녀의 연애예보」 글자 위에 겹침 | 무대(JiknyeoStory) 브랜드 바 top16~36 과 위저드 `pt-5`(점 top28)가 같은 높이 | 직녀 세계관만 `pt-12` |
| 맨 끝 고정바가 결제 버튼 15px + 환불 문구를 덮음 | 바닥 스페이서가 티저 안(결제 시트 **앞**)에 있어 효과 0 | 스페이서도 포털로 body 끝에 |
| 마감 펀치 「달 이름만,」 이 안 보임 | `.text-moonlit`(흰 그라데 글자)는 검은 무대용 — 밝은 판에서 투명 | 토큰 먹색. 「아직이에요」는 보라 박스에 흰 글자(먹색은 대비 2.6:1) |
| 배웅 컷 뒤 보라→검정 띠 80px | InkFade(밝은판→검은결제 전환)가 판 **중간**에 있었음 — 뒤에 금기 카드·컷이 더 옴 | 제거 |
| 목차에서 직녀 코멘트가 다음 장 제목에 간격 0 으로 붙음 | `TocChapter` 의 `first:mt-0` — 호출부가 장마다 `<div>` 로 감싸 **모든 장이 first** | 간격을 호출부 래퍼(`mt-10`)로 이동 |
| 「같이 볼까요?」 말풍선이 타원 | `preserveAspectRatio="none"` + 상자 1:1 vs viewBox 100×122 | 상자 비율 = viewBox 비율, 속성 제거 |

### B. 말풍선 문법 v2 — 눈·손을 가리던 것
운영 실측: **w3 왼쪽 눈 정통 · t14 내민 손 통째 · w7 달력 짚는 손가락** 을 가리고 있었다.
원인: `sayAt="top|bottom"` 모서리 지정 — 우리 컷은 2:3→4:5 크롭이라 인물이 꽉 차서 "빈 모서리"가 컷마다 다르다.

- **자리는 좌표로**: `src/lib/jiknyeo-say-box.ts` `SAY_BOX_DEFAULT`(x·w=컷폭%, y=컷높이%) + 컷별 얼굴/손 실측치 주석. 그림 새로 구우면 이 표부터 다시 잰다.
- **`?edit=say` 편집 모드**: 티저 주소에 붙이면 말풍선 드래그 이동 · 노란 손잡이로 크기 · 하단 바에서 글씨 11~24px · 「코드 복사」→ SAY_BOX_DEFAULT 교체 조각. localStorage 저장, 손님 화면 영향 0.
- 규칙(청월당 result_02·07·09 실측): **눈·입·손 금지** / 머리칼 윤곽 한 조각은 OK(전부 피하면 UI 라벨이 됨) / 꼬리는 인물 쪽 / 얇은 검정 테두리.
- 컷 밖 걸침은 우리 구조(풀블리드 컷)에선 성립 안 함 — 컷 안에도 자리가 있다(w3 만 하단이 유일).
- 컷 아래 60% 깔리던 **밑그늘 그라데 제거** — 자막 상자 시절 받침. j1 하단 밝기가 원본의 51%였다.
- ComicSay 는 이제 `tail`(up/down) + `point`(인물 방향) + 폭은 부모 sayBox 가 결정. 테두리는 같은 도형 2회 칠(테두리→흰 덮기)로 이음매 없음.

### C. 형님 지시 3건
- VS 카드: **앱 구독 제거**(우리보다 싼 앵커였음), 철학관 **10만~30만**, 두 칸 중앙 정렬(가로 스크롤 소멸)
- POINT 1~3 을 `PointCard`(168° 그라데 판 + 채운 배지)로 — 섹션 경계가 보이게. 4·5는 자체 비주얼(한지·VS)이 있어 배지만 강화
- 말풍선 글씨 16px 고정(`--say-font`), 위계 본문15.5 < 대사16. 17px↑는 10자 대사가 원을 넘어 폭도 같이 키워야 — 편집 모드에서 조절

## ⚠ 워킹트리 사고 기록 (다음 세션 필독)
`all_saju` 워킹트리는 브랜치 `ad-v5-seoyun` 인데 **최소 2개 세션이 동시에 쓰는 중**이다. 이 배포는 스크래치 worktree 로 main 을 따로 받아 **내 파일 8개만** 옮겨 커밋했다. 그 과정에서 골라낸 것:
- **반개방 작업**(짝 카드 4줄 중 2줄 열기, `face.lookOpen`·`ageDir`)과 **「이 달들만 기억하세요」 카피**는 다른 세션 것이라 **이 커밋에서 제외**했다. 그 작업은 `src/lib/saju/partner-face.ts` 수정(+111줄, **아직 미커밋**)에 의존한다 — **SajuWizard 쪽만 커밋하면 main 이 타입 에러로 깨진다.** 반드시 partner-face 와 한 커밋으로.
- 워킹트리의 `JiknyeoResult`·`prompt.ts`·`dev/sangun-result` 미커밋분도 같은 세션 것으로 보임.

## 검증 경로 (재사용)
헤드리스 크롬 + raw CDP 로 폰 뷰포트 캡처·DOM 실측 — 메모리 `mobile-capture-cdp` 참조. 핵심: UA 에 Headless 가 들어가면 `/api/saju/chart` 가 막혀 "헤드리스는 티저 못 띄움" 오해가 생겼던 것. 스크립트는 세션 스크래치 `shoot/shoot-mobile.mjs`·`probe.mjs`.

## 다음
1. **광고영상 v6 승인** (보드 1순위, §12 결정 6개)
2. 형님: `npm run seed:products`(상품명 옛 이름) · `?edit=say` 로 말풍선 최종 위치 픽 → 코드 조각 전달
3. 카피 결정 2: 「열 장」vs「A4 여덟 장」 통일 · 같은 목록 3회 반복(잠금줄→구매카드→목차) 축소
4. 콜드리딩 크림 카드 톤(밝은 판에서 딴 색) — 달빛 계열로 갈지
