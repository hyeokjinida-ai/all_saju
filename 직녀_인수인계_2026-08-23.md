# 직녀 인수인계 — 2026-08-23

> 다음 세션은 이것부터 읽는다. 오늘 하루 종일 직녀 티저·게이트 조판과 영상을 고쳤다.

## 1. 지금 상태 (운영 배포 완료, main `723186f`)

### 영상 7편 — 직녀 라인
| 자리 | 파일 | 내용 | 재생 |
|---|---|---|---|
| 게이트(착지) | `j3.mp4` | 까치가 날아와 손에 앉고 직녀가 정면을 봄 | **1회 후 정지**(`once` 플래그) |
| 설화 1씬 | `w1.mp4` | 은하수·별 반짝임 | 루프 |
| 설화 2씬 | `w2.mp4` | 클로즈업 아이들(깜빡임·머리칼) | 루프 |
| 설화 3씬 | `w4.mp4` | 까치 떼가 은하수로 날아오름 | 루프 |
| 설화 4씬 | `w7.mp4` | 달력 칸이 달빛처럼 맥동 | 루프 |
| 입력 위저드 배경 | `w2.mp4` 재사용 | 전체화면 몰입 + 유리판 blur | 루프 |
| 분석 대기(9/9) | `loading.mp4` | **직녀가 베를 짜는** 미디엄샷 | 루프 |

- 제작: 힉스필드 MCP. **줌인=Seedance 2.5(start+end+ref)**, **루프=Kling 3.0**(7.5cr, 첫=끝 프레임 안 씀)
- 판정선: 0.5s 변화 평균 3~6 · 움직이는 셀 150+ · 이음매 ≤10 · **10fps 접촉시트 눈 검수 필수**
- 오늘 크레딧 약 260 사용 (잔액은 `mcp__higgsfield__balance` 로 확인)

### 조판 — 말풍선/컷 (오늘 여섯 번 고침)
- 말풍선 = **하나의 SVG 도형**(원+꼬리 한 `<g>`). 조립 금지 — 이음매 생김
- 규격: 정원 · 폭 46% · 글 2줄(span+nowrap) · 명패 없음 · 꼬리가 인물 쪽
- 위치: **컷 안 빈 모서리**(`sayAt="top"|"bottom"`). 컷 밖 걸침은 **금지**(밝은 띠 생김)
- 컷 간격: `InyeonCut` 의 **`padTop`** 으로만. 바깥 `mt-*` 은 곧 흰 띠
- 컷은 `#0b0f1a` 밤 배경 블록으로 감쌈. 정보 카드(원국표·달력·가격)는 밝은 판 유지

### 그 외 오늘 배포분
- 게이트 헤드라인 손실회피 C: 「이미 한 번, / 지나쳤을지도…」
- 한글 `word-break: keep-all` (월드 3종) — 어절 중간 절단 해결
- 결과지 크로스셀 배너(직녀 72%·산군 65%) + 후기 CTA(97~98%)
- 章 간지(직녀 결과지 10개)

## 2. 형님 대기 — 이것부터 물을 것
1. **`npm run seed:products`** — 운영 상품명이 아직 「직녀 연애사주 — 만나는 달」(옛 이름).
   코드·랜딩·결과지는 「직녀의 연애예보」라 화면마다 어긋남. auto mode 가 차단해 형님이 직접 1회 실행.
2. **8/18~22 광고 상태** — 픽셀·GA4 키, 폰 게이트, 산군 광고 ON 여부를 모름.
   이게 없으면 이번 주 「착지→입력」 판정선이 안 잡힘.
3. 직녀 티저 폰 확인 — 오늘 마지막 배포(SVG 말풍선)를 아직 형님이 안 봄

## 3. 다음 후보 (형님 결정 전)
- j2 컷 프레이밍(얼굴이 작음) · 말풍선 꼬리 각도 미세조정
- 증거 영상 3편(예보판 애니·결과지 스크롤·만세력 검증) — 전부 화면 녹화, 크레딧 0
- 페이월 손실회피 문장 「올해 남은 보름 N번」(N = gradeMonths 실제값)
- t01·t04 티저 슬롯(그림 없음)

## 4. 오늘 크게 헤맨 것 — 반복 금지
1. **브랜치 사고**: 워킹트리가 `ad-v5-seoyun`(다른 세션 광고 v5 작업)인데 확인 없이 커밋·푸시.
   `git push` 는 성공했지만 main 이 아니라 형님 화면이 계속 옛 버전이었다.
   → **커밋 전 `git rev-parse --abbrev-ref HEAD` 확인.** 다른 세션이 워킹트리를 쓰고 있으면
   `git worktree add <tmp> main` 으로 **별도 worktree** 에서 작업한다(오늘 이 방법으로 해결).
2. **컴포넌트 안만 고치고 호출부를 안 봄** — 흰 띠의 범인은 `InyeonCut` 내부가 아니라
   호출부의 `<div className="mt-6">` 였다. 세 번 고쳐도 화면이 그대로였던 이유.
3. **레퍼런스 규격의 전제를 안 봄** — 청월당 걸침·사다리꼴은 「종이 한 장 바탕」이라 성립한다.
   우리는 밝은 판+어두운 컷이라 정반대 결과가 났다.
4. **검증 수단 함정**: 헤드리스 크롬은 직녀 티저를 못 띄운다(API 실패로 빈 배경).
   실제 브라우저 탭에서 스크롤하며 캡처해야 보인다. 창 크기를 바꾸면 스크린샷 API 가 깨진다.

## 5. 참고 문서
- `경쟁사레퍼런스/청월당/게이트영상/판독_greeting.md` — 루프 영상 10fps 판독(합격선의 근거)
- `docs-private/청월당_유료결과지_완전해부.md` — 크로스셀 위치·빈 액자 기법
- `영상지시서_힉스필드.md` — MCP/UI 사용법, 모델 스펙, 크레딧
- 청월당 티저 원본: `경쟁사레퍼런스/청월당/랜딩/연애비책/티저원본/result_*.png`
