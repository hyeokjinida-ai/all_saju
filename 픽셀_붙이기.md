# 메타 픽셀 붙이기 — 형님용 5분 절차

코드는 이미 다 들어가 있다. **환경변수 한 줄만 채우면 켜진다.**

---

## 1. 픽셀 ID 찾기 (또는 만들기)

메타 광고를 아직 안 돌렸으면 픽셀부터 만들어야 한다.

1. business.facebook.com 접속
2. 왼쪽 메뉴 **이벤트 관리자**(Events Manager)
3. **데이터 소스** → 픽셀이 있으면 그 이름 아래 **숫자 15~16자리**가 ID다. 그걸 복사.
4. 없으면 **데이터 소스 연결 → 웹 → 픽셀** 로 새로 만든다. 이름은 아무거나(예: 명운록).
   - 설치 방법을 물으면 **"직접 설치"** 를 고르고 코드 복사 화면은 그냥 닫으면 된다.
     (우리 코드에 이미 들어 있어서 붙여넣을 필요가 없다)

> ID 예시 모양: `1234567890123456`

---

## 2. Vercel 에 넣기

1. vercel.com → 프로젝트(all_saju) → **Settings** → **Environment Variables**
2. **Add New** 눌러서

   | 칸 | 넣을 값 |
   |---|---|
   | Key | `NEXT_PUBLIC_META_PIXEL_ID` |
   | Value | 위에서 복사한 숫자 |
   | Environments | **Production 체크** (셋 다 체크해도 무방) |

3. **Save**

---

## 3. ⚠️ Redeploy — 이걸 안 하면 반영이 안 된다

환경변수는 저장만으로는 안 먹는다. 반드시 다시 배포해야 한다.

1. 위쪽 탭 **Deployments**
2. 맨 위(가장 최근) 줄 오른쪽 **⋯** → **Redeploy**
3. "Use existing Build Cache" 는 **체크 해제** 하는 쪽이 안전
4. **Redeploy** 누르고 2~3분 기다린다

---

## 4. 됐는지 확인

형님이 직접 볼 수 있는 방법:

- 폰이든 PC든 https://myeongunrok.com 를 연다
- 메타 **이벤트 관리자 → 테스트 이벤트** 화면을 열어두면 `PageView` 가 실시간으로 찍힌다

**끝나면 나한테 "픽셀 넣었어" 한마디만 해라.** 내가 라이브 HTML 을 직접 긁어서
픽셀이 실제로 붙었는지·이벤트가 나가는지 확인해 준다.

---

## 붙고 나면 자동으로 잡히는 것들

우리 코드가 이미 이 다섯 개를 메타 표준 이벤트로 흘리게 배선돼 있다(`src/lib/analytics.ts`).
픽셀 ID 만 채우면 아래가 전부 살아난다 — 추가 작업 없음.

| 손님 행동 | 메타 이벤트 | 왜 중요한가 |
|---|---|---|
| 페이지 열람 | `PageView` | 기본 |
| 게이트를 넘어 상품(목차·가격)을 봄 | `ViewContent` | **게이트 이탈률**을 여기서 처음 잰다 |
| 무료 티저에서 자기 사주를 확인 | `AddToCart` | 구매 의사 최고조 지점 |
| 결제 시작 | `InitiateCheckout` | |
| 결제 완료 | `Purchase` | **ROAS 판정의 근거** |

---

## 다음 순서 (픽셀 다음)

1. **RESEND 연결** — 비회원이 결제하면 결과지를 메일로 못 받는다. 탭 닫으면 돈만 내고 끝.
2. **실카드 결제 1건** — 아직 실결제로 결과지까지 완주한 주문이 **0건**이다.
3. **후기 3~5건 시딩** — 소셜프루프 숫자가 비어 있어 블록 자체가 안 나온다.
4. **광고 진입점 결정** — 게이트로 받을지, 세일즈 페이지로 직행시킬지.
5. **광고 ON** — 세트1 · 소재5 · 일 3만 · 1주 방치.
