# 명운록 저장소 — 세션 필독

> 이 파일은 **모든 세션 시작에 자동으로 읽힌다.** 여기 적힌 건 기억에 의존하지 않는다.
> 2026-08-26 신설 — 하루에 재작업 7건이 났고, 전부 원인이 하나였다:
> **이미 있는 정본·자산·문서를 안 열고 기억으로 만들었다.**

## 규칙 1 — 제작 착수 전 3검색 (예외 없음)

이미지·카피·부품·조판 **무엇이든 새로 만들기 전에** 이 셋을 먼저 한다. "ㄱㄱ" 를 받아도
곧장 산출로 달리지 말 것 — 오늘 재작업 7건이 전부 이 단계를 건너뛴 결과다.

1. 메모리 인덱스에서 관련 카드 확인
2. 저장소 문서 검색 — `grep -rl "<키워드>" --include=*.md .` (루트 md 93개 + `docs-private/`)
3. 기존 자산 확인 — `ls public/products/<상품>/` · `직녀/` 등

⚠ 실패 사례: 타이트 유료 결과지를 「없다」고 3번 답했다(있었다) · 캐릭터시트 정본이 있는데
안 쓰고 완성 컷을 참조로 던져 5회 연속 같은 그림만 나왔다 · 말풍선을 실물 안 보고 만들어
크기가 5배 틀렸다 · 에셋 생산 시트를 안 열고 화풍을 다르게 썼다.

## 규칙 2 — 직녀 인물 컷은 **반드시 조립기로**

「직녀 캐릭터」 「인물 컷」 「일러 뽑기」 「캐릭터 수정」이 나오면 프롬프트를 손으로 짜지 말 것.

```bash
npx tsx scripts/jiknyeo-cut-prompt.ts --id <컷id>
```

조립기가 **시트 첨부 안내 + 고정 블록 + 나이 가드 + COMPOSITION + 4앵커 검수표**를 완성문으로
뽑아 준다. 손으로 조립하면 반드시 뭔가 빠진다(실측: 나이 가드 누락 → 십대로 나옴,
고정 블록 요약 → 다른 인물, 화풍 오기 → 수묵으로 나옴).

정본 문서: `직녀/캐릭터시트_프롬프트.md`(프로토콜·4앵커) · `직녀/에셋_생산_프롬프트시트.md`(컷별 문장)
정본 자산: `직녀/캐릭터시트_정본.png` — **이게 신분증이다.** 완성 컷(j1 등)을 신분증으로 쓰면
그 구도째 복제된다(2026-08-26 5회 실패의 원인).

## 규칙 3 — 이미지는 ChatGPT 웹에서만

GPT 이미지 **API 금지**(퀄리티, 형님 지시 2026-08-17). 크롬 확장으로 chatgpt.com 을 구동한다.
함정: ① 파일 업로드 직후 바로 타이핑하면 **텍스트가 안 들어간 채 전송**된다 —
전송 후 `document.body.innerText` 에 내 지시문이 있는지 **반드시 확인**할 것.
② 새 탭은 screenshot 으로 활성화한 뒤 타이핑해야 ASCII(영문·공백)가 안 증발한다.
③ 입력이 안 먹으면 `document.execCommand('insertText', ...)` 로 직접 넣는다.
④ **전송은 좌표 클릭도 ref 클릭도 자주 씹힌다.** 확실한 건 JS 로 버튼을 직접 누르는 것:
   `[...document.querySelectorAll('button')].find(b=>(b.getAttribute('aria-label')||'').includes('보내기')).click()`
⑤ **전송 확인은 「입력창이 비었는가」로 한다.** `document.body.innerText` 에는 입력창 내용도
   들어 있어서 「내 지시문이 페이지에 있다」는 전송 증거가 못 된다(8/26 이걸로 헛다리 짚음).

## 규칙 3-B — 인물 컷 첨부는 **언제나 2장**

시트(부품 신분증) + **최신 합격 컷**(계보 신분증). 한쪽만 붙이면 갈라진다 —
시트만 붙여 뽑은 N1·N2 는 4앵커를 다 통과했는데 **골격이 달라** 기존 컷 옆에서 다른 사람이었다.
검수도 혼자 보지 말고 **기존 컷을 옆에 붙인 대조 시트**로 볼 것(⑤ 동일인 앵커).

## 규칙 4 — 검증은 DOM 실측으로

캡처 픽셀 판독은 **밤 그림과 밤 배경이 겹쳐 못 잰다**(컷 폭이 28%~94% 로 튀었다).
`node 직녀/가격카드/tools/measure-dom.mjs --url ... --sel ...` 로 getBoundingClientRect 를 쓴다.
긴 페이지 캡처는 `shoot-long.mjs`(shoot-url.ps1 은 16,000px 에서 잘린다).

## 규칙 5 — 파이썬으로 저장소 파일 고칠 때 `'wb'`

`io.open(p, 'w')` 는 Windows 에서 **파일 전체를 CRLF 로 바꿔** 한 줄 고친 게 전 줄 diff 가 된다.
읽기는 `encoding='utf-8'`, **쓰기는 `io.open(p,'wb').write(s.encode('utf-8'))`**.
(bash heredoc 은 이스케이프를 먹으므로 정규식·역슬래시가 든 코드는 파이썬 쪽에서 조립할 것.)

## 작업 관례

- 커밋 전 브랜치 확인. 워킹트리를 다른 세션과 공유하므로 **내가 만진 파일만** 스테이징한다.
- dev 서버는 `preview_start`(.claude/launch.json 의 `all_saju-dev`). dev 가 떠 있으면 `npm run build` 금지.
- 계획 → 보고 → 컨펌 → 실행. 이미지 픽·큰 결정은 형님 몫.
