# 작업기록 — 카탈로그 홈 · 상품 빌더 · 결제 장치 (2026-08-23)

> 한 줄: **「파는 화면이 없던 가게」를 「가게」로 만들었다.** 홈·목록·결제·상품 추가 수단까지.
> 커밋 13개(`0e83efe` ~ `41ef7a0`), 전부 푸시·배포 완료. 운영 DB 마이그레이션 0011·0012 적용 완료.
> ※ 같은 날 다른 세션이 진행한 게이트 영상·디자인 부품함·로고 배선은 이 문서 범위 밖.

---

## 1. 왜 했나

명운록에는 **홈이 없었다.** 루트가 자수정 랜딩 한 장이었고, 그 한 장이 가리키는 상품마저 비활성이었다.
광고를 켜도 손님이 도착하는 곳이 「상품 하나짜리 랜딩」이라 다른 상품을 볼 길이 없었다.
비교 대상(청월당)은 카탈로그 홈 + 캐릭터 카드로 상품을 **여러 개 동시에** 판다.

곁가지로 드러난 것 둘:

- 상품을 추가하려면 **코드를 고쳐야** 했다 (seed 파일 → 커밋 → 배포).
- 결제 화면이 직녀 상품에서 **「결제」 두 글자로 끝났다.**

---

## 2. 커밋 (시간순)

| 시각 | 커밋 | 무엇 |
|---|---|---|
| 01:34 | `0e83efe` | 카탈로그 홈 신설 (+ 검증 전용 빌드/서버 분리) |
| 01:50 | `4ffbee5` | 히어로가 첫 장에 갇혀 있던 것 — 점·자동넘김이 실제로 안 넘어감 |
| 02:07 | `e94dff7` | 어드민 상품 빌더 — 코드 없이 상품 추가·수정 |
| 02:31 | `15f3457` | 카드 글씨의 「밤티」 — 폰트를 눌러 만든 가짜 압축체였다 |
| 03:07 | `99da83c` | 레터링을 GPT 웹에서 직접 뽑아 카드에 얹음 |
| 03:36 | `59c55e1` | 레터링 3/3 완결 |
| 11:14 | `22aad01` | 폰트가 아니라 **캘리그라피**로 다시 — 누끼는 크로마키로 |
| 11:48 | `bbad4e2` | 결혼예보 판매 중단 (그림·엔진은 보존, 판매만 내림) |
| 12:16 | `4fd24ac` | 사이트가 두 벌로 갈려 있던 것 — 자수정 보라 → 먹빛 전면 통일 |
| 12:32 | `1c99b38` | 직녀 결제 전용 화면 신설 |
| 13:01 | `726b50e` | 결제 화면을 **영수증**으로 — 다른 데만큼만 |
| 13:51 | `cc1eec8` | 결제 화면 장치 넷 — 레퍼런스에 있는데 우리에게 없던 것 |
| 14:07 | `41ef7a0` | 카카오 버튼이 provider 꺼진 채 손님 앞에 서지 않게 |

---

## 3. 만든 것

### 3-1. 카탈로그 홈 (`/`, `/products`)

청월당 골격을 **기계 대조**로 복제했다. 눈대중 금지 — 레퍼런스를 픽셀 스캔해 잉크 박스를 뜨고,
우리 렌더도 같은 방식으로 스캔해 편차 `d:` 를 0 으로 수렴시켰다 (랜드마크 7개 d:0).

**변장 모드 판정선**(`docs-private/직녀_디자인/레퍼런스_복제_작업법.md`)을 지켰다:
① 골격은 레퍼런스와 ±6px 안 ② 표면(색·카피·캐릭터)은 전부 우리 것.

- `src/config/home.ts` — 홈의 **모든 문구·순서·토큰이 여기 한 곳**에 있다. 컴포넌트엔 하드코딩 없음.
- `src/lib/home-data.ts` — 상품·실후기 로드. **service 키를 쓴다**(후기 이름이 있는 `profiles` 는 RLS 가 본인만 select 라 anon 으로 join 하면 조용히 0건).
- `src/components/home/` — HomeShell · HeroCarousel · HeroLettering · ProductCard · ProductRow · ReviewRow · TrustBlock · HomeFooter
- 후기는 **실후기 3건 미만이면 섹션 자체를 안 그린다.** 샘플 후기·지어낸 누적구매 숫자 금지 원칙.

### 3-2. 상품 빌더 (`/admin/products`)

상품 추가가 코드 배포에서 **화면 입력**으로 내려왔다.

- 4개 구획: 기본(값·노출) / 홈 카드(캐릭터·카드제목·한줄·히어로 순위) / 랜딩 카피 / 결과지 설계
- **실시간 미리보기가 진짜 `ProductCard`·`HeroLettering` 을 그대로 쓴다** — 미리보기와 실물이 갈라질 수 없다.
- 결과지 설계(프롬프트)는 `products` 가 아니라 **별도 테이블 `product_styles`** 로 뺐다.
  `products` 는 public read 라, 같은 테이블에 두면 **프롬프트 설계가 그대로 공개된다.**
  `product_styles` 는 RLS 켜고 **정책을 하나도 안 만들었다** = service 키로만 읽힌다.
- 마이그레이션 전에도 안 죽는다: 게이트가 `select("category")` **성공 여부 하나**뿐이라,
  컬럼이 없으면 기본 항목만 저장하고 화면이 안내를 띄운다.

### 3-3. 카드 레터링 — 폰트가 아니라 캘리그라피

세 번 갈아엎었다.

1. **1차(밤티)** — Noto Sans KR 을 `lengthAdjust="spacingAndGlyphs"` 로 54~66% 눌러 압축체를 흉내냈다.
   형님 지적: "너무 밤티나". 맞다. **폰트를 눌러 만든 가짜 압축체**였다.
2. **2차** — Black Han Sans(진짜 사각·헤비)로 교체, 압축 없음. 밤티는 빠졌으나 여전히 **글씨**였다.
3. **3차(확정)** — 형님 지적: "청월당은 캘리그라피처럼 이쁘게 되어있잖아?"
   청월당은 **어떤 폰트도 아니고 손붓글씨**다. 그래서 원본을 가평한석봉 붓서체로 굽고,
   GPT 지시를 「표면만 바꿔라」에서 **「붓으로 다시 그려라」**로 바꿨다.

**누끼**(형님 지적: "누끼도 다음에는 제대로 따고")

- 원인은 알고리즘이 아니라 **입력**이었다. 흰 배경 위 흰 획은 **밝기 임계값 하나로 절대 못 가른다**
  (은자개 직녀엔 흰 테두리가 남고, 금박 산군은 획이 잘렸다).
- 해결: GPT 에 **초록 크로마키 배경**을 요구하고 `G − max(R,B)` 로 뽑았다. 초록 번짐은 따로 눌렀다.

### 3-4. 전사 리스킨 — 자수정 보라 → 먹빛

형님 발견: "로그인페이지 등 특정 페이지들은 메인 배경이 보라색?"
홈만 검정으로 갈아서 **사이트가 두 벌로 갈려 있었다.** 전체 통일(옵션 A)로 갔다.

- `globals.css` 의 shadcn HSL + 원시 토큰, `tailwind.config.ts` 팔레트를 한꺼번에 중립 먹빛으로.
- **세계관 스킨은 살렸다** — `.world-sangun`(금) · `.world-jiknyeo`(달빛 은) · `.world-wealth`(주사)
  는 CSS 변수 스코프라 그대로 위에서 덮어쓴다.
- 팔레트는 `var()` 가 아니라 **hex 로 유지**했다. `bg-wine-2/50` 같은 **투명도 수식이 `var()` 로는 안 먹는다.**

### 3-5. 결제 화면

**형님이 두 번 반려했다.** 반려가 옳았다.

- 1차: 캐릭터 대사체 → "고객 친화적이고 어색하지않게"
- 2차: → "말할 필요 없는 말들도 있고 / 다른 레퍼런스에서는 뭐라고 되어있는지 보고 말해"

그래서 실측한 레퍼런스 3사(청월당·타이트·음양관)의 결제 화면을 다시 읽었다. 결론:
**세 곳 다 캐릭터 대사도, 가격 앵커도, 환불 문장도 없다. 그냥 영수증이다.**
→ "다른데만큼만 해" → 산군·직녀 둘 다 영수증으로 재작성.

이어서 형님이 **"다른데도 이정도만 한거지?? 잘 파악하고 말해라"** 로 한 번 더 눌렀고,
정직한 답은 **「카피는 맞췄지만 장치는 못 맞췄다」** 였다. 그래서 빠진 장치 넷을 채웠다:

| 장치 | 구현 |
|---|---|
| 할인 배너 | 정가 → 할인 → 청구액을 영수증에 명시 |
| 쿠폰/로그인 슬롯 | 레퍼런스는 **빈 쿠폰함**으로 로그인만 시킨다. 우리는 쿠폰이 없고 **실할인 1,900원이 진짜 있으므로** 금액을 그대로 말하고 로그인시킨다 |
| 휴대폰 | **선택** (알림톡 채널이 아직 없어 필수로 받을 명분이 약하다) |
| 마케팅 동의 | **기본 해제** (타이트는 기본 체크지만 법적으로 다툼이 있는 자리) |

**주문 이관**(`api/orders/[orderId]/claim`) — 결제 직전에 로그인하고 돌아온 손님에게
생년월일·시각·고민을 **다시 입력시킬 수 없다.** `orders/create` 계약은 불변으로 두기로 한 자리라,
**같은 `order_id` 를 유지한 채 주인(`user_id`)과 금액만 바꾸는** 라우트를 따로 냈다.
금액은 여기서도 서버가 산정하고, 승인 단계(`orders/confirm`)가 토스 금액과 재대조한다.

**카카오 자동 숨김** — provider 가 꺼져 있는데 버튼만 서 있으면 손님이 눌렀다 튕긴다.
`useKakaoEnabled()` 가 `/auth/v1/settings` 를 읽어 꺼져 있으면 버튼을 **아예 안 그리고**,
"또는 이메일로" 구분선까지 같이 데려간다(혼자 남으면 유령 구분선이 된다).

---

## 4. 운영 DB — 0011 · 0012 적용 완료

Supabase SQL 편집기에서 실행. 「destructive operations」 경고가 떴는데
원인은 `drop policy if exists` 한 줄이고 **바로 다음 줄에서 같은 정책을 다시 만든다.** 데이터 삭제 문장 없음.

코드가 실제로 던지는 쿼리 그대로 재검증한 결과:

| 대상 | 결과 |
|---|---|
| `products` 빌더 8칸 (category·character_name·card_title·tagline·hero_rank·art·pitch·updated_at) | 읽기·쓰기 ✓ |
| `product_styles` | ✓ / **anon 으로는 안 읽힘**(프롬프트 비공개 확인) |
| `product-art` 버킷 | ✓ |
| `orders.phone` · `marketing_opt_in` · `_at` | 쓰기 ✓ |

### ⚠ 마이그레이션 직후 홈이 죽을 뻔한 자리

새 컬럼이 **전부 NULL** 이라, `home-data.ts` 가 DB 값을 스프레드로 덮으면서
`config/home.ts` 의 `CATEGORY_FALLBACK` 을 **null 로 밀어버린다.**
마이그레이션 전에는 쿼리가 에러 → 빈 맵이라 폴백이 살아 있었는데, 붙는 순간 뒤집히는 구조였다.

**살아남은 이유는 소비처가 한 번 더 받아내기 때문이다:**
`p.category ?? CATEGORY_FALLBACK[p.slug]` — `src/app/page.tsx:68`, `src/app/products/page.tsx:23`
히어로도 `heroRank != null` 로 걸러 비면 `HERO_ORDER` 로 떨어진다.
→ **이중 폴백을 지울 때 조심할 것.** 한쪽만 지우면 홈의 상품 줄이 통째로 빈다.

---

## 5. 밟은 함정 (재현 방지)

| 함정 | 실체 |
|---|---|
| `npm install sharp` 실패 | 이 저장소는 **pnpm**. `pnpm add sharp` |
| 다른 세션의 dev 서버를 죽임 | 같은 `.next` 를 공유해 서로 덮어썼다 → `distDir: process.env.NEXT_DIST_DIR` + 검증 전용 3100 포트 분리 |
| 빌드 뒤 `tsconfig.json` 이 바뀜 | Next 가 다시 쓴다. 빌드 후 `git checkout -- tsconfig.json` |
| 히어로 슬라이드가 16px 작음 | 스크롤러 `px-2` 와 슬라이드 `calc(100%-16px)` 로 **두 번 뺐다** |
| `getBoundingClientRect()` 로 잰 높이가 +78 어긋남 | 그건 **레이아웃 박스**지 잉크가 아니다. 레퍼런스를 스캔한 방식 그대로 우리 것도 픽셀 스캔해야 한다 |
| 헤드리스 크롬 창이 28px 어긋나고 우측이 잘림 | Windows 크롬이 **창 너비를 ~500px 로 강제**한다. 넓게 찍고 중앙 열을 잘라내는 `-Col` 로 우회 |
| 브라우저 패널 스크린샷 타임아웃 | 숨은 탭에서는 합성이 멈춘다. 부드러운 스크롤·`rAF`·`setInterval`·리액트 상태까지 얼어붙는다. **판정 전에 `document.visibilityState` 확인.** 핵심 인터랙션을 컴포지터 스크롤에 의존시키지 말 것 |
| 카드에 회색 판때기가 생김 | sharp 가 1채널로 준 줄 알았던 버퍼가 3채널이었다. `toBuffer({resolveWithObject:true})` 의 `info.channels` 로 보폭을 잡을 것 |
| sharp `trim()` 이 엉뚱하게 자름 | 이 빌드의 `trim()` 은 **알파를 안 본다.** 알파 기준 bbox 를 직접 구해 `extract()` |
| 어드민 편집이 조용히 되돌아감 | `seed-products.ts` 가 **upsert** 라 실행할 때마다 덮어썼다 → 기본을 insert-only 로, 덮어쓰려면 `--force` |
| `hero_rank` 를 select 에 넣자 쿼리가 통째로 죽음 | 0011 전이면 PostgREST 가 전체를 떨군다. **새 컬럼은 select 를 쪼개고 실패하면 빈 값으로** |
| ChatGPT 웹 자동화 | 작성창 클릭 후 **첫 입력이 삼켜진다**(내용 확인 후 ctrl+Home 으로 앞에 덧대기) · 전송 버튼 클릭이 자주 안 먹는다(포커스 후 Enter) · 연타하면 계정 전체가 몇 분 묶이고 모달이 클릭을 막는다(`ref` 로 클릭하면 통과) |

곁다리: 작업 중 ChatGPT 작성창에 **형님이 안 보낸 초안**(명운록 로고 심볼 브리프)이 남아 있었다.
지우기 전에 `직녀/레터링/_형님_미전송초안_로고심볼.md` 로 백업해 뒀다.

---

## 6. 새로 생긴 명령·도구

```bash
pnpm art:home
```

```bash
pnpm art:lettering
```

```bash
pnpm art:cutout
```

- `art:home` — 레퍼런스 크롭 → `public/home/*.webp`
- `art:lettering` — 카드 레터링 원본 굽기 (`face: block | brush`)
- `art:cutout` — 크로마키/흰배경 자동 판별 누끼
- `.claude/start-verify.js` — 검증 전용 서버(포트 3100, 산출물 `.next-verify`).
  띄우기 전 `NEXT_DIST_DIR=.next-verify pnpm build`.
- `직녀/가격카드/tools/shoot-url.ps1` — 헤드리스 크롬 URL 촬영(`-Col` 로 창 폭 강제 우회).

---

## 7. 라이브 실측 (이 문서 작성 시점)

| 항목 | 상태 |
|---|---|
| 홈·상품·결제·빌더 | ✅ 라이브 정상 |
| `myeongunrok.com` | ✅ 200, 정품 도메인 연결됨 |
| 어드민 | ✅ 비밀번호 게이트 있음 |
| **메타 픽셀** | ❌ **배포본에 없음** — `NEXT_PUBLIC_META_PIXEL_ID` 미설정 |
| 공개 후기 | 0건 (홈이 후기 섹션을 안 그림) |
| 실제 결제 | 2건 (`ord_*`) — 나머지 16건은 검증용 `VERIFY-*` |
| 카카오 로그인 | 꺼짐 (`kakao:false`, 이메일 버튼으로 자동 대체 중) |

---

## 8. 남은 일

### 형님 몫

1. **메타 픽셀 ID** — 이벤트 관리자 → 데이터 소스에서 복사.
   ⚠ 이 값은 **빌드 때 코드에 박힌다.** 환경변수만 넣고 재배포를 안 하면 아무 일도 안 일어난다.
   → ID 만 주시면 Vercel 등록 + 재배포는 이쪽에서.
2. **8/22 산군 캠페인 발사했는지** — 켰다면 지금 픽셀 없이 도는 중이라 급하다.
3. **이번 주 전선** 확정.
4. (급하지 않음) 카카오 provider 켜기 + 리다이렉트 URI 등록 — 결제 직전 로그인 전환이 오른다.
5. (급하지 않음) 폰으로 배포본 확인 — 히어로 스와이프, 결제 화면.

### 나중에

- **후기** — 실결제가 2건이라 정직하게 받을 후기가 없다. 광고로 실구매가 붙은 뒤 요청하는 게 순서.
- **휴대폰 필수 전환** — 알림톡 채널이 열리면 선택 → 필수로 올린다.
- **마케팅 동의 기본 체크** — 켜려면 `defaultChecked` 한 글자.
- **빌더 새 칸 채우기** — 지금은 전부 NULL 이라 `config/home.ts` 폴백으로 돈다. 정상이되,
  빌더에서 채우면 그때부터 DB 값이 이긴다.
