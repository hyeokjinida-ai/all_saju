# 결제 화면 장치 — "다른 데만큼" 채우기 (실행 계획서, 2026-08-23)

> 작성: Fable(계획) · 실행: Opus. 형님 "고고" 후 이 문서만 보고 구현한다.
> 출발점: 결제 화면 **문구**는 레퍼런스만큼 비웠다(커밋 726b50e). **장치**는 세 곳 중 제일 빈 화면이다.
> 형님 「다른데도 이정도만 한거지?? 잘 파악하고 말해라」 → 요소표로 대조한 결과가 §1.

## 0. 먼저 확인한 사실 (계획을 바꾼 것)

| 사실 | 어디 | 의미 |
|---|---|---|
| **로그인 할인 1,900원이 서버에 이미 있다** | `api/orders/create/route.ts:18` `MEMBER_DISCOUNT = 1900`, 금액은 서버 산정 | 타이트의 「쿠폰 확인 = 로그인 미끼」를 **쿠폰 테이블 없이, 정직하게** 만들 수 있다 |
| 그 할인을 말하는 화면은 은퇴한 퍼널뿐 | `components/funnel/screens.tsx:675` | 지금 손님은 아무도 모른다 → 결제 화면이 말해야 한다 |
| 카카오 로그인 코드는 있으나 provider 미활성 가능 | `KakaoLoginButton.tsx:35` "provider is not enabled" 분기 | **형님 게이트**: Supabase Auth → Kakao 활성 + Kakao Developers redirect URI |
| `orders/create` 는 명식 입력을 같이 받는다(재호출 불가) | `route.ts` inputRow·saju_inputs | 로그인 후 주문 이관은 **새 엔드포인트**로(계약 불변 원칙 지킴) |
| 주문에 휴대폰 컬럼 없음 / profiles.phone 은 있음 | `types/database.ts` | 휴대폰은 `orders.phone` 신설(0012) + 프로필 값으로 미리 채움 |
| 개인정보처리방침 수집 항목 = 이메일·비번·닉네임 | `legal/privacy/page.tsx:24` | 휴대폰·마케팅 동의를 넣으면 **방침 문구 수정이 따라온다**(형님 확인) |
| TossWidget 에 결제 직전 훅 없음 | `TossWidget.tsx` handlePay | `onBeforePay` 한 개 추가 |
| 번들 3단은 위저드 결제시트에 이미 있다 | `SajuWizard.tsx:547~563`(추천 배지·할인율 역전) | 결제 화면에 다시 안 세운다 — 우리 구조상 한 단계 앞 |

## 1. 요소표 — 레퍼런스 vs 우리 (판정선)

| 장치 | 청월당 | 타이트 | 음양관 | 우리 지금 | 이 계획 후 |
|---|---|---|---|---|---|
| 상품·정가 취소선·할인·결제금액 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 「총 N원 할인받았어요」 배너 | – | ✓ | – | ✗ | **P0** ✓ |
| 회원(로그인) 할인 줄 | – | – | – | 서버만 | **P0** 영수증에 줄 |
| 쿠폰 슬롯 = 로그인 미끼 | ✓ | ✓ | – | ✗ | **P1** ✓ (1,900원 실할인) |
| 휴대폰 번호 | ✓ 필수 | – | – | ✗ | **P2** ✓ |
| 마케팅 수신 동의 | – | ✓ 기본체크 | – | ✗ | **P2** ✓ |
| 결제수단 여러 개 | 4종 | – | 4종 | 토스 위젯(콘솔 설정대로) | 변경 없음 |
| 수단별 추가할인(토스페이 2%) | ✓ | – | – | ✗ | **안 함**(PG 프로모션 영역) |
| 누적구매·만족도 | 배지 | – | ✓ | ✗ | **안 함**(실수치 없음, 원칙) |
| 번들 3단 | ✓ | ✓ | – | 위저드에 | 변경 없음 |

## 2. 결정 기본값 (형님이 안 바꾸면 이대로)

| # | 결정 | 기본값 | 근거 |
|---|---|---|---|
| D1 | 로그인 할인 금액 | **1,900원 유지**(기존 값) | 타이트는 3,000원. 바꾸려면 `MEMBER_DISCOUNT` 한 줄 |
| D2 | 미끼 문구 | 「**로그인하면 1,900원 할인돼요** — 카카오로 1초」 | 타이트의 「쿠폰이 있는지 확인해보세요」(있는지도 안 알려줌)보다 **실할인을 그대로 말한다**. 빈 쿠폰함을 보여주면 미끼가 아니라 실망 |
| D3 | 휴대폰 | **선택 입력**(청월당은 필수) | 알림톡 채널이 아직 없어 필수로 받을 명분이 약함. 채널 열리면 필수로 전환 |
| D4 | 마케팅 동의 기본값 | **해제** | 타이트는 기본 체크. 형님이 체크를 원하면 한 글자(`defaultChecked`) |
| D5 | 휴대폰·동의 저장 위치 | `orders.phone` / `orders.marketing_opt_in(+_at)` | 주문 단위 동의 기록(언제·무엇에 동의했는지 남는다). profiles.phone 은 미리 채우기 용 |
| D6 | 로그인 후 주문 처리 | **이관**(같은 order_id 유지, user_id·amount 갱신) | 재생성은 명식 입력이 필요해 불가. `orders/create` 계약 불변 |

## 3. 단계

### P0 — 영수증 완성 (30분 · 게이트 없음)
`src/app/checkout/[orderId]/page.tsx`
- 영수증 줄: 정가 → `지금 결제 시 할인 -N%` → **`회원 할인 -1,900원`**(order.user_id 있을 때만) → 결제금액
  - 회원 할인 금액은 `MEMBER_DISCOUNT` 를 `lib/pricing.ts` 로 빼서 route 와 페이지가 **같은 상수**를 본다
- 제목 아래 배너 한 줄(타이트식): `총 8,900원 할인받았어요` = (정가 − 결제금액). 정가 없으면 배너 생략
- 검증: 산군 게스트(29,900→19,900 · 배너 10,000) / 회원(→18,000 · 배너 11,900) / 정가 없는 상품(배너·할인 줄 없음)

### P1 — 로그인 미끼 (반나절 · 게이트: Kakao provider 활성)
**새 파일**
- `src/app/api/orders/[orderId]/claim/route.ts` — POST. 조건: 로그인 상태 · 주문 pending · `user_id is null`.
  처리: `user_id = user.id`, `amount = max(100, product.price − MEMBER_DISCOUNT)`, 같은 `order_id` 유지.
  실패(이미 회원 주문·paid 등)는 409. **`orders/create` 는 건드리지 않는다.**
- `src/components/checkout/LoginNudge.tsx` (client) — 비로그인 주문에서 영수증 아래:
  ```
  ┌────────────────────────────────────┐
  │ 로그인하면 1,900원 할인돼요          │
  │ [ 💬 카카오로 1초 만에 로그인 ]      │
  │   이메일로 로그인                   │
  └────────────────────────────────────┘
  ```
  `KakaoLoginButton redirect="/checkout/<orderId>?claim=1"` 재사용. 이메일 링크는 `/login?next=/checkout/<orderId>?claim=1`.
**수정**
- `checkout/[orderId]/page.tsx` — `?claim=1` 이고 로그인 상태이고 주문이 게스트면 → 서버에서 claim 로직 호출 후 같은 주소로 redirect(쿼리 제거). 화면엔 `회원 할인 -1,900원` 줄이 생긴다.
- `TossWidget` — `customerKey` 가 게스트→회원으로 바뀌므로 페이지가 다시 그리면 자연히 갱신(위젯은 서버 props 로 받음).
- 검증: 게스트 주문 → 카카오 로그인 → 돌아오면 금액 −1,900 · 같은 order_id · `orders.user_id` 채워짐 · 결제 confirm 에서 금액 불일치 없음(실결제 대신 `/api/payments/confirm` 의 금액 검증 경로를 코드로 확인).
- **게이트(형님)**: Supabase → Authentication → Providers → Kakao 활성 + Kakao Developers 에 `https://<도메인>/auth/callback` 등록. 미활성이면 버튼이 "provider is not enabled" 안내를 띄우고 이메일 로그인으로 떨어진다(기존 분기).

### P2 — 휴대폰 + 마케팅 동의 (반나절 · 게이트: 0012 SQL + 방침 문구)
- `supabase/migrations/0012_checkout_contact.sql`
  ```sql
  alter table public.orders
    add column if not exists phone text,
    add column if not exists marketing_opt_in boolean not null default false,
    add column if not exists marketing_opt_in_at timestamptz;
  ```
- `types/database.ts` orders Row/Insert 에 3개 추가
- `src/app/api/orders/[orderId]/contact/route.ts` — PATCH {phone?, marketingOptIn} · 주문 pending 이고 (게스트면 같은 브라우저의 주문 — 추가 검증: 요청 본문의 order_id 만으로는 약하므로 **게스트는 guest_email 일치**를 요구하지 않고, pending 주문의 연락처 갱신은 허용하되 phone 형식만 검증 / 회원은 user_id 일치). 마케팅 동의가 true 면 `_at = now()`.
- `TossWidget` — `onBeforePay?: () => Promise<boolean>` 추가. false 면 결제 안 함. `customerMobilePhone` 를 requestPayment 에 전달.
- `checkout/[orderId]/page.tsx` — 위젯 위에 `ContactFields`(client): 휴대폰(선택, 숫자만, 010 형식 검사, profiles.phone 미리 채움) + 체크박스 「(선택) 할인·새 풀이 소식 받기」. 결제 버튼 누르면 PATCH → 성공 시 결제 진행.
- `legal/privacy/page.tsx` — 수집 항목에 **휴대폰 번호(선택 · 결제 영수증·결과 안내)**, **마케팅 수신 동의(선택 · 철회 방법)**, 보유기간 행 추가. **형님 확인 후 반영**(법적 문서).
- 검증: 미입력 결제 정상 / 잘못된 번호 차단 / 동의 체크 시 `_at` 기록 / 방침 페이지에 항목 노출.

## 4. 하지 않는 것
- 쿠폰 테이블·코드 입력 — 실할인(로그인 1,900)이 이미 있어 불필요. 후기 인증 쿠폰 같은 건 카카오 채널 열릴 때 별도
- 토스페이 2% 같은 수단별 할인 — 토스 콘솔 프로모션 영역
- 누적구매·만족도 숫자 — 실수치 없음(원칙)
- 「실값 치르기」 등 세계관 용어 — 다른 데만큼: 버튼은 「결제하기」

## 5. 순서·게이트
1. **P0** 바로 → 커밋
2. **P1** 코드 먼저 → 형님 Kakao 활성(5분) → 실기기 로그인 왕복 1회 → 커밋
3. **P2** 0012 SQL 보고 + 방침 문구 초안 보고 → 형님 적용·확인 → 커밋
4. 0011(상품 빌더)과 0012 는 따로 — 0011 이 먼저 붙어 있어야 하는 건 아니다(독립)

## 6. 되돌리기
P0·P1: 페이지·컴포넌트 파일 되돌리면 끝(엔드포인트는 남아도 무해). P2: 컬럼은 nullable/default 라 코드만 되돌려도 동작.
