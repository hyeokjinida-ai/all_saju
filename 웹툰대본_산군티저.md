# 산군 티저 웹툰 대본 — 3그림 + 3글

> 들어가는 자리: 결제 직전 **티저 화면 맨 위**(A안). 이 웹툰 아래에 원국표·콜드리딩 3문장·잠긴 줄 5개가 이어지고, 그 밑이 결제다.
> 만드는 곳: `/admin/webtoon` → 산군 신점

---

## 캐릭터 확정 (2026-08-03)

**산군(山君) = 산신을 받든 박수의 당호.** 호랑이가 아니라 사람이다.

한국 무속에서 산신은 호랑이를 타고 다닌다. 그 산신을 모시는 신당의 주인이라 사람들이 그를 산군이라 부른다. 호랑이는 **신당 벽의 탱화·호피 방석**으로 배경에 남는다 — 이름의 근거가 화면에 보이고, 실제 신당에 흔한 소품이라 실사로도 뽑힌다.

**얼굴은 갓에 가려 안 보인다. 이건 결점이 아니라 컨셉이다** — "얼굴을 들지 않고 장부를 읽는 이". 기억 장치는 얼굴 대신 **갓 실루엣 + 방울 + 오방색 소매**가 맡는다.

코드 반영 완료: `prompt.ts`(결과지 정체성) · `SangunWebtoon.tsx`(소개문·FAQ) · `products.seed.ts`

---

## 설계 판단 둘

**① 콜드리딩은 웹툰에 넣지 않는다.**
바로 아래 텍스트 티저에 이미 나온다. 같은 문장을 두 번 보여주면 두 번째가 첫 번째 힘을 깎는다.

| | 웹툰이 하는 일 | 텍스트 티저가 하는 일 |
|---|---|---|
| 이름·간지 | 극적으로 던진다 | 원국표로 증거를 댄다 |
| 전환점 | 한 방으로 던진다 | 같은 값을 계산 근거와 함께 |
| **신살 해석** | **여기서만 나온다** | 이름만 배지로 |
| 콜드리딩 3문장 | 안 건드린다 | 여기서만 |

전환점만 일부러 겹친다. 문장이 아니라 숫자라서 두 번 봐도 안 지겹고, 아래 표가 받쳐주면 "진짜구나"가 된다.

**② 첫 칸은 반드시 손님 얘기로 연다.**
초안은 *"네 장부는 벌써 펴 두었다"* 로 시작했는데, 이건 **무당이 주인공**인 문장이다. 타이트가 *"너 겉으론 쿨해 보여도 눈치 많이 보잖아"* 로 여는 것과 정반대다. 손님이 구경꾼이면 몰입이 안 된다.

---

## 대본

### 1칸 — 그림 (m3 재활용)
**그림**: 문틀 너머 신당. 갓 쓴 박수가 부채를 펼치고 서 있다. 얼굴 안 보임.

**말풍선** (위쪽 중앙)
```
{이름}.
아무 말 안 해도 된다.
네가 왜 왔는지는 여기 다 적혀 있으니까.
```
- 모양 `둥근` · 꼬리 `bottom` · 크기 `4.2` · 글씨체 `명조 (격식)`

---

### 2칸 — 글
```
나는 아무것도 묻지 않았다.
생일 하나 받았을 뿐이다.
```
- 글씨체 `명조` · 크기 `4.2` · 배경 `#0a0908` · 글자 `#e8ded0` · 여백 `9`

> A안("결제 전엔 캐묻지 않는다")을 자랑으로 바꾼 칸이다. 이건 웹툰에서만 할 수 있는 말이다.

---

### 3칸 — 그림 (m2 재활용)
**그림**: 방울 쥔 손 클로즈업. 촛불이 뒤로 번진다.

**말풍선** (아래쪽 여백에)
```
{간지}.
그것만으로 여기까지 나왔다.
```
- 모양 `사각` · 꼬리 `top` · 크기 `4.2` · 글씨체 `송명조 (묵직)`

> 간지는 손님이 못 읽는다. 못 읽는 건 괜찮지만 **왜 대단한지는 말해줘야** 한다. "생일 하나로 여기까지"가 그 역할.

---

### 4칸 — 글
```
네게는 {신살}이 붙어 있더군.
{신살풀이}.

— 짚이는 데 있지?
```
- 글씨체 `펜 손글씨 (속마음)` · 크기 `4.4` · 여백 `10`

> **웹툰에만 있는 정보.** 텍스트 티저는 신살을 이름 배지로만 보여주고 해석이 없다. 이 칸이 있어야 웹툰이 장식이 아니게 된다.
> "짚이는 데 있지?"는 동의를 구하는 문장 — 타이트의 "~잖아" 자리다. 손님이 참여한다.

---

### 5칸 — 그림 (**신규 1장 필요**)
**그림**: 펼쳐진 옛 장부를 검지로 한 줄 짚는 손. 뒤로 호랑이 탱화가 흐릿하게.

**말풍선** (짚는 손가락 근처)
```
{전환점}.
이 해에 한 번 크게 갈린다.
```
- 모양 `둥근` · 크기 `5.0` · 글씨체 `거친 붓 (공포)` · 글자색 `#e8c96a`

---

### 6칸 — 글 (결제로 넘기는 칸)
```
나는 두루뭉술하게 말하지 않는다.
하라, 말라 — 아홉 번 못 박아 뒀다.
```
- 글씨체 `명조` · 크기 `4.4` · 여백 `11` · 글자 `#e8c96a`

> 초안은 *"나머지는 장부를 열어야 나온다"* 였는데, 결제 직전 마지막 칸이 아무것도 약속하지 않는 건 낭비다. 타이트의 "5만 자·100페이지" 자리를 **확답 개수**로 채운다.
> ⚠ **"아홉"은 아직 확정 숫자가 아니다.** 샘플 1건 기준이라 실제 생성물 몇 건으로 세어보고 확정할 것. 안 나오면 문구를 바꾼다.

---

## 그림 조달 — 새로 뽑을 건 1장뿐

| 칸 | 그림 | 상태 |
|---|---|---|
| 1칸 | m3 (부채 든 박수) | ✅ 있음 — 하단 5% 크롭(워터마크) |
| 3칸 | m2 (방울 쥔 손) | ✅ 있음 — 하단 5% 크롭 |
| 5칸 | 장부 짚는 손 | ❌ **새로 1장** |

※ m4는 m2와 같은 컷인데 하단에 회색 밴드가 박혀 있어 쓰지 않는다.

### 5칸 이미지 프롬프트 (나노바나나 / 실사)

```
A close-up photograph of an aged hand with a single index finger pressing down on
one vertical line of text in an open old Korean ledger book (hanji paper, brush-written
vertical columns). Shot from above at a slight angle. The hand wears the cuff of a
colorful obangsaek shaman robe — only the cuff and hand are visible, no face, no body.
Behind, softly out of focus, a traditional Korean shrine wall with a tiger folk-painting
(minhwa) and candlelight. Warm candlelight from the left, deep shadow on the right.
Shallow depth of field, the fingertip and the pressed line in sharp focus.
Photorealistic, cinematic, 35mm, vertical 9:16 composition.
```

**주의점**
- **얼굴이 절대 안 나오게.** 손과 소매만.
- 세로 9:16. 가로로 나오면 컷이 뭉개진다.
- 장부 글자가 또렷하게 읽히면 안 된다 — 엉터리 한자가 보이면 신뢰가 깨진다. 흐릿한 세로쓰기 질감이면 충분.
- 손가락이 짚은 자리가 화면 **위쪽 1/3**에 오게. 그 근처에 말풍선을 놓는다.
- 뒤에 **호랑이 탱화**를 흐리게 — 산군이라는 이름의 근거가 화면에 남는다.

---

## 아직 안 정한 것 — 말투

지금 결과지는 사극체(`~하거라`, `~이니라`)로 강제돼 있다(`prompt.ts`). 경쟁사 둘은 **사극체를 안 쓴다**:

- 청월당 흑월도령(흑백 수트) — *"외면할 수가 **없더군**"* 현대 하대체
- 타이트 범산도령 — *"~**잖아**", "~**하지?**"* 완전 현대 반말

위 대본은 사극 어미를 걷어내고 현대 하대체로 썼다. **웹툰만 이 톤으로 갈지, 결과지 프롬프트까지 같이 내릴지는 미정.**
따로 가면 결제 후에 온도가 갈라진다. 같이 내리면 결과지 품질 확인용 샘플 3건이 필요하다.

---

## 형님 할 일

1. m2·m3 하단 5% 크롭 → 워터마크 제거
2. 5칸 그림 1장 뽑기 (위 프롬프트)
3. 준비되면 "받아" — 제가 webp로 줄여 넣습니다. 또는 `/admin/webtoon`에서 직접 올려도 됩니다(자동으로 줄어듭니다)
