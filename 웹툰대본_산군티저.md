# 산군 티저 웹툰 — 완료 (2026-08-03)

> ✅ **다 들어갔습니다.** 사진 3장 업로드(형님) + 대사·말풍선(DB 직접 주입) + 노출 켜짐.
> 아래는 지금 화면에 실제로 들어가 있는 값입니다. 고칠 때는 `/admin/webtoon` → 산군 신점.

## 말풍선은 쓰지 않는다 — 실사에는 하단 대사 띠 (2026-08-03 판정)

처음엔 컷마다 **말풍선**(둥근·사각 + 꼬리)을 얹었다. 화면에서 보니 안 맞았다.

1. **경쟁사 둘 다 실사에 말풍선을 안 쓴다.** 청월당·타이트는 캐릭터가 웹툰 일러스트다.
   말풍선은 그림체와 같은 언어라 붙는 것이고, 실사에 얹으면 뉴스 사진에 만화 말풍선 단 꼴이 된다.
2. **화자가 산군 한 명뿐이다.** 말풍선의 존재 이유는 "여럿 중 누가 말하는지"인데,
   혼자 말하는 화면에서는 정보를 하나도 더 주지 않으면서 사진만 가린다.
3. **상품 안에서 문법이 갈렸다** — 이게 제일 컸다. 게이트·스토리 화면은 이미
   하단 대사창(비주얼 노벨)인데 웹툰만 말풍선이었다. 손님이 같은 상품에서 두 문법을 본다.

→ `SangunWebtoon` 의 `Bubble` 과 **같은 값**으로 맞췄다(실측 대조: 아래 여백 3.0% vs 2.9%).

| 항목 | 값 |
|---|---|
| 폭 | 92 (컷 폭 대비 %) · 꼬리 없음 |
| 배경 | `linear-gradient(180deg,#f3ead6,#e9dec2)` |
| 테두리 | `0.26cqw solid #c9b98e` |
| 글씨 | 명조 · 4.35 · 굵기 600 · 줄간격 1.8 · `#241d10` · 왼쪽 정렬 |
| 여백 | 좌우 1.2em · 위아래 0.95em |

렌더러에 `width`(고정 폭) 옵션을 새로 넣어 만들었다. 편집기에서는 **말풍선폭(0=자동)** 으로 조절한다.

## 글씨체 — 손글씨는 쓰지 않는다 (실측 판정)

처음엔 3칸에 `펜 손글씨`, 4칸에 `거친 붓`을 썼는데 **화면에서 무너졌다.**
펜 손글씨는 가늘고 흔들려서 중학생 쪽지로 읽히고, 거친 붓은 검정 말풍선 + 금테와 겹쳐
게임 쿠폰 뱃지처럼 보였다. 산군은 "낮고 단단한 어른"인데 정반대다.

→ **전부 명조 계열(명조·송명조)로 통일.** 강조는 색이 아니라 **글자 크기**로 준다.
말풍선 색이 컷마다 다르면 문장보다 그 색이 먼저 읽힌다.


> **들어가는 자리**: 결제 직전 티저 화면 **맨 위**. 이 웹툰 아래로 원국표 → 콜드리딩 3문장 → 잠긴 줄 5개 → 결제 버튼이 이어진다.
> **만드는 곳**: `/admin/webtoon` → **산군 신점**

---

## 지금 상태 — 전부 완료

| | |
|---|---|
| 사진 3장 | ✅ Supabase Storage `webtoon/sangun-sinjeom/` |
| 대사·말풍선 | ✅ 5칸 다 들어감 |
| 노출 | ✅ 켜짐 (`is_published: true`) |

---

# 순서 — 이대로만 하면 됩니다

## 0. 편집기 열기

브라우저에 이 주소를 칩니다.

```
http://localhost:3000/admin/webtoon
```

비밀번호 넣고 → 목록에서 **산군 신점** 클릭.

---

## 1. 사진 3장 한 번에 올리기

**[사진 여러 장 올리기]** 클릭 → `바탕화면\명운록_신점\` 폴더에서
**cut1.png · cut2.png · cut3.png 세 개를 한꺼번에 선택** (Ctrl 누르고 클릭) → 열기.

- 위에서 아래로 1·2·3번 컷이 자동으로 생깁니다.
- 용량은 브라우저가 알아서 줄입니다. 원본 그대로 넣으셔도 됩니다.

여기까지 하면 **컷 3개**가 생깁니다.

---

## 2. 글 컷 2개를 사이에 끼우기

**[+ 글 컷]** 을 **두 번** 누릅니다. 맨 밑에 두 개가 붙습니다.

```
지금:   1 cut1 · 2 cut2 · 3 cut3 · 4 글 · 5 글
```

**4번째 글 컷의 ▲ 버튼을 딱 한 번** 누릅니다. 이걸로 끝입니다.

```
완성:   1 그림 cut1   문틀 너머 신당, 부채 든 산군
        2 그림 cut2   방울 쥔 손
        3 글          ← 신살 이야기
        4 그림 cut3   장부 짚는 손
        5 글          ← 마지막, 결제로 넘기는 칸
```

> 📌 **왜 그림 2·3이 붙어 있냐면** — 3번 글 컷이 "신살"을 말하는 칸이라, 방울 쥔 손(cut2) 바로 뒤에 와야 말이 이어집니다.

---

## 3. 대사 넣기 — 아래 5개를 그대로 복붙

**그림 컷**은 → 사진 위를 **클릭**하면 말풍선이 생깁니다. 거기에 붙여넣습니다.
**글 컷**은 → 오른쪽 글 상자에 바로 붙여넣습니다.

`{이름}` 같은 **중괄호는 지우지 마세요.** 손님 실제 데이터가 그 자리에 들어갑니다.

---

### 1칸 · 그림 (cut1)

말풍선 위치: **위쪽 가운데** (산군 머리 위 빈 공간)

```
{이름}.
됐다, 그 정도면.
나머지는 네 장부가 말해준다.
```

> `{이름}`은 **님 없이 성까지** 나옵니다 — "김지훈. 됐다, 그 정도면."
> 산군은 반말 하대체라 "지훈님, 됐다"처럼 님과 섞이면 캐릭터가 무너집니다.
> 장부에서 이름을 호명하는 그림이라 성이 붙는 쪽이 더 맞습니다.
> 존댓말 캐릭터 상품을 나중에 만들면 `{이름님}`(→ 지훈님)을 쓰세요.

| 설정 | 값 |
|---|---|
| 모양 | 둥근 |
| 꼬리 | bottom |
| 글씨체 | 명조 (격식) |
| 글자크기 | 4.2 |

---

### 2칸 · 그림 (cut2)

말풍선 위치: **아래쪽** (손 아래 어두운 자리)

```
{간지}.
생일 하나 받았을 뿐인데 여기까지 나왔다.
```

| 설정 | 값 |
|---|---|
| 모양 | 사각 |
| 꼬리 | top |
| 글씨체 | 송명조 (묵직) |
| 글자크기 | 4.2 |

> 간지는 손님이 못 읽습니다. 못 읽는 건 괜찮은데 **왜 대단한지는 말해줘야** 합니다. "생일 하나로 여기까지"가 그 역할입니다.

---

### 3칸 · 글

```
네게는 {신살}이 붙어 있더군.
{신살풀이}.

— 짚이는 데 있지?
```

| 설정 | 값 |
|---|---|
| 글씨체 | 펜 손글씨 (속마음) |
| 글자크기 | 4.4 |
| 위아래 여백 | 10 |

> **웹툰에만 있는 정보입니다.** 아래 텍스트 티저는 신살을 이름만 배지로 보여주고 해석이 없습니다. 이 칸이 있어야 웹툰이 장식이 아니게 됩니다.
> "짚이는 데 있지?"는 동의를 구하는 문장 — 타이트의 "~잖아" 자리입니다.

---

### 4칸 · 그림 (cut3)

말풍선 위치: **짚는 손가락 근처** (화면 위쪽 1/3)

```
{전환점}.
이 해에 한 번 크게 갈린다.
```

| 설정 | 값 |
|---|---|
| 모양 | 둥근 |
| 글씨체 | 거친 붓 (공포) |
| 글자크기 | 5.0 |
| 글자색 | `#e8c96a` |

---

### 5칸 · 글 (결제로 넘기는 칸)

```
나는 두루뭉술하게 말하지 않는다.
하라, 말라 — 아홉 장에 못 박아 뒀다.
```

| 설정 | 값 |
|---|---|
| 글씨체 | 명조 (격식) |
| 글자크기 | 4.4 |
| 위아래 여백 | 11 |
| 글자색 | `#e8c96a` |

> **"아홉 장"은 실제 숫자입니다.** 결과지 챕터가 정확히 9개고(`prompt.ts`), 랜딩에도 "9장 · 확답 일곱 이상"으로 이미 적혀 있어 말이 맞습니다.

---

## 4. 저장하고 켜기

1. 오른쪽 위 **[저장하기]** 클릭
2. 그 밑 체크박스 **"숨김 (나만 봄)"** → 눌러서 **"손님에게 보이는 중"** 으로 바꾸기
3. **[저장하기]** 한 번 더

⚠️ 이 체크박스를 안 켜면 형님만 보이고 **손님한테는 안 나갑니다.** 기본이 꺼짐이라 그렇습니다.

---

## 5. 확인

손님 화면으로 들어가서 산군 신점 → 끝까지 입력 → 티저 화면 **맨 위**에 웹툰이 뜨면 끝입니다.

---

# 참고 — 알아두시면 좋은 것

## 쓸 수 있는 토큰 12개

| 토큰 | 들어가는 것 | 예시 |
|---|---|---|
| `{이름}` | **반말용** — 님 없이, 성 붙여서 | 권혁진 |
| `{이름님}` | 존댓말용 — 성 떼고 님 | 지훈님 |
| `{생년월일}` | 입력한 생일 | 1995년 12월 27일 |
| `{간지}` | 원국 3기둥 | 기묘년 갑술월 임자일 |
| `{일간}` | 타고난 기운 | 임수 |
| `{신강약}` | 기운 세기 | 태약 |
| `{오행최다}` | 제일 많은 오행 | 토 |
| `{전환점}` | 크게 갈리는 해 | 2028년, 30세 |
| `{신살}` | 가진 것 중 **가장 희소한** 살 | 천을귀인 |
| `{신살풀이}` | 그 살 뜻 한 줄 | 혼자 있는 시간이 있어야 사는 살 |
| `{콜드1~3}` | 콜드리딩 3문장 | (아래 티저에 이미 나옴 — 웹툰엔 안 씀) |

표본 50명 전원에게 채워지는 것만 넣어놨습니다. 그래도 못 채우면 **그 말풍선만 조용히 빠집니다** — 빈 중괄호가 손님한테 보이는 일은 없습니다.

## 손이 미끄러져도 괜찮습니다

- 저장 전 작업은 브라우저에 임시 보관돼서 새로고침해도 안 날아갑니다.
- 저장할 때마다 **직전 버전이 서버에 쌓입니다.** [되돌리기]로 돌아갈 수 있습니다.

## 말풍선 미세조정

손잡이를 **끌어서** 옮기고, **방향키**로 한 칸씩 (Shift 누르면 크게).

---

# 대본이 왜 6칸 → 5칸으로 줄었나

**빠진 칸**: *"나는 아무것도 묻지 않았다 / 생일 하나 받았을 뿐이다"*

처음엔 "결제 전엔 캐묻지 않는다"가 우리 무기라 그걸 자랑하는 칸이었습니다. 그런데 그 뒤에 **취향·연애·직업 질문을 다시 넣었습니다.** 손님은 방금 10단계를 답하고 이 화면에 옵니다. 거기서 "아무것도 묻지 않았다"고 하면 **바로 들통납니다.**

같은 이유로 1칸 대사도 바꿨습니다.

| | |
|---|---|
| 전 | "아무 말 안 해도 된다. 네가 왜 왔는지는 여기 다 적혀 있으니까." |
| 후 | "됐다, 그 정도면. 나머지는 네 장부가 말해준다." |

방금 답하고 온 걸 **인정**하고 넘어갑니다 — "그 정도면 충분하다, 나머진 장부가 안다."

마지막 칸도 `아홉 번` → `아홉 장`으로 고쳤습니다. 확답 개수는 명식마다 달라서 "아홉 번"이 안 나올 수 있는데, **챕터 9개는 항상 고정**이라 거짓말이 안 됩니다.

---

## 캐릭터 (참고)

**산군(山君) = 산신을 받든 박수의 당호.** 호랑이가 아니라 사람입니다.

산신은 호랑이를 타고 다니고, 그 산신을 모시는 신당의 주인이라 사람들이 그를 산군이라 부릅니다. 호랑이는 **신당 벽의 탱화**로 배경에 남습니다 — cut1·cut3 뒤에 보이는 그것입니다.

**얼굴은 갓에 가려 안 보입니다. 결점이 아니라 컨셉입니다** — "얼굴을 들지 않고 장부를 읽는 이". 기억은 얼굴 대신 **갓 실루엣 + 방울 + 오방색 소매**가 맡습니다.
