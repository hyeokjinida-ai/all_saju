# 명운록 인수인계 — 2026-08-06

이 세션(2026-08-05 밤 ~ 08-06 새벽)에서 한 일과, 다음 세션이 바로 이어받을 것.
이전 이력은 `_이어가기_인수인계.md` 와 메모리 `[[myeongunrok-handover]]` 참고.

---

## 🔴 지금 당장 막고 있는 것 — 형님만 할 수 있음

`아침_체크리스트.md` 에 클릭 절차까지 다 적혀 있다. **토요일(2026-08-08) 09:00 KST 에
클라우드 루틴이 자동으로 점검·보고하도록 예약해 뒀다.**

| 항목 | 상태 | 영향 |
|---|---|---|
| `NEXT_PUBLIC_SITE_URL` = `http://localhost:3000` | ❌ 운영 | 회원가입 인증메일·비번재설정·결과지 메일 링크가 **전부 죽는 주소**로 나감 |
| `RESEND_API_KEY` 없음 | ❌ | 비회원 결과지 메일 미발송 |
| `ADMIN_PASSWORD` = `0000` | ⚠️ | /admin 에 주문·매출·고객정보 |

검증 방법(밖에서 확인 가능한 건 이것뿐):
```bash
curl -s -i https://all-sajub.vercel.app/admin/logout | grep -i location
```
→ `https://all-sajub.vercel.app/admin/login` 나오면 고쳐진 것.

**환경변수는 저장만 하면 반영 안 된다 — Redeploy 필수.**

---

## ⏭️ 다음 세션이 이어받을 것 (컨펌 대기 중)

### 결제 페이지 리디자인 — 형님 답변 대기
`/checkout/[orderId]` 를 실측한 결과를 보고했고, **A안으로 갈지 물어본 상태에서 세션이 끝났다.**

실측한 문제:
- 페이지 배경 `rgb(22,10,54)` 보라 / 버튼 `rgb(201,168,255)` 라벤더 → **산군 세계관이 여기서 끊긴다.**
  직전 화면에서 산군이 "이제 복채 얘기를 하자"라고 말하는데 다음 화면 제목이 "결제"다.
- 페이지 전체 글자 수 **43자**. 결제 직전 재확신 요소가 0개(9장 목차·전환점 연도·운명의 상대·가격 앵커 전부 없음).
- 환불 보장 문구(TrustStrip)가 카드 **바깥 아래**에 있다.

제안한 두 안:
- **A. 산군 옷 입히기** (2~3시간, 추천) — 검정+금 토큰, 제목을 산군 대사로, 받을 것 요약 + 전환점 연도
  재노출, 가격 앵커(타이트 49,800 대비), 보장 문구를 버튼 바로 위로.
- **B. 결제 단계 제거** (반나절) — 티저 하단 시트에서 토스 위젯을 바로 띄워 `/checkout` 을 건너뛴다.
  이탈 지점이 하나 줄지만 결제 흐름을 건드리므로 **PG 심사 대기 중인 지금은 리스크**.

### 그 외 대기
- **운명의 상대 사진 8장** (`partner-m1~4` / `partner-f1~4`) — 지금은 회색 실루엣 폴백.
  프롬프트는 `이미지지시서_티저컷.md`.
- 소셜프루프 숫자 (`src/config/social-proof.ts` 비어 있음)
- 토스 PG 심사 결과 → 승인 시 `PG_REVIEW_MODE=false`
- 재구매 루프(후기→쿠폰 + 결과지 끝 큐레이션) — 미착수

---

## ✅ 이 세션에서 한 일

### 1. 돈 경로 전 구간 검증 (신규 도구)
지금까지 실결제 후 결과지까지 완주한 주문이 **0건**이었다. 광고 트기 전에 터뜨려 보려고 만들었다.

```bash
npx tsx scripts/verify-money-path.ts            # 3 페르소나 전부
npx tsx scripts/verify-money-path.ts --only=A   # 캐시된 생일만(사주 API 0콜)
npx tsx scripts/verify-money-path.ts --keep     # 결과를 안 지움(눈으로 볼 때)
```

주문(paid) → 명식 → LLM → 저장 → **배포본 렌더**까지 실제로 태운다. 끝나면 만든 주문을 지운다
(매출 통계 오염 방지). `--keep` 쓰면 남으니 `VERIFY-` 로 찾아 지울 것.

**결론: 파이프라인 자체는 멀쩡하다.** 9챕터·명식·raw_analysis 저장, 라이브 200 + 표지 + 章간지 + 75KB,
시각모름/음력/이름없음/회원·비회원 전부 통과, 멱등성 확인, 생성 8~19초.
문제는 위의 환경변수 2개였다.

### 2. 비회원 고아 방지
비회원이 결제 → 생성이 삐끗 → 안내는 "마이페이지에서 확인"인데 **비회원은 마이페이지에 못 들어간다**.
메일도 안 간다(RESEND 없음). 탭 닫으면 돈만 내고 끝나는 구조였다.

- `confirm` 응답에 `guest` 플래그 추가
- 비회원 보류 화면에 **이 페이지 주소 + 주문번호 + 복사 버튼 + 지금 다시 확인**
  (이 URL 은 paymentKey·orderId 를 들고 있고 confirm 이 멱등이라, 나중에 열면 완성된 결과지로 들어간다)
- `sendResultEmail` 이 SITE_URL 이 비었거나 localhost 면 발송을 막는다

### 3. 결과지 문체 — 치명 위반 4종 → 0종 (신규 도구)
```bash
npx tsx scripts/lint-result.ts --temp      # temp 의 sample-*.md 전부
npx tsx scripts/lint-result.ts <파일>
```

| 항목 | 전 | 후 |
|---|---|---|
| 존대 "당신" | 2 | **0** |
| 이름 3인칭 | 2 | **0** |
| 하라체("활용하라") | 5 | **0** |
| 한자병기("경신(庚申)") | 4 | **0** |
| 내부값("대길·대흉") | 5 | **0** |
| 헷지("가능성이 크다") | 14 | 7 |
| 형광펜 규칙 위반 | 5장 | 1장 |

**핵심 교훈: 판단이 필요한 것은 프롬프트, 기계적으로 정답이 하나인 것은 코드.**
프롬프트에 금지 규칙만 넣고 재생성했더니 하라체가 오히려 **5→6으로 늘었다**.
작은 모델은 한국어 명령형 형태소를 안정적으로 못 지킨다.
→ `src/lib/saju/normalize-voice.ts` 후처리로 못 박았다(어미·괄호한자·판정값·2인칭·형광펜 개수).
문장을 지우거나 새로 쓰지 않고 **형태만** 바꾼다.

한자 병기의 진짜 원인은 **입력**이었다 — `[확정 사실]` 블록이 `경신(庚申)` 을 그대로 넣어 줬다.
`saju-api.ts` 에서 뺐다. 나이도 만/세는 둘 다 적어 주게 바꿨다(하나만 주면 모델이 계산하다 틀린다).

### 4. 보안
- 어드민 **5회 실패 → 15분 잠금**(IP 단위, 라이브 검증). 서버리스라 인스턴스가 갈리면
  카운터도 갈린다 — 완전 차단이 아니라 **비용을 수백 배로** 올리는 조치.
- 쿠키에 비밀번호 원문을 넣던 것 → 파생 해시. (기존 어드민 세션은 만료됨)
- `timingSafeEqual` 비교
- **우리 결제 콜백엔 이메일이 없다** — 타이트는 `buyer_email` 을 평문으로 실어 보내는데 우리는 안 샌다.
- 크론은 `CRON_SECRET` Bearer 로 보호 — 정상. 다만 `vercel.json` 이 **하루 1회(정오 KST)** 다.
  낮에 실패하면 자동 복구가 22시간 뒤. Pro 플랜이면 `"0 * * * *"` 로 올릴 것.

### 5. 조판 실측
브라우저에서 직접 재서 고쳤다(눈대중 금지).
- **챕터 앞 여백 38px 이 문단 사이(28px)와 거의 같았다** → 새 章이 시작된 게 안 느껴짐.
  `mt-6`→`mt-12`, 시각공백 62px = 문단 사이의 **2.21배**. (章 간지 카드 다음 챕터는 mt-6 유지)
- 챕터 제목 18 → 19px (본문 15.5 대비 1.16배는 제목으로 안 섰다)
- 렌더된 글자 크기 **14종 → 9종**. 10~13.5px 에만 7종이 몰려 있었다(0.5px 차이는 안 보인다).
- 원국 아래 간지 읽기 10 → 11px (타깃이 3040 여성)
- 위저드 입력 안내·오류 문구 11 → 13px (읽어야 진행되는 기능 문구)

### 6. 티저 컷 + 먹 동그라미
- v2 컷 5장 전부 투입, **세로 4:5 무크롭**(틀 374×467 = 이미지 840×1050, 같은 비율)
- 말풍선 자리 실측: 새 컷 상단 밝기 2.1~14.5 (이전 26~33)
- 전환점 연도의 붉은 원을 **굵기가 변하는 먹 자국**으로 교체 (`ink-circle-path.ts`,
  중심선 마스크로 훑어서 그려지는 연출). 재생성 스크립트가 주석에 통째로 들어 있다.
- 결과지 표지 `cover.webp` 신규(전신 컷, objectPosition 24% 로 갓이 안 잘리게)
- 원본 PNG 는 `design/sangun/` 로 이동 — `public/` 에 두면 방문자에게 서빙되고 배포마다 실린다

---

## ⚠️ 이 세션에서 배운 함정 (다음 세션이 반복하지 말 것)

1. **`npm run build` 를 dev 서버가 켜진 채로 돌리지 마라.** `.next` 가 깨져서 dev 서버가
   "Cannot find module './vendor-chunks/…'" 로 스타일 없이 렌더된다. `preview_stop` → build → 재시작.

2. **typecheck 와 commit 을 한 명령에 묶지 마라.** `npx tsc && git commit` 처럼 묶었다가,
   앞이 실패해도 뒤가 돌아서 **JSX 가 깨진 채로 푸시됐다**(1분간 라이브). 통과를 확인한 **뒤에** 커밋.

3. **JSX 조건식 안에 주석을 넣으면 안 된다.** `{cond && (<주석/><p/>)}` 는 형제 두 개라 파싱 실패.
   주석은 조건식 **밖**으로.

4. **Bash 툴에 PowerShell here-string(`@'…'@`)을 쓰지 마라.** 커밋 메시지에 `@` 가 딸려 들어간다.
   heredoc 을 쓸 것.

5. **브라우저 탭을 새로 만들면 기존 탭이 백그라운드로 밀려 애니메이션이 멈춘다.**
   `getAnimations()` 가 `progress: null` 을 주고 `playState` 는 계속 "running" 이라
   **버그로 오인하기 쉽다.** 측정 전에 `tabs_select` 로 대상 탭을 앞으로.

6. **`getComputedStyle` 이 SVG `stroke-dashoffset` · `background-size` 의 애니메이션 중간값을
   반환하지 않을 때가 있다.** 멈춘 것처럼 보인다 → `element.getAnimations()` 로 확인할 것.

7. **내 측정기를 먼저 의심하라.** 이 세션에서 두 번 틀렸다:
   - "나이 오류 36세" → 오류 아님. 대운 구간이 27~36세라 정상 문장이었다.
     본문의 모든 `NN세` 를 현재 나이로 보던 검사기가 부풀린 것.
   - "이름 3인칭 3건" 중 하나는 문서 제목("산군이 읽은 지수님의 운명 장부") — 정상.

8. **`orders` 에 `user_or_guest` 체크 제약이 있다.** `user_id` 나 `guest_email` 둘 중 하나는 필수.

9. **토스 위젯 iframe 이 있는 페이지는 브라우저 스크린샷이 깨진다.** DOM 은 정상인데 합성만 실패한다
   → `getComputedStyle` 로 정량 측정할 것.

---

## 🧰 이 세션이 남긴 도구

| 파일 | 용도 |
|---|---|
| `scripts/verify-money-path.ts` | 주문→생성→저장→라이브 렌더 전 구간 검증 + 배포 환경변수 점검 |
| `scripts/lint-result.ts` | 결과지 문체 규칙 위반을 규칙별로 측정(치명/경고) |
| `scripts/_apply-norm.ts` | 후처리 정규화기를 기존 md 에 적용해 효과만 확인(재생성 없이) |
| `src/lib/saju/normalize-voice.ts` | 생성 후 문체 강제 교정(파이프라인에 연결됨) |
| `src/components/saju/ink-circle-path.ts` | 붓 자국 경로 + 재생성 스크립트(주석) |
| `design/sangun/README.md` | 원본↔webp 대응표 + 새 컷 받았을 때 절차 |

---

## 커밋 (이 세션)

```
e7a7569 체크리스트에 검증용 결과지 링크 + 정리 방법 추가
cdb4c0e 아침 체크리스트 — Vercel 에서만 할 수 있는 것 3개 + 밤 작업 요약
0127ee3 결과지 문체 — 치명 위반 4종 → 0종 (린터로 측정, 후처리로 강제)
7bdc113 어드민 자물쇠 강화 — 시도 제한 · 쿠키에서 비밀번호 제거 · 타이밍 안전 비교
09c431f 빌드 복구 — JSX 주석을 조건식 안에 넣어 깨뜨린 것 수정
145966c 결과지 조판 실측 — 챕터 위계 복구 + 글자 크기 14종을 7단으로
cf78318 돈 경로 전 구간 검증 + 비회원 고아 방지
27b31f2 산군 v2 컷 2장 + 결과지 표지 — 세로 4:5, 말풍선이 사진 위에 앉는다
1695211 붉은 붓 — 전환점에 동그라미가 그려지고, 결과지 보라 잔재 제거
```
