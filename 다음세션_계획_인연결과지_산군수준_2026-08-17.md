# 실행 계획 — 인연 결과지를 산군 수준으로 (2026-08-17)

> **Opus 실행용 작업 지시서.** 이 문서만 읽고 완주 가능해야 한다. 대화 맥락 불필요.
> 형님 품 **0** — 전 태스크가 자율 완주 대상이다. 막히면 기록 남기고 다음 태스크로.
> 보고는 전체 완료 시 1회. 중간 턴 끊기 금지.

---

## 0. 왜 이 작업인가 (배경 3줄)

- 인연(`inyeon-saju`)은 **지금도 팔리는 상태**다: 활성 · 결제시트 · 생성 엔진 · 확정값 전부 산다.
- 그런데 **산군 11장/9,835자 vs 인연 8장/2,300~2,900자** — 가격은 19,900 vs 17,900으로 거의 같다.
  주력 판매 형태가 **번들(26,900)**이라 구매자가 두 결과지를 **연달아 읽는다** → 「인연은 이게 다야?」 = 환불·실망 리스크.
- 산군 카드에 분량 앵커(「A4 열 장 남짓」, 커밋 `9bb6170`)가 들어가면서 격차가 **화면에 드러난다**. 광고 켜기 전에 메운다.

## 1. 불변 제약 — 어기면 사고

| # | 제약 |
|---|---|
| 1 | **가격·slug·번들 산수 불변**: 17,900/24,900 · 번들 26,900/54,800(=29,900+24,900). R5(가격 인상)는 형님 컨펌 대기 — 이번에 안 한다 |
| 2 | `/api/orders/create` 계약 불변 |
| 3 | **INYEON_VOICE 금지선 전부 유지** (`src/lib/saju/prompt.ts:108-122`): 재혼 우위 · 부부연 박함 · 팜므파탈/성적 매력 · 「결혼 못 한다/늦다/혼자 살 팔자」 · 상대 직업/외모/초성/지역/학력 단정 · 현재 상대/전 애인 사실 단정 · 나이대 양다리 — **하나라도 나가면 환불이다** |
| 4 | 화자는 현행 유지: **먼저 아는 언니, 존대 해요체**. 직녀 캐릭터 전환은 다음 단계(별도 컨펌) — 이번엔 분량·구조만 |
| 5 | `InyeonWebtoon.tsx`의 **붉은 실 디바이저(ThreadDivider, :243-258)는 건드리지 않는다** — R4(베틀·은사 교체)도 다음 단계 |
| 6 | 만세력 API 한도 6,000회 — **캐시된 명식만** 사용(`tmpdir()/analysis-inyeon-saju.json`). 캐시가 없을 때만 첫 1~2콜 허용 |
| 7 | LLM 호출은 샘플 생성에만 (2명 × 10장 + 재시도 여유) |
| 8 | 결제 실행 금지 · 광고/메일 등 외부 발신 금지 |
| 9 | 한자는 장식만, 소비자 문구는 한국어 |

## 2. 현황 실측 (2026-08-17 확인, 파일:라인)

| 항목 | 값 | 위치 |
|---|---|---|
| 인연 outline | **8장** · 「약 2,300~2,900자 (A4 3장)」 | `src/lib/saju/prompt.ts:189-203` |
| 산군 규격 | **11장 × 800~1,000자 = 9,000~12,000자** · 「3040 모바일 완독선」 · 밀도=근거→장면→행동 | `prompt.ts:207-211` |
| 산군 실측 | 한글 **9,835자** (`/dev/sangun-result` 렌더 기준) | 커밋 `9bb6170` 메시지 |
| 챕터 생성 경로 | **슬러그 공통** — `buildChapterPrompts` → `generateByChapters`. 반말 정규화만 산군 분기(`banmal: slug === "sangun-sinjeom"`) | `src/lib/saju/generate-result.ts:323-344` |
| 확정값 블록 | `buildPlanForSangun(slug,…)`이 wealthBlock·**inyeonBlock**·**pastBlock**·prescriptionBlock 공급. 주석 「산군만 태운다」 — **inyeon에 뭐가 오는지 T1에서 확인 필수** | `generate-result.ts:320-339` |
| 랜딩 장수 표기 | 「받는 것 — **8개 챕터 · A4 3장**」 + TocCard rows 8줄 + FAQ 「8개 챕터, A4 세 장」 | `src/components/products/InyeonWebtoon.tsx:212, 196-205, 271` |
| 가격 앵커 | 「점심 한 번 값」 — **유지한다**(산군에서 뺀 근거는 4050 모의구매인데 인연 타깃은 30대) | `InyeonWebtoon.tsx:227` |
| 폴링 창 | selfHeal 8s×8=64s — 11장 실측 26~36s이므로 10장도 **무변경** | `src/app/checkout/success/page.tsx` |
| 샘플 도구 | `scripts/sample-results.ts:45`에 inyeon 케이스가 **주석 처리**돼 있음(지수 33 · 결혼 시기) | 〃 |
| 문체 린터 | `scripts/lint-result.ts --temp` — temp/sample-*.md 검사, 「지어낸달」 규칙 포함 | 〃 |

## 3. 목표 스펙

- **10장 × 850~1,050자 = 약 8,500~10,500자.** 산군의 90% 수준 — 같은 「모바일 완독선」 논리, 인연은 주제가 하나(연애)라 11장까지 늘리면 물타기가 된다.
- 분량은 목표가 아니라 **밀도(근거→장면→행동)의 부산물** — 산군 규격 주석과 같은 원칙을 outline에 명기한다.
- **목차=결과지 1:1** (목차에 있는데 결과지에 없으면 들통 — TocCard 주석 원칙 유지).

### 새 10장 골격 — 기존 확정값만 사용, 새 데이터 발명 금지

| 장 | 제목(방향) | 현행 | 근거 블록 |
|---|---|---|---|
| 1 | 내 인연 그릇 — 점수와 이유 | 1 유지 | [인연 확정값] 점수·끌림 신호·배우자 자리 |
| 2 | ★신설 **당신이 걸어온 연애의 길** — 지나온 해 되짚기 | — | **pastBlock** (산군 2장과 같은 원리. 조사 결론: 과거 검증이 만장일치 매수 조건) |
| 3 | 내가 놓치는 인연의 패턴 — 장면으로 | 2 확장 | 십성 분포·신강도·배우자 자리 |
| 4 | 인연이 들어오는 달 세 개 — 달마다 준비 하나 | 3 유지 | [인연 확정값] TOP3 + [월운] |
| 5 | 내게 올 사람 — 태도·말투·자리·나이대 | 4 유지 | 만날 사람의 결·만나는 길·나이대 |
| 6 | ★신설 **그 사람을 알아보는 신호 셋, 놓치게 만드는 버릇 하나** | — | 만날 사람의 결에서 파생 + **「하지 말 것」**(조사: 4사 전부 있음) |
| 7 | 마음이 흔들리는 달 — 대처로 감싼다 | 5 유지 | [인연 확정값] 흔들리는 달. 부정1:긍정4 유지 |
| 8 | 인연이 가장 크게 바뀌는 해 | 7 유지 | 크게 바뀌는 해 · 10년 흐름 |
| 9 | 내 고민, 정면으로 답해요 — 확답부터 | 6 이동 | 고민 + 확정값 (산군도 물음의 답이 9장 — 데이터 다 깐 뒤의 확답이 세다) |
| 10 | 이번 주에 할 것 3가지 — 가장 가까운 좋은 달과 연결 | 8 유지 | TOP3 중 최근접 달 |

각 장 지시문은 현행 8장 지시문(prompt.ts:194-201)의 **금지·단정 규칙을 그대로 승계**하고, 신설 2·6장만 새로 쓴다. 2장은 산군 「네가 걸어온 길」 지시문을 인연 렌즈(연애·연락·헤어짐의 해)로 번역하되 INYEON_VOICE 금지선(전 애인 사실 단정 금지)과 충돌하지 않게 — **과거의 '시기'만 짚고 '사건'은 독자가 채우게** 쓴다.

## 4. 태스크 큐 (직렬)

### T1. 확정값 공급 확인 (READ만)
`generate-result.ts`의 `buildPlanForSangun` 본문을 읽고 **inyeon-saju 호출 시 inyeonBlock·pastBlock이 실제로 채워지는지** 확인.
- 채워지면 → T2로.
- 산군 전용 게이트면 → **inyeon에 inyeonBlock+pastBlock만 공급하도록 게이트 확장** (wealthBlock·prescriptionBlock은 산군 전용 유지). 함수명이 Sangun 고정이면 이름은 두고 주석만 갱신(호출부 diff 최소화).

### T2. `prompt.ts` — STYLE_BY_SLUG["inyeon-saju"] 교체
- `length: "약 8,500~10,500자 (챕터당 850~1,050자)"` + 「분량은 밀도(근거→장면→행동)의 부산물」 주석
- outline 10개 (§3 골격). 긴 장(2·5·9)은 산군처럼 **「소제목으로 쪼갠다」** 지시 포함
- INYEON_VOICE 수정 금지

### T3. 샘플 생성 2명
`scripts/sample-results.ts` — :45 inyeon 케이스 주석 해제 + 30대 여성 1명 추가(캐시 있는 생일 우선). 실행:
```bash
PYTHONUTF8=1 npx tsx scripts/sample-results.ts
```
산출: `temp/sample-inyeon-*.md` 2건. (LLM 키는 `.env`에 있음 — 이 스크립트는 과거 세션에서 실행 실적 있음)

### T4. 검증 (전부 통과해야 T5)
```bash
npx tsx scripts/lint-result.ts --temp        # 치명 0종
```
- 한글 글자수 ≥ 8,500: `python -c "import re,io;print(len(re.findall(r'[가-힣]',io.open('temp/sample-inyeon-1.md',encoding='utf-8').read())))"`
- 챕터 수 = 10 (`^## ` 카운트, 표지 제외)
- 금지선 grep **0건**: `재혼|박하|팜므|성적 매력|못 한다|혼자 살 팔자|남편복|남자복|연상일 수도`
- 달·해 지어냄 없음(린터 「지어낸달」 규칙 + 눈으로 TOP3 대조)
- 미달 시: outline 지시문을 고쳐 재생성(최대 2회). **글자수를 늘리라고 쓰지 말고 장면·근거를 늘리라고 쓴다**(리포 교훈: 「○○자로 써라」는 채워넣기를 부른다)

### T5. 랜딩·카피 동기화 (`InyeonWebtoon.tsx`)
- `:212` → 「받는 것 — **10장 · 앞으로 12개월 전부**」
- TocCard rows **10줄**로 (§3 골격 제목과 1:1)
- **분량 앵커 라인 신설** — TocCard 가격줄 위. 산군 문구 복붙 금지(같은 결제시트에 나란히 선다 — 중복 문구 금지 원칙). 언니 보이스 예:
  「A4 열 장이에요. 다 읽는 데 스무 분 — 겁주려고 길게 쓴 게 아니라, 끝까지 읽히게 썼어요.」
- `:271` FAQ 답변 → 10장 목차로 갱신
- 「점심 한 번 값」(:227) **유지**
- 전역 grep `8개 챕터|A4 3장|A4 세 장` → 잔여 0건

### T6. 검증 2
```bash
npx tsc --noEmit && npm run build
```
- dev 서버(`preview_start` 이름 `all_saju-dev`, 포트 3000) → 375px → `/products/inyeon-saju` 위저드 경로로 TocCard **실렌더** 확인(게이트·스토리는 「건너뛰기」, 입력은 React 네이티브 setter — 값 대입은 `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set` 사용)
- 좌우 넘침 없음 확인

### T7. 커밋 (main)
```
인연 결과지 8장→10장, 산군과 같은 완독 밀도로

번들(26,900) 구매자가 산군 11장/9,835자 뒤에 인연 8장/2,600자를 연달아 읽는다
— 가격은 2,000원 차이인데 분량이 1/4이라 "이게 다야?"가 나오는 자리.
산군 카드에 분량 앵커(9bb6170)가 들어가며 격차가 화면에 드러나게 됐다.

- outline 10장(신설: 걸어온 연애의 길=과거 검증 / 알아보는 신호 셋+하지 말 것)
- 850~1,050자/장, 근거→장면→행동 밀도 규칙 승계
- 샘플 2건 실측 ○○자·치명 0·금지선 0
- 랜딩 목차 10줄 + 분량 앵커, "8개 챕터·A4 3장" 표기 전량 갱신
```

### P3 (이번 범위 밖 — 기록만)
- D-DAY 실천카드(조사 W9 기준선: 날짜+시간대+장소유형3+옷·향) — 코드 계산(`prescription.ts` 패턴), 실제 상호명 금지
- 직녀 캐릭터 전환(보이스·베틀 매개·티저 12블록) — R4·R8 형님 컨펌 후
- 근거카드 6장(§4-B) — 직녀 단계에서 결과지 조판과 함께

## 5. 완료 정의
샘플 2건 **8,500자↑** · lint 치명 0 · 금지선 grep 0 · 챕터 10 · 랜딩 표기 일치 · tsc/build 통과 · 375px 실렌더 확인 · 커밋 해시.
보고: 실측 글자수 2건 + 바뀐 파일 목록 + 커밋 해시 + (T1에서 게이트를 확장했다면 그 diff 요약).

## 6. 소스 문서
1. `src/lib/saju/prompt.ts` (108-122 보이스 · 189-203 현행 · 207-333 산군 규격/지시문 문법)
2. `인연사주_최종기획_2026-08.md` §4(결과지 골격) · §4-A(하지 말 것) — 단 **직녀 고유 요소(편지·은사)는 이번에 반영하지 않는다**
3. `경쟁사레퍼런스/00_인덱스.md` 실행목록 #34(형용사를 장면으로) #35(과거검증 다리문장) #37(죄책감 해제) — outline 지시문에 쓸 것
4. `인수인계_2026-08-06.md` — 검증 도구 사용법
