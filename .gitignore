node_modules
.next
.next-*
out
# 환경변수/비밀키 — .env.example(placeholder)만 추적, 나머지 .env* 는 전부 무시
.env*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts
# 로컬 스크래치 로그 (커밋 불필요)
*.log
_dev.log
_pnpm_install.log
.vercel
supabase/.branches
supabase/.temp
# 비공개 조사·전략 문서 — 이 리포는 공개(public fork)라 여기 넣은 건 절대 커밋되지 않게 한다.
# (2026-07-30 감사: 가격전략·경쟁사 해부 문서가 이미 공개 푸시된 것을 확인 → 신규분은 전부 여기로)
docs-private/
# 다른 프로젝트 폴더가 워킹트리에 섞여 있어 실수로 딸려 올라가는 것을 막는다
morelle-brand/

# 광고 영상 산출물·재료(대용량) — 드라이브/로컬에서 관리
marketing/소재/산군/영상/*.mp4
marketing/소재/산군/재료/클립/
marketing/소재/산군/재료/캡처/
marketing/소재/산군/재료/UGC/*.png
marketing/소재/산군/재료/UGC/*.mp4
marketing/소재/산군/재료/UGC/gpt/

# 파이썬 캐시
__pycache__/
*.pyc
