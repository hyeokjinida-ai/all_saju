# 아침에 형님만 할 수 있는 것 (2026-08-06 밤 작업 결과)

밤새 코드로 고칠 수 있는 건 다 고쳐서 배포했습니다.
**아래 3개는 Vercel 대시보드에서만 되는 일이라 형님 손이 필요합니다. 다 합쳐 10분입니다.**

---

## 먼저 — 밤새 고친 결과지 실물

https://all-sajub.vercel.app/results/c3c89900-d890-466a-8d2a-2033f4f0a8df

챕터 여백·글자 크기·문체 교정이 다 들어간 실제 결과지입니다.

⚠️ 이건 **검증용 가짜 주문**(`VERIFY-1785954339688-530`, 19,900원 paid)이라
어드민 매출에 잡힙니다. 다 보시고 나면 지워 주세요:

```bash
npx tsx scripts/verify-money-path.ts --only=A
```

(위 명령은 새로 하나 만들어 검증하고 **자기가 만든 것만** 지웁니다.
남은 VERIFY 주문은 어드민 주문 목록에서 `VERIFY-` 로 찾아 지우시면 됩니다.)

---

## 🔴 1. NEXT_PUBLIC_SITE_URL — 지금 운영이 `http://localhost:3000` 입니다 (5분)

### 뭐가 망가져 있나
배포된 사이트에서 이 값이 localhost라, **이 주소로 만들어지는 링크가 전부 죽습니다.**

| 어디 | 지금 나가는 링크 | 결과 |
|---|---|---|
| 회원가입 인증 메일 | `http://localhost:3000/auth/callback` | 클릭해도 안 열림 → **가입 완결 불가** |
| 비밀번호 재설정 메일 | 〃 | 재설정 불가 |
| 결과지 도착 메일 | `http://localhost:3000/results/…` | 결과지 못 봄 |

확인 방법(제가 쓴 것): `curl -i https://all-sajub.vercel.app/admin/logout` → `Location` 헤더에 그 값이 그대로 나옵니다.

### 하는 법
1. https://vercel.com → 프로젝트 → **Settings → Environment Variables**
2. `NEXT_PUBLIC_SITE_URL` 찾기 → **Edit**
3. 값을 `https://all-sajub.vercel.app` 로 (끝에 `/` 없이)
   - 나중에 도메인을 사면 그 도메인으로 다시 바꾸면 됩니다.
4. **Production / Preview / Development 세 군데 다** 체크
5. 저장 → **Deployments 탭 → 맨 위 배포의 ⋯ → Redeploy**
   (환경변수는 **재배포해야** 반영됩니다. 저장만 하면 안 바뀝니다.)

### 확인
```bash
curl -s -i https://all-sajub.vercel.app/admin/logout | grep -i location
```
→ `Location: https://all-sajub.vercel.app/admin/login` 이 나오면 성공입니다.

---

## 🔴 2. RESEND_API_KEY — 비회원 결과지 메일이 아예 안 나갑니다 (5분)

### 왜 중요한가
비회원이 결제 → 결과 생성이 한 번 삐끗 → 탭을 닫으면 **돌아올 길이 사라집니다.**
(마이페이지는 회원만 들어갑니다. 메일도 안 갑니다.)
어젯밤 임시 구제책은 넣어 뒀습니다 — 보류 화면에 "이 주소 저장하세요 + 복사 버튼"이 뜹니다.
하지만 **메일이 진짜 해법**이고, 보내는 코드는 이미 다 짜여 있습니다. 키만 꽂으면 됩니다.

### 하는 법
1. https://resend.com 가입 (무료 월 3,000통 — 우리 규모엔 충분)
2. **API Keys → Create API Key** → 값 복사
3. Vercel → Settings → Environment Variables → **Add New**
   - `RESEND_API_KEY` = 복사한 값
   - `EMAIL_FROM` = `명운록 <onboarding@resend.dev>` (도메인 인증 전 임시)
     ※ 도메인 인증 전에는 **형님 본인 메일로만** 발송됩니다. 실제 고객에게 보내려면
       Resend에서 도메인(Domains) 인증을 해야 합니다 — 도메인 사고 나서 해도 됩니다.
4. Redeploy

---

## 🟠 3. ADMIN_PASSWORD — 아직 `0000` 입니다 (2분)

`/admin` 뒤에 주문·매출·고객 정보가 다 있습니다. 광고 틀어서 사람이 들어오기 시작하면 위험합니다.

밤새 자물쇠는 보강해 뒀습니다:
- 5회 틀리면 15분 잠금 (라이브 검증 완료)
- 쿠키에 비밀번호 원문을 넣던 것 → 해시로 교체
- 타이밍 공격 안전 비교

**그래도 `0000`은 `0000`입니다.** 코드로는 짧은 비밀번호를 길게 못 만듭니다.

### 하는 법
Vercel → Environment Variables → `ADMIN_PASSWORD` → 20자 이상 랜덤으로 → Redeploy.
비밀번호 관리자에 저장하세요. (기존 어드민 로그인 세션은 어차피 만료됩니다 — 다시 로그인하면 됩니다.)

---

## 🟡 4. (선택) 복구 크론이 하루 1번뿐입니다

`vercel.json` 이 `"0 3 * * *"` — **정오(KST)에 하루 한 번**만 돕니다.
낮 2시에 결과 생성이 실패하면 자동 복구가 **22시간 뒤**입니다.

Vercel **Pro 플랜이면** 매시간으로 올리는 게 맞습니다:
```json
"schedule": "0 * * * *"
```
Hobby 플랜은 하루 1회가 한도라 그냥 두시면 됩니다
(그 사이는 어젯밤 넣은 '비회원 복구 주소'가 메꿉니다 — 다시 열면 즉시 재시도됩니다).

---

# 밤새 한 것 (참고)

### ① 돈 경로 — 처음으로 끝까지 태웠습니다
지금까지 실결제 후 결과지까지 완주한 주문이 **0건**이었습니다. 검증 스크립트를 만들어
주문(paid) → 명식 → LLM → 저장 → **배포본 렌더**까지 실제로 돌렸습니다.

**결론: 파이프라인 자체는 멀쩡합니다.**
- 9챕터·명식·분석값 전부 저장, 라이브 200 + 표지 + 章간지 + 75KB 렌더 확인
- 시각 모름 / 음력 / 이름 없음 / 회원·비회원 경로 전부 통과
- 멱등성 확인(중복 호출해도 재생성 안 함)
- 생성 시간 8~19초

문제는 코드가 아니라 **위의 환경변수 2개**였습니다.

다시 돌리려면:
```bash
npx tsx scripts/verify-money-path.ts
```

### ② 결과지 문체 — 치명 위반 4종 → 0종
규칙마다 검사기를 붙인 린터를 만들어 전후를 숫자로 비교했습니다.

| 항목 | 전 | 후 |
|---|---|---|
| 존대 "당신" | 2 | **0** |
| 이름 3인칭 ("지수의") | 2 | **0** |
| 하라체 ("활용하라") | 5 | **0** |
| 한자병기 ("경신(庚申)") | 4 | **0** |
| 내부값 노출 ("대길·대흉") | 5 | **0** |
| 헷지 ("가능성이 크다") | 14 | 7 |
| 형광펜 규칙 위반 | 5장 | 1장 |

프롬프트로 부탁해서는 안 지켜지는 것들(어미 형태 등)은 **후처리 코드로** 못 박았습니다.

```bash
npx tsx scripts/lint-result.ts --temp
```

**정정합니다**: 제가 전에 보고한 "나이 계산 오류 36세"는 **오류가 아니었습니다.**
대운 구간이 27~36세라 "36세까지 이어진다"가 맞는 문장이었고, 제 검사기가 본문의 모든
'NN세'를 현재 나이로 착각한 것이었습니다. 검사기를 고쳤습니다.

### ③ 보안
- 어드민 5회 실패 → 15분 잠금 (라이브 검증)
- 쿠키에서 비밀번호 원문 제거
- **우리 결제 콜백엔 이메일이 없습니다** — 타이트는 `buyer_email`을 평문으로 실어 보내는데
  우리는 안 샙니다. 점검 완료.
- 크론은 CRON_SECRET으로 보호돼 있습니다 — 정상.

### ④ 디자인 실측
브라우저에서 직접 재서 고쳤습니다(눈대중 금지).
- **챕터 앞 여백이 38px로 문단 사이(28px)와 거의 같았습니다** → 새 章이 시작된 게 안 느껴짐.
  48px로 벌려 시각공백 62px(문단 사이의 2.2배)로.
- 챕터 제목 18 → 19px (본문 15.5 대비 1.16배는 제목으로 안 섰습니다)
- 렌더된 글자 크기가 **14종**인데 10~13.5px에만 7종이 몰려 있었습니다(0.5px 차이는 안 보입니다).
  → 34 / 24 / 20 / 19 / 15.5 / 13 / 11 의 7단으로 정리
- 원국 아래 간지 읽기 10px → 11px (타깃이 3040 여성입니다)
- 위저드 입력 안내·오류 문구 11 → 13px (읽어야 진행되는 기능 문구입니다)

**못 한 것**: 타이트 티저는 퍼널 상태 없이 URL로 못 들어가고(게이트로 리다이렉트) 게이트
입장 버튼이 자동 클릭에 반응하지 않아, 그쪽 타이포 수치를 다시 재지 못했습니다.
숫자를 지어내느니 못 했다고 적습니다.

### ⑤ 티저 컷 + 먹 동그라미 (어제 저녁분)
- 형님이 뽑아 주신 v2 컷 5장 전부 투입, 세로 4:5 무크롭
- 전환점 연도의 붉은 원을 **굵기가 변하는 먹 자국**으로 교체

---

## 아직 필요한 것

- **운명의 상대 얼굴 10장** (`partner-{m|f}-{wood|fire|earth|metal|water}.webp`) — 지금은 회색 실루엣입니다.
  프롬프트 10종은 `이미지가이드_운명의상대.md` 에 있습니다.
  프롬프트는 `이미지지시서_티저컷.md` 에 있습니다.
- 소셜프루프 숫자 (`src/config/social-proof.ts` 가 비어 있습니다)
- 토스 PG 심사 결과 확인 → 승인되면 `PG_REVIEW_MODE=false`
