# 로고 배선 2차 — 사이트에 남은 자리 전부 (Fable 계획 · Opus 실행)

> 1차(8/23 낮)에서 헤더 3곳·파비콘·apple-icon·OG·결과지 마크를 붙였다(`로고_기획_2026-08-23.md` §8).
> 이 문서는 **그 뒤 남은 자리**를 스캔해 Opus 가 그대로 실행할 수 있게 앵커·확정 문자열·검증까지 적은 것.
> 형님 몫(메타 프로필 업로드)은 여기 없다. 전부 코드.
>
> 자산은 이미 있다 — `public/brand/*.png`, 컴포넌트 `src/components/brand/Logo.tsx`(`Logo`·`LogoSymbol`·`LogoSeal`).
> **자산을 손으로 고치지 말 것.** 부족하면 `design/brand/build_assets.py` 를 고쳐 다시 굽는다.

## 0. 실행 전 확인 (Opus 첫 3분)

```bash
git status --short                 # 1차 변경이 미커밋 상태로 있어야 정상 (Logo.tsx · 헤더 3곳 · public/brand · src/app/icon 등)
netstat -ano | findstr :3000       # dev 서버 살아 있으면 npm run build 금지 (nextjs-dev-build-conflict)
cd design/brand && python verify_assets.py   # 16/16 통과가 출발점
```

## 1. 작업 목록 (위에서부터 순서대로 — 앞이 뒤의 전제)

| # | 자리 | 지금 | 바꿀 것 | 왜 |
|---|---|---|---|---|
| A | `SAJU LAB` 영문 라벨 6곳 | 글자로 남아 있음 | **제거** | 로고에 영문 0. 라벨만 남으면 옛 브랜드가 두 겹 |
| B | 결과지(자수정) 머리 `ResultScroll` | 글자 「명운록 / SAJU LAB」 | 가로 락업 `logo-h-ivory.png` | 결과지 첫 화면 = 브랜드가 제일 오래 보이는 자리 |
| C | 결과지 3종 맺음 「기록 완료」 도장 | 없음 (산군은 낙관 찍는 *사진*만) | `LogoSeal` 한 개 | 기획서 §2 「B 인장은 결과지 말미 도장으로」 — 錄(기록)의 마침표 |
| D | 광고 착지 `StoryFooter` | 법정 표기만, 로고 0 | 맨 위에 `Logo` h20 | **유입 100%가 떨어지는 곳**에 브랜드가 없다 |
| E | 자수정 랜딩 `SajuLabLanding` 앱바 | 글자 「명운록 / SAJU LAB」 | `Logo` h24 | 헤더와 같은 얼굴 |
| F | 결과지 이메일 `email.ts` | 글자 「명운록 / SAJU LAB」 | `<img>` 절대 URL 락업 | 카톡 다음으로 오래 남는 브랜드 접점 |
| G | PWA `manifest.ts` | 없음 | 신설 (아이콘 192·512) | 안드로이드 「홈 화면에 추가」 = 카톡 인앱 손님의 재방문 경로 |
| H | 빌드·커밋 | 미빌드·미커밋 | dev 서버 내려간 뒤 `npm run build` → 커밋 1개 | 배포본엔 아직 옛 헤더 |

**안 하는 것**: 산군·직녀 결과지 *머리*에 로고 얹기(두 결과지는 캐릭터가 주연이고 이미 자체 머리가 있다 — 도장 C 만 찍는다) ·
로그인/404/에러 페이지(브랜드 글자 없음, 건드릴 이유 없음) · 세계관별 로고 색 변형(우산 원칙, `myeongunrok-logo` 메모리).

## 2. 작업별 앵커 · 확정 문자열

### A. 「SAJU LAB」 제거 (6곳)

| 파일:행 | 지금 | 바꿈 |
|---|---|---|
| `src/lib/saju/result-view.ts:276` | `brand: { title: "명운록", sub: "SAJU LAB" },` | `brand: { title: "명운록", sub: "" },` |
| `src/lib/saju/result-view.ts:292` | 〃 (DEMO) | 〃 |
| `src/components/saju/ResultScroll.tsx:384` | `명운록 · SAJU LAB · 평생 다시 보기` | `명운록 · 평생 다시 보기` |
| `src/components/landing/saju-lab/SajuLabLanding.tsx:51-58` | 앱바 div 2개 | E 에서 통째로 교체 (여기선 손대지 않음) |
| `src/lib/email.ts:22` | `<div …>SAJU LAB</div>` 한 줄 | F 에서 통째로 교체 |
| `src/lib/email.ts:35` | `명운록 · SAJU LAB` | `명운록` |

`ResultView.brand.sub` 타입은 `string` 그대로 두고 빈 문자열. B 에서 머리를 락업으로 바꾸면 `sub` 는 렌더에서 사라진다 —
타입까지 지우면 `result-view.ts` 를 읽는 다른 곳(`grep -rn "brand\." src`)이 깨질 수 있으니 **필드는 남기고 값만 비운다.**
주석 속 「SAJU LAB」(`globals.css:703`, `SajuLabLanding.tsx:3`, `SangunResult.tsx:8`)은 역사 기록이라 **그대로 둔다.**

### B. 자수정 결과지 머리 → 가로 락업

`src/components/saju/ResultScroll.tsx:205-210` 현재:
```tsx
{/* 헤더 — 랜딩과 동일하게 중앙 워드마크, 공유는 우측 상단 */}
<div style={{ position: "relative", display: "flex", flexDirection: "column", alignItems: "center", padding: `16px ${PAD}px 0` }}>
  <div style={{ fontFamily: "var(--font-myeongjo-nanum),serif", fontWeight: 800, fontSize: 18, letterSpacing: ".1em", color: "#f3edff" }}>{view.brand.title}</div>
  <div style={{ fontSize: 8.5, fontWeight: 700, letterSpacing: ".22em", textIndent: ".22em", color: "#c9a8ff", marginTop: 3 }}>{view.brand.sub}</div>
  {!locked && <span …>⤴</span>}
</div>
```
바꿈 — 글자 div 2개를 `<img>` 하나로:
```tsx
{/* 헤더 — 브랜드 락업(命 원 + 붓글씨). 글자 워드마크는 2026-08-23 로고 확정으로 은퇴 */}
<div style={{ position: "relative", display: "flex", justifyContent: "center", padding: `16px ${PAD}px 0` }}>
  {/* eslint-disable-next-line @next/next/no-img-element */}
  <img src="/brand/logo-h-ivory.png" alt={view.brand.title} width={96} height={35} style={{ width: 96, height: "auto" }} />
  {!locked && <span …>⤴</span>}   {/* 기존 줄 그대로 */}
</div>
```
`logo-h-ivory.png` 는 597×220 → 폭 96 이면 높이 35. 기존 글자 머리(18px + 8.5px + 3px ≈ 30px)와 같은 키라 아래 오브 위치가 안 밀린다.
**`Logo` 컴포넌트를 안 쓰는 이유**: `ResultScroll` 은 인라인 style 전용 파일이고 락업은 `Logo` 에 없다(`Logo` 는 워드마크 단독). 새 variant 를 만들지 말고 `<img>` 한 줄.

### C. 「기록 완료」 도장 — 결과지 3종 맺음

공용 부품 하나를 만들고 세 곳에서 쓴다.

**신설** `src/components/saju/ResultSealOff.tsx`:
```tsx
// 결과지 맺음 — 「기록 완료」 낙관. 錄(기록)의 마침표이자 재열람 씨앗.
// 산군 맺음 컷(낙관 찍는 손 사진) 바로 아래, 직녀·자수정은 본문 끝에 찍는다.
import { LogoSeal } from "@/components/brand/Logo";

export function ResultSealOff({ date, tone = "night" }: { date: string; tone?: "night" | "ink" | "paper" }) {
  const color = tone === "paper" ? "#5b4e3c" : "rgba(243,234,214,0.55)";
  return (
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 8, padding: "28px 20px 8px" }}>
      <LogoSeal size={56} />
      <div style={{ fontSize: 11, letterSpacing: ".08em", color }}>{date} · 명운록이 기록함</div>
    </div>
  );
}
```
`date` 는 결과 생성일 — 세 호출부가 이미 가진 값으로 넣는다(없으면 `new Date().toLocaleDateString("ko-KR")` 로 시작하되 **주문일이 있으면 그걸로** — 「기록」은 그날의 것이어야 한다).

| 호출부 | 넣는 자리 | tone |
|---|---|---|
| `SangunResult.tsx` 맺음 `<ResultCut src="/products/sangun/close.webp" …/>` **바로 아래**, `ResultReviewCTA` **위** | 낙관 찍는 손 사진 → 진짜 낙관 → 후기 질문. 순서가 이야기다 | `ink` |
| `JiknyeoResult.tsx:414` `{reviewOrderId && <ResultReviewCTA …/>}` **바로 위** | | `night` |
| `ResultScroll.tsx` 푸터 블록(`{!locked && (…)}`) 안, PDF/공유 버튼 **위** | 잠금(무료)에선 안 찍는다 — 기록이 끝나지 않았다 | `night` |

### D. 광고 착지 `StoryFooter` 에 로고

`src/components/products/StoryFooter.tsx:24` `<footer …>` 첫 자식으로:
```tsx
<div className="mb-5 flex justify-center">
  <Logo tone="ivory" height={20} className="opacity-70" />
</div>
```
import 추가: `import { Logo } from "@/components/brand/Logo";`
착지 3종(`/products/sangun-sinjeom` · `/products/inyeon-saju` · `/products/wealth-saju`) 전부 이 푸터를 쓰므로 한 번에 붙는다.
⚠ 푸터는 스크롤 맨 끝이라 **착지 첫 화면엔 여전히 로고가 없다.** 첫 화면에 로고를 넣을지는 몰입형 설계(자체 앱바 없음)를 깨는 일이라 **이 계획 밖** — 산군 광고 첫 주 데이터 본 뒤 판단.

### E. 자수정 랜딩 앱바

`src/components/landing/saju-lab/SajuLabLanding.tsx:51-58` 의 div 2개 →
```tsx
{/* app bar — 브랜드 워드마크 */}
<div className="relative z-30 flex flex-none justify-center pb-1 pt-6">
  <Logo tone="ivory" height={24} priority />
</div>
```
import 추가. 이 파일은 `"use client"` 인지 확인하고(`Logo` 는 서버/클라 무관) 기존 클래스 `flex-col items-center` → `justify-center` 로만 바꾼다.

### F. 이메일 락업

`src/lib/email.ts:21-22` 두 div →
```html
<img src="${siteUrl()}/brand/logo-h-ivory.png" alt="명운록" width="120" height="44" style="display:block;margin:0 auto;width:120px;height:auto;" />
```
`siteUrl()` 은 같은 파일 12행에 이미 있다. **운영 `NEXT_PUBLIC_SITE_URL` 이 localhost 면 이미지가 깨진다** — 런칭 블로커 메모리(`myeongunrok-launch-blockers`)에 이미 있는 항목이라 여기서 새로 만들지 않고, 비어 있으면 **글자 폴백**을 남긴다:
```ts
const logo = siteUrl() ? `<img …>` : `<div style="font-family:serif;font-weight:800;font-size:20px;letter-spacing:.08em;color:#f3edff;">명운록</div>`;
```
35행 `명운록 · SAJU LAB` → `명운록`.

### G. PWA manifest

**신설** `src/app/manifest.ts`:
```ts
import type { MetadataRoute } from "next";
import { siteConfig } from "@/config/site";

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: siteConfig.name,
    short_name: siteConfig.name,
    description: siteConfig.description,
    start_url: "/",
    display: "standalone",
    background_color: "#0e0e10",
    theme_color: "#0e0e10",
    icons: [
      { src: "/brand/icon-192.png", sizes: "192x192", type: "image/png" },
      { src: "/brand/icon-512.png", sizes: "512x512", type: "image/png", purpose: "maskable" },
    ],
  };
}
```
아이콘 2장은 **`build_assets.py` 에 추가해 굽는다** (손으로 리사이즈 금지):
```python
# §4 파비콘·앱아이콘 블록 끝에
save(fit_square(symbol_ink, 192, 0.70, bg=IVORY).convert("RGB"), PUB / "icon-192.png")
save(fit_square(symbol_ink, 512, 0.62, bg=IVORY).convert("RGB"), PUB / "icon-512.png")   # maskable = 안전영역 80% 안
```
`verify_assets.py` 파일 규약 블록에 두 줄 추가(`(PUB/"icon-192.png",(192,192))`, `(PUB/"icon-512.png",(512,512))`).
Next 가 `<link rel="manifest">` 를 자동으로 넣는다 — `layout.tsx` 수정 없음.

### H. 빌드 · 커밋

1. `netstat -ano | findstr :3000` 이 비어 있을 때만 `npm run build`. 살아 있으면 **빌드 생략하고 `npx tsc --noEmit` 로 대신** — 그리고 보고에 「빌드 미실행」을 명시.
2. 커밋 1개:
   ```
   로고 확정·배선 — 붓글씨 명운록 + 命 원 + 주사 낙관

   비어 있던 파비콘·apple-icon·OG·manifest 전부 채움. 헤더 3곳·결과지 머리·도장·광고착지 푸터·이메일.
   SAJU LAB 영문 라벨 은퇴. 구 족자 SVG 삭제. 자산은 design/brand/build_assets.py 가 굽는다.
   ```
   push 는 **형님 한마디 후**(배포가 따라온다).

## 3. 검증 (숫자로 — 눈대중 금지)

```bash
# 1. 자산 게이트
cd design/brand && python build_assets.py && python verify_assets.py        # 18/18 (G 추가분 포함)
# 2. 타입
npx tsc --noEmit
# 3. 영문 라벨 0
grep -rn "SAJU LAB" src --include=*.ts --include=*.tsx | grep -v "^\S*:\s*//"   # 주석 제외 0건
# 4. 라이브 (dev 서버 3000)
#    /results/demo         → 머리에 <img src="/brand/logo-h-ivory.png"> 96×35, 푸터 위 LogoSeal 56
#    /products/sangun-sinjeom → 푸터 첫 자식 wordmark-ivory 20px
#    /manifest.webmanifest → 200, icons 2개
#    /brand/icon-192.png /brand/icon-512.png → 200
```
헤드리스 캡처 명령은 메모리 `headless-chrome-render` (프로젝트 밖 file:// 는 브라우저 패널이 못 읽는다).
라이브 증거는 `design/brand/logo-live.png` 에 **덧붙여** 한 장으로(1차 시트 만든 방식 — `scratchpad` 의 PIL 스크립트 참고).

## 4. 예상 함정

- `ResultScroll` 머리 높이가 바뀌면 **일간 오브 y 가 밀려** 티저 캡처 기준선이 어긋난다 → B 에서 `height` 35 고정, 바꾸면 `measure:teaser` 재실측.
- 이메일 `<img>` 는 Gmail 이 프록시로 캐시한다 → 자산을 다시 구워도 옛 그림이 보일 수 있다. 파일명을 바꾸지 말고 **쿼리스트링 없이** 그대로(Next 가 안 붙임).
- `manifest.ts` 의 `theme_color` 는 홈 검정(`#0e0e10`)과 맞춘 것 — 자수정 페이지에서 안드로이드 상단바가 검정이 되는데, 홈이 검정이라 그게 맞다.
- 도장 C 를 **잠금(무료) 결과지에 찍지 말 것** — 「기록 완료」가 거짓이 된다(퍼널 CTA 가 그 자리를 쓴다).

## 5. 끝나면

- `로고_기획_2026-08-23.md` §8 「남은 것」에서 2·3번을 ✅ 로.
- 전선보드 명운록 행: 「로고 2차 배선 완료 · push 대기」.
- 형님 보고: 라이브 시트 1장 + 「push 할까요」 한 줄. 그 이상 묻지 말 것.

---

## ✅ 실행 완료 (2026-08-23, Opus) — 커밋 `60f4c90`

A~H 전부 완료. 52 파일 · +1747/−50.

| # | 결과 |
|---|---|
| A | 「SAJU LAB」 6곳 은퇴 — 코드·화면 **0건**(주석 속 역사 기록만 남김) |
| B | 결과지 머리 = `logo-h-ivory.png` 96×35 (옛 글자 조판 30px 과 키 맞춤 → 오브 안 밀림) |
| C | `ResultSealOff` 신설 → 자수정·산군·직녀 3종 맺음에 낙관. `recordedAt` 은 `result.created_at` 에서 내려온다. 잠금 결과지엔 안 찍힘 |
| D | 광고 착지 3종 푸터에 워드마크 h20 |
| E | 자수정 랜딩 앱바 교체(현재 라우트 미사용 — 되돌릴 때 붙음) |
| F | 이메일 락업 + `brandMark()` 폴백. SITE_URL 빈값·localhost → 글자, 운영 도메인 → 이미지 (3경우 실측) |
| G | `manifest.ts` 신설 + `icon-192/512` maskable. `/manifest.webmanifest` 200 |
| H | dev 서버가 3000 에 살아 있어 **build 생략**, `tsc --noEmit` 로 대체. 커밋 1개 · **push 는 형님 대기** |

### 실행 중 잡은 결함 1건 (계획에 없던 것)

**낙관 주위에 회색 판이 비쳤다.** `seal_rgba()` 가 알파를 **밝기**로 만들어서 한지(밝은 베이지)가
약한 알파로 살아남았고, 어두운 결과지 바탕에서 네모 판으로 보였다.
→ 알파를 **채도(붉은 정도)** 로만 만들도록 고침. 종이 잔존 1.4% 로 떨어지고, 덤으로
   파비콘 잉크 면적이 31% → 69% 로 좋아졌다. 게이트에 이 항목이 없었던 게 원인 —
   `verify_assets.py` 에 「어두운 바탕 종이 잔존」 게이트를 넣을지는 다음 판단.

### 검증 기록

게이트 19/19 · `tsc` 통과 · SAJU LAB 0건 · 라우트 6개 200 · `/manifest.webmanifest` 200 ·
결과지 3종 도장 렌더 확인 · 이메일 폴백 3경우 · 증거 `logo-live2.png`.

### 남은 것

1. **push** — 형님 한마디. 그 뒤 Vercel 자동 배포.
2. **`npm run build` 1회** — 다른 세션 dev 서버 내려간 뒤.
3. 형님 몫: 메타 페이지 프로필에 `public/brand/symbol-512-circle-safe.png` 업로드.
4. 광고 착지 **첫 화면** 로고는 여전히 없음(푸터만) — 몰입형 설계를 깨는 일이라 산군 첫 주 데이터 뒤 판단.
