# 홈 메인(청월당 뼈대 × 타이트 검정) + 상품 빌더 — 실행 계획서 (2026-08-23, 검토 2판)

> 작성: Fable(계획 전용) · 실행: Opus. 형님 "고고" 후 이 문서만 보고 그대로 구현한다.
> 판정선: **변장 모드**(`docs-private/직녀_디자인/레퍼런스_복제_작업법.md`) —
> ① 구조 대조: **청월당 홈**과 치수·리듬 일치(±6px) ② 표면(색·톤): **타이트** 다크 토큰 ③ 문장·이모지 제목·캐릭터는 우리 것(두 회사 문장 재사용 0).
> 형님 지시: 「검정색 배경으로, 타이트처럼」 · 「상품을 추가/관리하는 빌더」 → Part A 홈 · Part B 빌더.

## ★ 검토 2판에서 고친 것 (1판 대비) — 코드로 확인한 결과
| # | 1판의 가정 | 확인 결과 | 수정 |
|---|---|---|---|
| R1 | 후기에 `profiles.display_name` 을 join 해 이름을 쓴다 | ❌ `profiles` RLS = **본인만 select** → anon 클라이언트에선 0건 | 홈의 상품·후기 읽기는 **`createServiceClient()`**(서버 전용). 게스트 후기는 `guest_email` 앞 2자 + `****` |
| R2 | `next/image` 로 2MB PNG 를 최적화 서빙 | ❌ 코드베이스 전체가 `<img>` 이고 **`sharp` 미설치**(Next 15.5 는 sharp 없으면 최적화 불가) | 카드 크기 webp 를 **스크립트로 사전 생성**(`public/home/`), 런타임은 `<img>`. `sharp` 는 dependencies 에 추가(빌더 업로드 리사이즈에도 필요) |
| R3 | 홈 select 에 Part B 컬럼을 바로 넣는다 | ❌ 마이그레이션 전 배포에서 **홈이 통째로 죽는다**. 상품 페이지는 이미 이 함정을 피해 select 를 쪼개 놓음(`[slug]/page.tsx` 142행 주석) | A 의 select 는 기존 컬럼만. B 컬럼은 **별도 쿼리, 실패하면 조용히 폴백**. 배포 순서: 마이그레이션 → B |
| R4 | `style`(결과지 목차·말투) 을 `products` 컬럼에 | ⚠ `products` 는 **public read** → anon REST 로 프롬프트 설계가 노출 | **`product_styles` 별도 테이블**(RLS on, 정책 없음 = 서비스 키만). `pitch` 는 랜딩에 찍히는 공개 카피라 products 에 둠 |
| R5 | 권위 문구 "철학관 대운표와 대조" | ⚠ 실제 대조 대상은 **포스텔러(만세력 앱)** — 사실과 다름 | 「다른 만세력과 대운 간지를 대조해 검증한 계산으로 풉니다」 |
| R6 | 홈 루트 div 가 검정을 깔면 끝 | ⚠ `body` 전역 배경이 **자수정 그라데이션**(`globals.css` 270행) → iOS 바운스·짧은 화면에서 보라색이 비침 | `HomeShell` 마운트 시 `document.body.style.background='#18191A'`, 언마운트 복구(홈·/products 에서만) |
| R7 | 제목 "명운록 — 박수무당 사주…" | ⚠ layout 템플릿 `%s \| 명운록` → "…\| 명운록" 중복 | `title: { absolute: … }` |
| R8 | h2 「풀이 세 가지」 | ⚠ 상품 늘면 거짓이 됨 | 「✦ 먼저 보고 가는 풀이」 |
| R9 | 앵커 탭 스크롤 | ⚠ 고정 헤더 104px 에 제목이 가려짐 | 행 section 에 `scroll-mt-[120px]` |
| R10 | 카드 링크에 `?from=home-…` | ⚠ `?from=jiknyeo` 가 직녀 몰입형에서 **게이트 건너뛰기** 의미로 이미 쓰임 | `?via=home-hero \| home-big \| home-<row>` (페이지뷰가 쿼리까지 기록 → 행별 전환이 `/admin/analytics` 에 남음 = 청월당 main_page/main_page_2 구분) |
| R11 | 탭바 「전체 풀이」→ `/products` | ⚠ `/products` 는 자수정·금색 전역 헤더 + 와인 카드 → 검정 홈에서 누르면 딴 사이트 | **A-9**: `/products` 를 같은 `HomeShell` + `ProductCard` 2열 격자로 교체(ChromeGate bare 에 `/products` 추가). 기본 포함 |
| R12 | B 검증에서 테스트 상품 활성화 | ⚠ dev 도 **운영 Supabase** → 운영 `/products` 목록에 잠깐 노출 | 활성 테스트는 5분 안에 끝내고 삭제. 나머지는 비활성 상태로 어드민 미리보기 검증 |
| R13 | scroll-snap 히어로 | ⚠ 데스크톱 마우스로는 못 넘김(터치 전용) | 점 클릭 이동 + 자동 넘김. 유입 100% 모바일이라 허용. 청월당의 3D 카드 스택 효과는 **의도적으로 생략**(판정선 밖) |
| R14 | `shoot.ps1` 로 우리 홈 촬영 | ⚠ 도구가 `file://` 전용 | `shoot-url.ps1` 5줄 추가(URL 인자) |
| R15 | 업로드 5MB | ⚠ Vercel route 본문 한도 4.5MB | **4MB** |
| ✓ | 확인돼 그대로 둔 것 | 범용 템플릿은 pitch 없어도 안전(`pitch?.`) · 위저드/티저는 특수 slug 외 기본 경로 있음 · 로그인 칩 문구는 사실(게스트 주문도 이메일 매칭으로 보관함에 뜸, `mypage/orders` 20행) · `/login` 존재 · Pretendard 가 이미 body 기본 · `images.remotePatterns` 에 supabase 허용됨 | — |

---

## 0. 왜 만드나 (한 줄)

지금 `/` 는 자수정 풀스크린 단일 CTA(→ `/funnel`, 파는 상품 life-saju 는 **비활성**) — 상품이 3개인데 홈이 하나만 가리킨다.
홈 = **카탈로그 + 재구매 허브**(로고 탭·결과지 크로스셀·오가닉). 광고 착지는 여전히 `/products/*` 몰입형(변경 없음).
빌더 = 상품 추가가 **코드 수정 없이 어드민에서** 끝나게(카드·랜딩 카피·결과지 목차까지 DB).

## 1. 확정 기본값 (형님이 안 바꾸면 이대로)

| # | 결정 | 기본값 | 이유 |
|---|---|---|---|
| D1 | 바탕 | **검정 앱셸 — 타이트 토큰**(§2-3). 구조는 청월당 | 형님 지시. 우리 카드(산군 촛불·직녀 밤)는 검정 위에서 가장자리가 녹음 → 카드에 `white/10` 헤어라인 |
| D2 | 하단 탭바 | **타이트식 떠 있는 알약** 3탭: 홈 · 전체 풀이(`/products`) · 보관함(`/mypage`) | 검색·오늘의운세는 우리에게 없음 |
| D3 | 기존 자수정 랜딩 | `/` 에서 내리고 **파일 보존**(`SajuLabLanding.tsx` 미사용) · `/funnel` 은 손대지 않음 | 되돌리기 1줄 |
| D4 | 산군 카드 제목 | `박수무당 사주` 를 SVG `textLength` 로 폭 맞춤 | 청월당은 4글자 제목 |
| D5 | 후기 | 실후기 DB **3건 이상**일 때만 섹션 등장 | 샘플 후기 노출 금지(`SHOW_SOCIAL_PROOF=false`) |
| D6 | 상품 소스 | Supabase `products`(is_active & !is_addon, 폴백 seed). B 컬럼은 별도 쿼리(R3) | 빌더에서 켜면 홈이 저절로 찬다 |
| D7 | 빌더 범위 | 카탈로그 + 홈 카드 + 랜딩 카피 + 결과지 목차(§B). 전용 웹툰 랜딩(Story)·전용 결과지 템플릿은 밖 | 신규 상품은 기존 **범용 템플릿**으로 바로 판매 가능(확인됨) |
| D8 | 삭제 | 주문 1건이라도 있으면 **삭제 불가(비활성만)** | orders/saju_results 참조 |
| D9 | 홈 카드 가격 | **표시 안 함** | 청월당·타이트 모두 홈 무가격 — 가격은 티저 뒤 결제 시트에서(페이월 공식) |
| D10 | 이미지 | 사전 생성 webp + `<img>`(R2). 크기: hero 864×1080 · big 600×752 · row 400×500 (2x) | 코드베이스 관례 유지, LCP |

---

# Part A — 홈 메인

## 2. 실측표

### 2-1. 청월당 뼈대 (2026-08-23, 뷰포트 1280 · 컨테이너 max-w-md=448) — 치수는 이 표 그대로
| 요소 | 실측 |
|---|---|
| 컨테이너 | `max-w-md`(448) 가운데. 본문 `px-5`(20) |
| 헤더 | `fixed top-0 h-[60px] max-w-md`, `px-5 flex justify-between`. 로고 h-6(실측 83×24, x=20 y=18). 우측 아이콘 버튼 28×28 |
| 카테고리 탭 | `fixed` 헤더 아래 **h-44**, `px-5 flex items-end justify-between`. 탭 `px-2 pt-3 text-base tracking-[-2.5%]`; 활성 `font-bold border-b-2 pb-3.5`, 비활성 `font-medium pb-4` |
| 본문 시작 | 콘텐츠 y=104 (60+44) |
| 히어로 | `section.pt-6` → 슬라이드 y=128. 슬라이드 **432×540**(x=8, `w-[calc(100%-16px)]`, 4:5), `rounded-2xl`(16) · TOP 뱃지 36×46 `absolute right-3 top-0` · 점 `mt-4`: 버튼 12×12 안 점 6×6, margin 1px |
| 구분선 | `mb-10 mt-6`(1px) · 이후 `my-10` · 굵은 구분 `my-12`(4px) |
| 섹션 A(큰 카드) | h2 `ml-6 mb-2 text-xl(20) font-bold leading-[130%]` · 안내칩 `ml-5 rounded-[4px] p-2 text-xs font-bold`(실측 199×32) · 스크롤러 `mt-4 pl-5 flex gap-3`, 카드 `w-[70%]`(실측 **300×376**) `aspect-[295/370] rounded-xl`(12) |
| 권위 블록 | 알약 `mx-auto mb-[18px] rounded-full px-3 py-1.5 text-[10px] font-semibold` · h3 `text-lg(18) font-bold text-center mb-3.5` |
| 후기 | `section.mt-16` · 머리 `px-5 flex items-end justify-between`: h3 `px-1 text-xl font-bold` · 스크롤러 `mt-4 pl-5 gap-3` · 카드 **256×153** `rounded-lg px-4 py-3` · 아바타 **44×44** 원형 · 별 5개 **16px** · 시각 `text-xs` 우측 · `이름 \| 상품명` `text-xs` · 본문 `mt-2 text-xs leading-[150%] min-h-16` |
| 카테고리 행 묶음 | `flex flex-col gap-12`(48) · 행 h3 `mb-4 px-5 text-lg(18) font-bold truncate` · 스크롤러 `pl-5 pr-5 flex gap-3` · 카드 `w-[44%]`(실측 **180×283** 이미지, 전체 323) `aspect-[295/370] rounded-lg`(8) · 글 `mt-3 px-1 gap-1`: h5 `text-base(16) font-semibold truncate` / p `text-sm(14) font-medium line-clamp-1` |
| 푸터 | `px-4 py-10 pb-24 max-w-md` · 로고 `h-6 mx-auto mb-7` · 정보 `text-[10px] gap-3`, `strong` semibold, 세로 구분 `h-3 mx-2` · 링크 `mt-8 text-xs font-semibold gap-3` · 저작권 `text-xs` |

### 2-2. 히어로 카드 레터링 (청월당 원본 688×861 PNG 픽셀 스캔 → 비율)
| 층 | 잉크 박스(원본) | 비율 | 432×540 환산 |
|---|---|---|---|
| 캐릭터명 | y 521–560 (h39), x 274–415 (w141), 가운데 | top 60.5%·h 4.5%·w 20.5% | y≈327, 잉크 24px → 폰트 ≈28px |
| 제목 | y 590–724 (h134), x 97–569 (w472) | top 68.5%·h 15.6%·w 68.6% | y≈370, 잉크 84px, **폭 296px 고정**(textLength) |
| 한 줄 카피 | y 767–786 (h19), x 183–504 (w321) | top 89%·h 2.2%·w 46.7% | y≈481, 잉크 12px → 폰트 ≈15px |

기법: `직녀/가격카드/가격카드.html` 108~124행 — SVG `<text>` + `stroke` + `paint-order="stroke"` + `textLength`. 캐릭터명 흰(명조) · 제목 흰→연파랑 그라데이션 + 글로우 · 카피 흰 `“ ”`.

### 2-3. 타이트 다크 토큰 (2026-08-23, www.sajutight.me SSR HTML + JS 번들 클래스에서 추출)
| 역할 | 타이트 실측 |
|---|---|
| html/body | `bg-[#18191A]` (컨테이너 바깥) |
| 페이지 기둥 | `min-h-screen bg-black`(#000), `max-w-[440px] mx-auto`, 본문 `px-4`, 하단 `pb-[64px]` |
| 헤더 | `bg-transparent h-[44px] px-4`, 로고 좌 · 아이콘 36×36 우 (우리는 청월당 높이 60 유지, 배경 `black/90 backdrop-blur`) |
| 글자 | 섹션 제목 `text-[18px] font-semibold #FAFAFA` · 부제 `text-[14px] #71717A` · 카드 제목 `text-[13px] font-medium #FAFAFA` · 카드 부제 `text-[11px] #A1A1AA` · 보조 `#D4D4D8` |
| 카드 | 이미지 박스 `rounded-xl bg-zinc-900`(#18181B); 뱃지 `rounded bg-white/15 px-1.5 py-0.5 text-[10px] font-bold text-white/80` |
| 후기 | 아바타 `h-11 w-11 rounded-full bg-white/10` · 메타 `text-[12px] text-white/60` · 구매횟수 `bg-amber-400/15 text-amber-300 text-[10px]` · 별 `h-3.5 w-3.5` · 시각 `text-[11px] text-white/40` · 본문 `text-[14px] leading-relaxed text-white/90` |
| 구분/테두리 | `border-white/10` |
| 푸터 | `bg-[#141414] border-t border-white/10 py-8 pb-[96px]`, 글 `text-sm text-gray-400`, 저작권 `text-gray-500` |
| 하단 탭바 | `fixed bottom-0 px-4 pb-[calc(env(safe-area-inset-bottom)+16px)]` 안에 **알약** `max-w-[408px] rounded-[32px] border border-[rgba(244,244,245,.5)] bg-[rgba(18,18,18,.5)] p-[8px] backdrop-blur-[6px]` · 탭 `rounded-[40px] py-[8px]`, 활성 `bg-[rgba(255,255,255,.2)]` · 아이콘 24(흰 85%) · 라벨 `text-[10px] leading-[10px]` 활성 `font-semibold #F4F4F5` / 비활성 `font-medium #D4D4D8` |

### 2-4. 색 치환표 (청월당 자리 → 타이트 색) — 구현은 이 표로만
| 자리 | 청월당 | **우리** |
|---|---|---|
| body(컨테이너 밖) | #FFF | `#18191A` (R6: HomeShell 이 body 에 적용·복구) |
| 페이지 기둥 | #FFF | `#000` |
| 헤더/탭 배경 | #FFF | `rgba(0,0,0,.9)` + blur, 탭 하단선 `white/10` |
| 제목 #111 | → | `#FAFAFA` |
| 부제 #757575 / #424242 | → | `#A1A1AA` / `#D4D4D8` |
| 탭 활성 #111 | → | `#FAFAFA` + 밑줄 `#FAFAFA`; 비활성 `#A1A1AA` |
| 구분선 #F1F1F1 (1·4px) | → | `white/10` |
| 카드 테두리 #E1E1E1 | → | `white/10` + 바탕 `#18181B` |
| 안내칩 #F3F5F8 / #2D4A71 | → | `white/10` / `#D4D4D8` |
| 알약 #F1F1F1 / #424242 | → | `white/20` / `#D4D4D8` |
| 후기 카드 #F1F1F1 | → | `white/10` + bg `white/[0.04]`; 아바타 `white/10`; 시각 `white/40`; 본문 `white/90` |
| 점 활성/비활성 | #111 | `#FAFAFA` / `white/30` |
| TOP 뱃지 | #111 바탕 흰 숫자 | `#FAFAFA` 바탕 검정 숫자 (CSS 리본) |
| 푸터 | #F7F9FA / #A1A1A1 / #757575 | `#141414`+`border-t white/10` / `#9CA3AF` / 저작권 `#6B7280` |
| 탭바 | 흰 판 | 타이트 알약(§2-3) |

## 3. 우리 콘텐츠 매핑 (문장·이모지 제목은 두 회사 것 재사용 금지)

### 3-1. 히어로 3장 (TOP 1·2·3) — 원본 → `scripts/make-home-art.ts` 가 자르는 기준
| TOP | slug | 원본 · 자를 위치(object-position) | 캐릭터명 | 제목 | 한 줄 카피(기존 description 에서만) |
|---|---|---|---|---|---|
| 1 | `sangun-sinjeom` | `/products/sangun/cover.webp` (840×1260) · `50% 18%` | 얼굴 없는 박수 · 산군 | 박수무당 사주 | “돌려 말하지 않고 고한다” |
| 2 | `inyeon-saju` | `/products/jiknyeo/j1.png` (1024×1536) · `50% 14%` | 직녀 | 연애예보 | “만나는 달과 조심할 달, 열두 달” |
| 3 | `marriage-saju` | `/products/jiknyeo/j2.png` · `50% 16%` | 직녀 | 결혼예보 | “결혼하는 해와 서두를 달” |

산출: `public/home/<slug>-hero.webp`(864×1080) · `-big.webp`(600×752) · `-row.webp`(400×500). 스크립트는 sharp 로 4:5 크롭+리사이즈(품질 82). Part B 이후엔 `products.art` 가 우선, 비면 이 파일.

### 3-2. 섹션 A — 큰 카드 70% 행
- h2: `✦ 먼저 보고 가는 풀이` (R8)
- 안내칩: `💡 로그인하면 받은 결과지를 보관함에서 다시 볼 수 있어요`
- 카드 3장 = 같은 3상품, **다른 컷**: 산군 `face.webp`(50% 30%) · 연애 `j3.png`(베틀, 50% 62%) · 결혼 `t14.png`(없으면 `w4.png`).

### 3-3. 권위 블록
- 알약 `정통 만세력 · 실측 검증` · h3 `다른 만세력과 대운 간지를 대조해 검증한 계산으로 풉니다`(R5) · 아래 `TrustStrip`.

### 3-4. 후기 행 (D5, R1)
- 서비스 클라이언트: `reviews`(is_public & is_approved) → `products.name` · `profiles.display_name`(있으면) / 없으면 `guest_email` 앞 2자+`****`. 최신 20, 3건 미만이면 섹션 없음.
- `maskName` · `timeAgo` · 이니셜 아바타 · 별 = rating. 일간 이모지 배지(타이트)는 2차.

### 3-5. 카테고리 행 (상품 0개면 행·탭 숨김)
| key | 제목 | slugs(A 폴백 매핑) | 앵커 |
|---|---|---|---|
| love | `💍 만나는 달, 결혼하는 해` | inyeon-saju, marriage-saju | `#love` |
| sinjeom | `🕯 돌려 말하지 않는 신점` | sangun-sinjeom | `#sinjeom` |
| wealth | `💰 돈 들어오는 달` | wealth-saju (비활성 → 자동 숨김) | `#wealth` |
- 카드 글: h5 = name, p = description 첫 절(“—” 앞). 그림: 산군 `t1-open.webp` · 연애 `j1.png`(50% 10%) · 결혼 `j2.png` · 재물 `cut-hand.webp` → `-row.webp`.
- 행 section `scroll-mt-[120px]`(R9). 카드 링크 `?via=home-<section>`(R10). 탭 활성 = IntersectionObserver.

### 3-6. 푸터 · 탭바
- 푸터: 워드마크 `명운록`(명조)+`命運錄` · `businessInfo` 전항목(**PG 심사용 홈 노출 유지**) · 약관/개인정보/환불 · 서체 출처 줄 · © 연도.
- 탭바: 타이트 알약, 홈(활성)·전체 풀이·보관함. 아이콘 `lucide-react`(Home / LayoutGrid / Archive) 24px.

## 4. 파일 계획 (Part A)

```
package.json                             [수정] dependencies 에 sharp 추가(업로드 리사이즈·사전 생성 공용)
scripts/make-home-art.ts                 [신규] §3-1/3-2/3-5 원본 → public/home/*.webp (3상품 × 3종). `npm run art:home`
public/home/*.webp                       [산출] 9장 커밋(총 ~1MB)
src/config/home.ts                       [신규] HOME_HERO 순서 · HOME_ROWS · CATEGORY_FALLBACK(slug→row) · HOME_COPY · 토큰 상수
src/lib/home-data.ts                     [신규] getHomeProducts()(서비스 클라 · 기존 컬럼만 · seed 폴백) · getHomeReviews()(서비스 클라) · (B 에서 getHomeMeta() 추가)
src/lib/utils.ts                         [수정] timeAgo(iso)
src/components/home/HomeShell.tsx        [신규, client] 헤더(워드마크+드로어: 전체 풀이·보관함·로그인/로그아웃·고객센터) + 카테고리 탭 + 알약 탭바 + body 배경 적용/복구(R6)
src/components/home/HeroCarousel.tsx     [신규, client] scroll-snap + 점(클릭 이동) + 6초 자동(터치 정지·reduced-motion 끔) · `overscroll-behavior-x: contain`
src/components/home/HeroLettering.tsx    [신규] SVG 레터링(§2-2)
src/components/home/ProductCard.tsx      [신규] 44%/70%/grid 변형, 4:5 `<img>`, border white/10, 가격 없음(D9)
src/components/home/ProductRow.tsx       [신규] 0개면 null
src/components/home/ReviewRow.tsx        [신규] 3건 미만 null
src/components/home/TrustBlock.tsx       [신규]
src/components/home/HomeFooter.tsx       [신규]
src/app/page.tsx                         [수정] 서버: 데이터 → 조립. `metadata.title = { absolute: "명운록 — 박수무당 사주 · 직녀의 연애예보" }`(R7), h1 sr-only
src/app/products/page.tsx                [수정, A-9] HomeShell + ProductCard 2열 격자(R11). 데이터는 getHomeProducts 재사용
src/components/layout/ChromeGate.tsx     [수정, A-9] bare 에 `/products` 추가(`/products/` 하위는 기존 규칙 유지)
src/app/globals.css                      [수정] `.no-scrollbar` 유틸 1개
직녀/가격카드/tools/shoot-url.ps1        [신규] URL 촬영판(R14)
```
- 헤더·탭·탭바 `fixed` + `left-1/2 -translate-x-1/2 w-full max-w-md`.
- 홈 루트 `min-h-[100dvh]` 기둥 `#000`, 바깥 `#18191A` 는 body(R6).

---

# Part B — 상품 빌더 (어드민)

## 5. 현황 (실측)
- 어드민 = `ADMIN_PASSWORD` 쿠키 게이트 + `createServiceClient()`. 이미 있는 것: 결제내역 · 분석 · **웹툰 편집기**(업로드 → 버킷 `webtoon`, 0009 에 버킷 생성 SQL 패턴 있음).
- `products` 컬럼: slug·name·description·price·display_order·is_active·compare_at_price·bundle_slugs·is_addon. **public read** 정책.
- 범용 경로 확인: Story 없는 slug → `[slug]/page.tsx` 기존 템플릿(`pitch?.` 안전) · 위저드 기본 경로 · 결과지 `STYLE_BY_SLUG[slug] ?? basic-saju`.
- ⚠ `scripts/seed-products.ts` 가 `upsert(onConflict: slug)` 로 **전 컬럼 덮어씀** → 어드민 수정이 seed 한 번에 되돌아감(§7 에서 insert-only 로).

## 6. 데이터
`supabase/migrations/0011_product_builder.sql` — 운영 Supabase, **형님이 SQL 편집기에 붙여넣기 1분**(실행 전 제가 전문 보고):
```sql
-- 홈 카드·카탈로그 (공개 읽기 OK)
alter table public.products
  add column if not exists category text,                 -- love | sinjeom | wealth | life | family | career
  add column if not exists character_name text,
  add column if not exists card_title text,               -- 비면 name
  add column if not exists tagline text,                  -- 비면 description 첫 절
  add column if not exists hero_rank integer,             -- null=히어로 제외
  add column if not exists art jsonb not null default '{}'::jsonb,  -- {hero:{url,pos:{x,y}},big:{…},row:{…}}
  add column if not exists pitch jsonb,                   -- 랜딩 공개 카피 {eyebrow,headline[],pains[],includes[],forWhom,hasCharts}
  add column if not exists updated_at timestamptz not null default now();
create unique index if not exists products_hero_rank_uidx on public.products(hero_rank) where hero_rank is not null;

-- 결과지 설계(프롬프트) — 비공개 (R4)
create table if not exists public.product_styles (
  product_id uuid primary key references public.products(id) on delete cascade,
  style jsonb not null,                                   -- {title,length,outline[],voice,banmal}
  updated_at timestamptz not null default now()
);
alter table public.product_styles enable row level security;  -- 정책 없음 = 서비스 키만

-- 카드 그림 버킷 (0009 webtoon 패턴)
insert into storage.buckets (id, name, public) values ('product-art','product-art', true) on conflict (id) do nothing;
drop policy if exists "product-art public read" on storage.objects;
create policy "product-art public read" on storage.objects for select using (bucket_id = 'product-art');
```

## 7. 파일 계획 (Part B)
```
supabase/migrations/0011_product_builder.sql   [신규] §6
src/types/database.ts                          [수정] products 새 컬럼 · product_styles 테이블
src/app/admin/page.tsx                         [수정] 메뉴 「상품 관리 · 추가/가격/카드/결과지 목차」
src/app/admin/products/page.tsx                [신규] 목록: 순서·이름·slug·가격(정가)·카테고리·TOP·활성 + [새 상품]
src/app/admin/products/new/page.tsx            [신규] 빈 폼(POST)
src/app/admin/products/[id]/page.tsx           [신규] 편집(PUT) + 「홈 카드 미리보기」(Part A 의 ProductCard/HeroLettering 그대로 import = 실물)
src/components/admin/ProductForm.tsx           [신규, client] react-hook-form + zod. 섹션 ①기본 ②홈 카드 ③랜딩 카피 ④결과지 설계
src/app/api/admin/products/route.ts            [신규] GET · POST
src/app/api/admin/products/[id]/route.ts       [신규] PUT · DELETE(orders 0건일 때만, 아니면 409 "비활성으로 내리세요")
src/app/api/admin/products/upload/route.ts     [신규] 4MB(R15) png/jpg/webp → sharp 로 원본 보관 + pos 기준 4:5 크롭 3종 webp → 버킷 product-art `products/<slug>/<kind>-{orig,hero,big,row}`
src/lib/home-data.ts                           [수정] getHomeMeta(): 새 컬럼 **별도 select, 실패 시 {} 폴백**(R3) → art/hero_rank/card_title/tagline/category 우선, 비면 A 폴백
src/app/products/[slug]/page.tsx               [수정] `pitch` 별도 select(실패 무시) → `dbPitch ?? PRODUCT_PITCH[slug]`
src/lib/saju/prompt.ts                         [수정] `resolveStyle(slug, dbStyle?)` — DB 있으면 그것, 없으면 STYLE_BY_SLUG 폴백
src/lib/saju/generate-result.ts                [수정] 서비스 클라로 `product_styles` 읽어 resolveStyle 에 전달(260·302행 근처)
scripts/seed-products.ts                       [수정] 기본 = 없는 slug 만 insert(`ignoreDuplicates: true`), `--force` 때만 전 컬럼 upsert
```
### 폼 필드 (zod)
- ①기본: `slug ^[a-z0-9-]{3,40}$`(**생성 후 잠금**) · name ≤ 30 · description ≤ 120 · price ≥ 0 · compare_at_price ≥ price 또는 빈값 · display_order · is_active · is_addon · bundle_slugs(콤마)
- ②홈 카드: category(select) · character_name · card_title(≤ 8자 권고, 초과 시 축소 경고) · tagline ≤ 24 · hero_rank(빈값/1~9, 중복 409) · art 3칸(업로드 + x/y % 슬라이더 → 저장 시 서버 재크롭 + 미리보기 즉시 반영)
- ③랜딩 카피: eyebrow · headline(줄=행) · pains(3) · includes(3~6) · forWhom · hasCharts
- ④결과지 설계: title · length(select) · outline(줄=장, 3~12) · voice(textarea) · banmal(toggle) + 「기본 틀 불러오기」(basic-saju STYLE 복사)
- 저장 후 링크: 「랜딩 보기」`/products/<slug>` · 「티저 웹툰 편집」`/admin/webtoon/<id>` · 「홈에서 보기」`/#<category>`

## 8. 검증 (완료 선언 전 전부 통과)

### Part A
1. **빌드**: dev `preview_stop` → `npm run build` → `.next` 정리 후 dev 재시작 (nextjs-dev-build-conflict).
2. **구조 대조(헤드리스 448px)**: `shoot-url.ps1` 로 우리 `/` 와 청월당 홈을 같은 폭으로 찍고 랜드마크 8개 `d:` → ±6px: 헤더 h60 · 탭 h44 · 히어로 432×540@y128 · 점 y · 큰카드 300×376 · 행카드 180×283 · 후기카드 256×153(후기 있을 때) · 푸터 패딩.
3. **색 대조**: 픽셀 샘플 — 기둥 `#000`, 바깥 `#18191A`, 푸터 `#141414`, 제목 `#FAFAFA` ±2.
4. **표면 대조**: grep 0건 — `어라, 이거 내 얘긴데` `맞춤형 추천` `더현대` `나는솔로` `따끈한 신상` `상위 1%` `발칙한` · `aifortunedoctor.com` · `sajutight.me`.
5. **모바일 375**: `scrollWidth===375` · 알약 탭바가 푸터 마지막 줄 안 가림 · iOS 바운스 영역이 보라가 아님(R6: body 배경 확인 `getComputedStyle(document.body).backgroundColor`).
6. **링크 200**: `/products/sangun-sinjeom?via=home-hero` `/products/inyeon-saju` `/products/marriage-saju` `/products` `/mypage` `/login` `/legal/*`. `/products/inyeon-saju?via=…` 가 **게이트를 건너뛰지 않는지**(R10) 확인.
7. **데이터 분기**: seed 폴백에서도 동일 · 후기 0건 → 섹션 없음 · wealth 비활성 → 행·탭 없음.
8. **`/products` 격자**(A-9)가 홈과 같은 셸·카드로 뜨고 `/products/<slug>` 는 기존 그대로.
9. **오윤록**: 모바일 첫 화면 스크린샷 1장 + [청월당 | 타이트 | 우리] 3단 대조샷 1장 제출.

### Part B
10. 마이그레이션 적용 후 `npm run typecheck` 통과. **적용 전에 A 가 그대로 살아 있는지**(R3) — 새 컬럼 없는 상태에서 홈 200.
11. 어드민에서 `test-builder` 생성(비활성) → 목록에 뜨고 홈·`/products` 에 없음 → art 업로드·슬라이더 → 미리보기 = 실물 카드(스크린샷 대조).
12. 5분 창(R12): 활성 + category=wealth → 홈 `💰` 행 자동 등장 → 바로 비활성.
13. `/products/test-builder` 범용 템플릿 200, pitch 가 DB 값.
14. `resolveStyle('test-builder', dbStyle)` 단위 + `scripts/sample-results.ts` 로 결과지 1건 → 장 제목 = DB 목차.
15. anon 키로 `product_styles` REST 조회 → **0건/거부**(R4). `products` 의 `pitch` 는 읽힘.
16. 주문 있는 상품 DELETE → 409 · test-builder DELETE → 200.
17. `npm run seed:products` 후 어드민 수정값 유지(insert-only).

## 9. 하지 않는 것 (후속)
- 무료 풀이·스낵·매거진 행 / 검색·오늘의 운세 탭 / 접속자 카운터(난수 금지) / 히어로 3D 카드 스택(R13).
- 후기 일간 이모지 배지 — saju_inputs 연결 후 2차.
- 빌더에서 전용 웹툰 랜딩(Story)·전용 결과지 템플릿 — 코드 영역.
- 「결과지 이렇게 옵니다」 증거 행 — `/dev/*` 스크린샷 카드, 2차.

## 10. 되돌리기
Part A: `src/app/page.tsx` 를 `return <SajuLabLanding />` 한 줄 + ChromeGate 의 `/products` 한 줄. Part B: 새 컬럼·테이블은 nullable/별도라 코드만 되돌려도 기존 동작 유지.

## 11. 순서·게이트
1. Part A 구현 → 검증 1~9 → 형님 눈 판정 1회(3단 대조샷)
2. Part B 마이그레이션 SQL 보고 → **형님 적용(1분)** → 구현 → 검증 10~17 → 시연 스크린샷
3. 커밋 2개(A/B 분리). push 는 별도 결정(현재 74커밋 미push).
