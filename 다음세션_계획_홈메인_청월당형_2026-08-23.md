# 홈 메인(청월당 뼈대 × 타이트 검정) + 상품 빌더 — 실행 계획서 (2026-08-23)

> 작성: Fable(계획 전용) · 실행: Opus. 형님 "고고" 후 이 문서만 보고 그대로 구현한다.
> 판정선: **변장 모드**(`docs-private/직녀_디자인/레퍼런스_복제_작업법.md`) —
> ① 구조 대조: **청월당 홈**과 치수·리듬 일치(±6px) ② 표면(색·톤): **타이트** 다크 토큰 ③ 문장·이모지 제목·캐릭터는 우리 것(두 회사 문장 재사용 0).
> 형님 지시 2건 반영: 「검정색 배경으로, 타이트처럼」(8/23) · 「상품을 추가/관리하는 빌더」(8/23) → Part A 홈 · Part B 빌더.

---

## 0. 왜 만드나 (한 줄)

지금 `/` 는 자수정 풀스크린 단일 CTA(→ `/funnel`, 파는 상품 life-saju 는 **비활성**) — 상품이 3개인데 홈이 하나만 가리킨다.
홈 = **카탈로그 + 재구매 허브**(로고 탭·결과지 크로스셀·오가닉). 광고 착지는 여전히 `/products/*` 몰입형(변경 없음).
빌더 = 상품 추가가 **코드 수정 없이 어드민에서** 끝나게(카드·랜딩 카피·결과지 목차까지 DB).

## 1. 확정 기본값 (형님이 안 바꾸면 이대로)

| # | 결정 | 기본값 | 이유 |
|---|---|---|---|
| D1 | 바탕 | **검정 앱셸 — 타이트 토큰**(§2-3). 구조는 청월당 | 형님 지시. 우리 카드(산군 촛불·직녀 밤)는 검정 위에서 오히려 덜 튄다 → 카드에 `white/10` 헤어라인 |
| D2 | 하단 탭바 | **타이트식 떠 있는 알약**(§2-3) 3탭: 홈 · 전체 풀이(`/products`) · 보관함(`/mypage`) | 검색·오늘의운세는 우리에게 없음. 홈에서만 뜸 |
| D3 | 기존 자수정 랜딩 | `/` 에서 내리고 **파일 보존**(`SajuLabLanding.tsx` 미사용) · `/funnel` 은 손대지 않음 | 되돌리기 1줄 |
| D4 | 산군 카드 제목 | `박수무당 사주` 를 SVG `textLength` 로 폭 맞춤 | 청월당은 4글자 제목 — 6글자는 폭 고정으로 흡수 |
| D5 | 후기 | 실후기 DB(`reviews`) **3건 이상**일 때만 섹션 등장 | `SHOW_SOCIAL_PROOF=false` 원칙 — 샘플 후기 노출 금지 |
| D6 | 상품 소스 | Supabase `products`(is_active & !is_addon, 폴백 seed) — **Part B 컬럼을 홈이 직접 읽는다** | 빌더에서 켜면 홈이 저절로 찬다 |
| D7 | 빌더 범위 | **카탈로그+랜딩 카피+결과지 목차**까지(§B). 전용 웹툰 랜딩(산군·직녀식 Story 컴포넌트)은 빌더 밖 | 신규 상품은 기존 **범용 템플릿**(`[slug]/page.tsx` "나머지: 기존 템플릿" 분기 + `STYLE_BY_SLUG` 폴백)으로 바로 판매 가능 |
| D8 | 삭제 | 주문이 1건이라도 있는 상품은 **삭제 불가(비활성만)** | orders/saju_results 가 product 를 참조 |

---

# Part A — 홈 메인

## 2. 실측표

### 2-1. 청월당 뼈대 (2026-08-23, 뷰포트 1280 · 컨테이너 max-w-md=448) — 치수는 이 표 그대로
| 요소 | 실측 |
|---|---|
| 컨테이너 | `max-w-md`(448) 가운데. 본문 `px-5`(20) |
| 헤더 | `fixed top-0 h-[60px] max-w-md`, `px-5 flex justify-between`. 로고 h-6(실측 83×24, x=20 y=18). 우측 아이콘 버튼 28×28 |
| 카테고리 탭 | `fixed` 헤더 아래 **h-44**, `px-5 flex items-end justify-between`. 탭 `px-2 pt-3 text-base tracking-[-2.5%]`; 활성 `font-bold border-b-2 pb-3.5`, 비활성 `font-medium pb-4` |
| 본문 시작 | 콘텐츠 y=104 (60+44) |
| 히어로 | `section.pt-6` → 슬라이드 y=128. 슬라이드 **432×540**(x=8, `w-[calc(100%-16px)]`, 4:5), `rounded-2xl`(16) · TOP 뱃지 36×46 `absolute right-3 top-0` · 점 `mt-4`: 버튼 12×12 안 점 6×6, margin 1px |
| 구분선 | `mb-10 mt-6`(1px) · 이후 `my-10` · 굵은 구분 `my-12`(4px) |
| 섹션 A(큰 카드) | h2 `ml-6 mb-2 text-xl(20) font-bold leading-[130%]` · 안내칩 `ml-5 rounded-[4px] p-2 text-xs font-bold`(실측 199×32) · 스크롤러 `mt-4 pl-5 flex gap-3`, 카드 `w-[70%]`(실측 **300×376**) `aspect-[295/370] rounded-xl`(12) |
| 권위 블록 | 알약 `mx-auto mb-[18px] rounded-full px-3 py-1.5 text-[10px] font-semibold` · h3 `text-lg(18) font-bold text-center mb-3.5` · 영상 `w-full`(우린 없음) |
| 후기 | `section.mt-16` · 머리 `px-5 flex items-end justify-between`: h3 `px-1 text-xl font-bold` · 스크롤러 `mt-4 pl-5 gap-3` · 카드 **256×153** `rounded-lg px-4 py-3` · 아바타 **44×44** 원형 · 별 5개 **16px** · 시각 `text-xs` 우측 · `이름 | 상품명` `text-xs` · 본문 `mt-2 text-xs leading-[150%] min-h-16` |
| 카테고리 행 묶음 | `flex flex-col gap-12`(48) · 행 h3 `mb-4 px-5 text-lg(18) font-bold truncate` · 스크롤러 `pl-5 pr-5 flex gap-3` · 카드 `w-[44%]`(실측 **180×283** 이미지, 전체 323) `aspect-[295/370] rounded-lg`(8) · 글 `mt-3 px-1 gap-1`: h5 `text-base(16) font-semibold truncate` / p `text-sm(14) font-medium line-clamp-1` |
| 푸터 | `px-4 py-10 pb-24 max-w-md` · 로고 `h-6 mx-auto mb-7` · 정보 `text-[10px] gap-3`, `strong` semibold, 세로 구분 `h-3 mx-2` · 링크 `mt-8 text-xs font-semibold gap-3` · 저작권 `text-xs` |

### 2-2. 히어로 카드 레터링 (청월당 원본 688×861 PNG 픽셀 스캔 → 비율)
| 층 | 잉크 박스(원본) | 비율 | 432×540 환산 |
|---|---|---|---|
| 캐릭터명 | y 521–560 (h39), x 274–415 (w141), 가운데 | top 60.5%·h 4.5%·w 20.5% | y≈327, 잉크 24px → 폰트 ≈28px |
| 제목 | y 590–724 (h134), x 97–569 (w472) | top 68.5%·h 15.6%·w 68.6% | y≈370, 잉크 84px, **폭 296px 고정**(textLength) |
| 한 줄 카피 | y 767–786 (h19), x 183–504 (w321) | top 89%·h 2.2%·w 46.7% | y≈481, 잉크 12px → 폰트 ≈15px |

기법: `직녀/가격카드/가격카드.html` 108~124행 — SVG `<text>` + `stroke` + `paint-order="stroke"` + `textLength`. 캐릭터명 흰(명조) · 제목 흰→연파랑 그라데이션 + 글로우 · 카피 흰 `“ ”`.

### 2-3. 타이트 다크 토큰 (2026-08-23, www.sajutight.me SSR HTML + JS 번들 클래스에서 추출)
| 역할 | 타이트 실측 |
|---|---|
| html/body | `bg-[#18191A]` (컨테이너 바깥, 데스크톱에서 보이는 캔버스) |
| 페이지 기둥 | `min-h-screen bg-black`(#000), `max-w-[440px] mx-auto`, 본문 `px-4`, 하단 `pb-[64px]` |
| 헤더 | `bg-transparent h-[44px] px-4`, 로고 좌 · 아이콘 버튼 36×36 우 (우리는 청월당 높이 60 유지, 배경 `black/90 backdrop-blur`) |
| 제목/본문 글자 | 섹션 제목 `text-[18px] font-semibold #FAFAFA` · 부제 `text-[14px] #71717A` · 카드 제목 `text-[13px] font-medium #FAFAFA` · 카드 부제 `text-[11px] #A1A1AA` · 보조 `#D4D4D8` |
| 카드 | 이미지 박스 `rounded-xl bg-zinc-900`(#18181B), 글자 `mt-2`; 뱃지 `rounded bg-white/15 px-1.5 py-0.5 text-[10px] font-bold text-white/80` |
| 가로 목록 카드 | `h-[76px] w-[120px] rounded-2xl bg-gray-800` + 순위 `text-[18px] font-bold white` |
| 후기 | 아바타 `h-11 w-11 rounded-full bg-white/10` · 메타 `text-[12px] text-white/60`, 일간 `font-medium text-white/80`, 구분 `text-white/20` · 구매횟수 `rounded-full bg-amber-400/15 text-amber-300 text-[10px]` · 별 `h-3.5 w-3.5` · 시각 `text-[11px] text-white/40` · 본문 `mt-3 text-[14px] leading-relaxed text-white/90` · 섹션 `my-10`, 항목 `gap-12` |
| 구분/테두리 | `border-white/10` |
| 푸터 | `bg-[#141414] border-t border-white/10 py-8 pb-[96px]`, 글 `text-sm text-gray-400`, 저작권 `text-gray-500`, 링크 `hover:text-white` |
| 하단 탭바 | `fixed bottom-0 px-4 pb-[calc(env(safe-area-inset-bottom)+16px)]` 안에 **알약** `max-w-[408px] rounded-[32px] border border-[rgba(244,244,245,.5)] bg-[rgba(18,18,18,.5)] p-[8px] backdrop-blur-[6px]` · 탭 `rounded-[40px] py-[8px]`, 활성 `bg-[rgba(255,255,255,.2)]` · 아이콘 24(흰 85%) · 라벨 `text-[10px] leading-[10px]` 활성 `font-semibold #F4F4F5` / 비활성 `font-medium #D4D4D8` |
| 글꼴 | Pretendard Variable (우리도 동일, 이미 로드됨) |

### 2-4. 색 치환표 (청월당 자리 → 타이트 색) — 구현은 이 표로만
| 자리 | 청월당 | **우리** |
|---|---|---|
| body(컨테이너 밖) | #FFF | `#18191A` |
| 페이지 기둥 | #FFF | `#000` |
| 헤더/탭 배경 | #FFF | `rgba(0,0,0,.9)` + blur, 탭 하단선 `white/10` (청월당 그림자 대신) |
| 제목 글자 #111 | → | `#FAFAFA` |
| 부제 #757575 / #424242 | → | `#A1A1AA` / `#D4D4D8` |
| 탭 활성 #111 + 밑줄 #111 | → | `#FAFAFA` + 밑줄 `#FAFAFA`; 비활성 `#A1A1AA` |
| 구분선 #F1F1F1 (1px·4px) | → | `white/10` (1px·4px) |
| 카드 테두리 #E1E1E1 | → | `white/10` + 이미지 바탕 `#18181B` |
| 안내칩 bg #F3F5F8 / #2D4A71 | → | `white/10` / `#D4D4D8` |
| 알약 border #F1F1F1 / #424242 | → | `white/20` / `#D4D4D8` |
| 후기 카드 border #F1F1F1 | → | `white/10` + bg `white/[0.04]`; 아바타 `white/10`; 시각 `white/40`; 본문 `white/90` |
| 점 활성 #111 / 비활성 | → | `#FAFAFA` / `white/30` |
| TOP 뱃지 #111 바탕 흰 숫자 | → | `#FAFAFA` 바탕 검정 숫자 (CSS 리본, 이미지 복사 금지) |
| 푸터 bg #F7F9FA / 글 #A1A1A1 / 링크 #757575 | → | `#141414` + `border-t white/10` / `#9CA3AF`(gray-400) / `#9CA3AF`, 저작권 `#6B7280` |
| 탭바 흰 판 | → | 타이트 알약 그대로(§2-3) |

## 3. 우리 콘텐츠 매핑 (문장·이모지 제목은 두 회사 것 재사용 금지)

### 3-1. 히어로 3장 (TOP 1·2·3)
| TOP | slug | 그림 (`object-cover`, object-position) | 캐릭터명 | 제목 | 한 줄 카피(기존 description 에서만) |
|---|---|---|---|---|---|
| 1 | `sangun-sinjeom` | `/products/sangun/cover.webp` (840×1260) · `50% 18%` | 얼굴 없는 박수 · 산군 | 박수무당 사주 | “돌려 말하지 않고 고한다” |
| 2 | `inyeon-saju` | `/products/jiknyeo/j1.png` (1024×1536) · `50% 14%` | 직녀 | 연애예보 | “만나는 달과 조심할 달, 열두 달” |
| 3 | `marriage-saju` | `/products/jiknyeo/j2.png` · `50% 16%` | 직녀 | 결혼예보 | “결혼하는 해와 서두를 달” |

(Part B 이후엔 이 표가 `products.art/hero_rank/card_title/...` 컬럼으로 옮겨가고, 코드의 `HOME_ART` 는 **컬럼이 비었을 때 폴백**.)

### 3-2. 섹션 A — 큰 카드 70% 행
- h2: `✦ 먼저 보고 가는 풀이 세 가지`
- 안내칩: `💡 로그인하면 받은 결과지를 보관함에서 다시 볼 수 있어요`
- 카드 3장 = 같은 3상품, **다른 컷**: 산군 `face.webp`(50% 30%) · 연애 `j3.png`(베틀, 50% 62%) · 결혼 `t14.png`(없으면 `w4.png`).

### 3-3. 권위 블록 (더현대·나는솔로 대체)
- 알약 `정통 만세력 · 실측 검증` · h3 `철학관 대운표와 간지가 일치하는지 직접 대조한 만세력으로 풉니다` · 아래 `TrustStrip` 재사용.

### 3-4. 후기 행 (D5)
- `reviews`(is_public & is_approved) ⨝ `profiles.display_name` ⨝ `products.name`, 최신 20. 3건 미만이면 섹션 없음.
- `maskName` · `timeAgo` · 이니셜 아바타 · 별 = rating. 타이트의 일간 이모지 배지는 **2차**(saju_inputs 에서 일간 뽑아야 함).

### 3-5. 카테고리 행 (상품 0개면 행·탭 숨김)
| key | 제목 | slugs | 앵커 |
|---|---|---|---|
| love | `💍 만나는 달, 결혼하는 해` | inyeon-saju, marriage-saju | `#love` |
| sinjeom | `🕯 돌려 말하지 않는 신점` | sangun-sinjeom | `#sinjeom` |
| wealth | `💰 돈 들어오는 달` | wealth-saju (비활성 → 자동 숨김) | `#wealth` |
- 카드 글: h5 = name, p = description 첫 절(“—” 앞). 그림: 산군 `t1-open.webp` · 연애 `j1.png`(50% 10%) · 결혼 `j2.png` · 재물 `cut-hand.webp`.
- 탭: `전체`(top) · `연애·결혼` · `신점` · `재물` — 앵커 스크롤, 활성은 IntersectionObserver. Part B 이후 행 정의도 `products.category` 로.

### 3-6. 푸터 · 탭바
- 푸터: 워드마크 `명운록`(명조)+`命運錄` · `businessInfo` 전항목(**PG 심사용 홈 노출 유지**) · 약관/개인정보/환불 · 서체 출처 줄 · © 연도.
- 탭바: 타이트 알약, 홈(활성)·전체 풀이·보관함. 아이콘은 `lucide-react`(Home/LayoutGrid/Archive) 24px.

## 4. 파일 계획 (Part A)

```
src/config/home.ts                       [신규] HOME_ROWS · HOME_ART 폴백 · HOME_COPY · 다크 토큰 상수
src/lib/home-data.ts                     [신규] getHomeProducts()(Part B 컬럼 포함 select, seed 폴백) · getHomeReviews()
src/lib/utils.ts                         [수정] timeAgo(iso)
src/components/home/HomeShell.tsx        [신규, client] 헤더(워드마크+메뉴 드로어: 전체 풀이·보관함·로그인/로그아웃·고객센터) + 카테고리 탭 + 알약 탭바
src/components/home/HeroCarousel.tsx     [신규, client] scroll-snap 100% 폭 + 점 동기화 + 6초 자동(터치 정지·reduced-motion 끔)
src/components/home/HeroLettering.tsx    [신규] SVG 레터링(§2-2)
src/components/home/ProductCard.tsx      [신규] 44%/70% 변형, 4:5, next/image sizes="(max-width:448px) 44vw, 197px", border white/10
src/components/home/ProductRow.tsx       [신규] 0개면 null
src/components/home/ReviewRow.tsx        [신규] 3건 미만 null
src/components/home/TrustBlock.tsx       [신규]
src/components/home/HomeFooter.tsx       [신규]
src/app/page.tsx                         [수정] 서버: fetch → 조립. metadata title "명운록 — 박수무당 사주·직녀의 연애예보"
src/app/globals.css                      [수정] `.no-scrollbar` 유틸 1개만 추가
```
- `html`/`body` 배경은 홈 컴포넌트가 `style`로 덮지 말고 **홈 최상위 div 가 `min-h-screen` 로 #18191A 를 깔고 안쪽 기둥이 #000** (전역 CSS 건드리지 않음 — 다른 페이지 톤 보호).
- 헤더·탭·탭바 `fixed` + `left-1/2 -translate-x-1/2 w-full max-w-md`.

---

# Part B — 상품 빌더 (어드민)

## 5. 현황 (실측)
- 어드민 = `ADMIN_PASSWORD` 쿠키 게이트(`requireAdminPassword`) + `createServiceClient()` 로 쓰기. 이미 있는 것: 결제내역 · 분석 · **웹툰 편집기**(`/admin/webtoon/[productId]`, 업로드 `/api/admin/webtoon/upload` → Storage 버킷 `webtoon`).
- `products` 컬럼: slug·name·description·price·display_order·is_active·compare_at_price·bundle_slugs·is_addon.
- **범용 경로가 이미 있다**: slug 전용 Story 가 없으면 `[slug]/page.tsx` 가 기존 템플릿(pitch + 위저드 + DB 웹툰 티저)로 렌더하고, 결과지는 `STYLE_BY_SLUG[slug] ?? basic-saju` 로 생성된다. → 빌더는 **이 폴백들을 DB 값으로 채우는 것**이 전부다.
- ⚠ 함정: `scripts/seed-products.ts` 가 `upsert(onConflict: slug)` 로 **전 컬럼을 덮어쓴다** → 어드민에서 고친 가격·이름이 seed 한 번에 되돌아간다. 반드시 손본다(§7).

## 6. 데이터
`supabase/migrations/0011_product_builder.sql` (운영 Supabase — **형님이 SQL 편집기에 붙여넣기 1분**, 실행 전 제가 내용 보고):
```sql
alter table public.products
  add column if not exists category text,                 -- love | sinjeom | wealth | life | family | career
  add column if not exists character_name text,           -- 카드 캐릭터명 ("직녀")
  add column if not exists card_title text,               -- 카드 제목 레터링 ("연애예보") 비면 name
  add column if not exists tagline text,                  -- 카드 한 줄 카피 비면 description 첫 절
  add column if not exists hero_rank integer,             -- null=히어로 제외, 1..n (unique where not null)
  add column if not exists art jsonb not null default '{}'::jsonb,  -- {hero:{url,pos},big:{url,pos},row:{url,pos}}
  add column if not exists pitch jsonb,                   -- ProductPitch {eyebrow,headline[],pains[],includes[],forWhom,hasCharts}
  add column if not exists style jsonb,                   -- SlugStyle {title,length,outline[],voice,banmal}
  add column if not exists updated_at timestamptz not null default now();
create unique index if not exists products_hero_rank_uidx on public.products(hero_rank) where hero_rank is not null;
-- Storage: 버킷 product-art (public read) — 대시보드에서 생성 또는 insert into storage.buckets
```
읽기 RLS 는 기존 `products public read` 정책이 새 컬럼도 덮는다. 쓰기는 서비스 키만.

## 7. 파일 계획 (Part B)
```
supabase/migrations/0011_product_builder.sql   [신규] §6
src/types/database.ts                          [수정] products Row/Insert/Update 에 새 컬럼
src/app/admin/page.tsx                         [수정] 메뉴 「상품 관리 · 추가/가격/카드/결과지 목차」 추가
src/app/admin/products/page.tsx                [신규] 목록: 순서·이름·slug·가격(정가)·카테고리·TOP·활성 + [새 상품]. 행 클릭 → 편집
src/app/admin/products/new/page.tsx            [신규] 빈 폼(POST)
src/app/admin/products/[id]/page.tsx           [신규] 편집(PUT) + 우측 「홈 카드 미리보기」(Part A 의 ProductCard/HeroLettering 를 그대로 import → 미리보기 = 실물)
src/components/admin/ProductForm.tsx           [신규, client] react-hook-form + zod. 섹션 4: ①기본 ②홈 카드 ③랜딩 카피(pitch) ④결과지 설계(style)
src/app/api/admin/products/route.ts            [신규] GET 목록 · POST 생성
src/app/api/admin/products/[id]/route.ts       [신규] PUT 수정 · DELETE(주문 0건일 때만, 아니면 409 "비활성으로 내리세요")
src/app/api/admin/products/upload/route.ts     [신규] webtoon/upload 복제 → 버킷 product-art, 키 products/<slug>/<hero|big|row>-<ts>.<ext>, 5MB, png/jpg/webp
src/lib/home-data.ts                           [수정] art/hero_rank/card_title/tagline/category 읽기, 비면 HOME_ART·description 폴백
src/app/products/[slug]/page.tsx               [수정] `product.pitch ?? PRODUCT_PITCH[slug]` (1곳)
src/lib/saju/prompt.ts                         [수정] `resolveStyle(slug, dbStyle?)` — DB style 있으면 그것, 없으면 STYLE_BY_SLUG 폴백
src/lib/saju/generate-result.ts                [수정] products select 에 `style` 추가 → resolveStyle 로 전달 (260행 · 302행)
scripts/seed-products.ts                       [수정] 기본 = **없는 slug 만 insert**(`ignoreDuplicates`), `--force` 일 때만 전 컬럼 upsert
```
### 폼 필드 (zod)
- ①기본: `slug ^[a-z0-9-]{3,40}$`(**생성 후 잠금** — 주문·결과지가 slug/id 참조) · name ≤ 30 · description ≤ 120 · price ≥ 0 · compare_at_price ≥ price 또는 빈값 · display_order · is_active · is_addon · bundle_slugs(콤마)
- ②홈 카드: category(select) · character_name · card_title(≤ 8자 권고, 초과 시 textLength 축소 경고) · tagline ≤ 24 · hero_rank(빈값/1~9, 중복 시 서버 409) · art 3칸(업로드 + object-position 슬라이더 x/y % + 미리보기 즉시 반영)
- ③랜딩 카피: eyebrow · headline(줄=행) · pains(3) · includes(3~6) · forWhom · hasCharts
- ④결과지 설계: title · length(select: 짧게/보통/길게 → 기존 값 문자열) · outline(줄=장, 3~12) · voice(textarea) · banmal(toggle) + 「기본 틀 불러오기」(basic-saju 의 STYLE 복사)
- 저장 후 링크: 「랜딩 보기」`/products/<slug>` · 「티저 웹툰 편집」`/admin/webtoon/<id>` · 「홈에서 보기」`/#<category>`

## 8. 검증 (완료 선언 전 전부 통과)

### Part A
1. **빌드**: dev `preview_stop` → `npm run build` → `.next` 정리 후 dev 재시작 (nextjs-dev-build-conflict).
2. **구조 대조(헤드리스 448px)**: `직녀/가격카드/tools/shoot.ps1` 로 우리 `/` 와 청월당 홈을 같은 폭으로 찍고 랜드마크 8개 `d:` 출력 → ±6px: 헤더 h60 · 탭 h44 · 히어로 432×540@y128 · 점 y · 큰카드 300×376 · 행카드 180×283 · 후기카드 256×153(후기 있을 때) · 푸터 패딩. (UA 모바일 크롬.)
3. **색 대조**: 우리 렌더 픽셀 샘플 — 기둥 `#000`, 바깥 `#18191A`, 푸터 `#141414`, 제목 `#FAFAFA` ±2.
4. **표면 대조**: 청월당·타이트 문장 grep 0건(`어라, 이거 내 얘긴데` `맞춤형 추천` `더현대` `나는솔로` `따끈한 신상` `상위 1%` `발칙한`) · `aifortunedoctor.com`·`sajutight.me` 0건.
5. **모바일 375**: `scrollWidth===375` · 알약 탭바가 푸터 마지막 줄 안 가림(`pb-24`+알약 높이).
6. **링크 200**: `/products/sangun-sinjeom` `/products/inyeon-saju` `/products/marriage-saju` `/products` `/mypage` `/login` `/legal/*`.
7. **데이터 분기**: seed 폴백에서도 동일 · 후기 0건 → 섹션 없음 · wealth 비활성 → 행·탭 없음.
8. **오윤록**: 모바일 첫 화면 스크린샷 1장 + [청월당 | 타이트 | 우리] 3단 대조샷 1장 제출.

### Part B
9. 마이그레이션 적용 후 `npm run typecheck` 통과.
10. 어드민에서 상품 `test-builder` 생성(비활성) → 목록에 뜨고 홈에는 없음 → 활성 + category=wealth + art 업로드 → 홈 `💰` 행이 **자동 등장**하고 카드 = 어드민 미리보기와 동일(스크린샷 대조).
11. `/products/test-builder` 가 범용 템플릿으로 200, pitch 가 DB 값으로 찍힘.
12. `resolveStyle('test-builder', dbStyle)` 단위 확인 + `scripts/sample-results.ts` 로 결과지 1건 생성해 outline 장 제목이 DB 목차와 일치.
13. 주문 있는 상품(sangun-sinjeom) DELETE → 409 · 없는 상품 DELETE → 200.
14. `npm run seed:products` 를 돌려도 어드민에서 바꾼 가격이 **안 되돌아감**(insert-only 확인).
15. 테스트 상품 삭제로 정리.

## 9. 하지 않는 것 (후속)
- 무료 풀이·스낵·매거진 행 / 검색·오늘의 운세 탭 / 접속자 카운터(난수 금지).
- 후기 일간 이모지 배지(타이트) — saju_inputs 연결 후 2차.
- 빌더에서 **전용 웹툰 랜딩(Story)·전용 결과지 템플릿** 만들기 — 그건 코드. 빌더는 범용 템플릿 상품까지.
- 「결과지 이렇게 옵니다」 증거 행 — `/dev/*` 스크린샷 카드, 2차.

## 10. 되돌리기
Part A: `src/app/page.tsx` 를 `return <SajuLabLanding />` 한 줄로 복구. Part B: 새 컬럼은 nullable/default 라 코드만 되돌려도 기존 동작 유지(컬럼은 남겨도 무해).

## 11. 순서·게이트
1. Part A 구현 → 검증 1~8 → 형님 눈 판정 1회(대조샷)
2. Part B 마이그레이션 SQL 보고 → **형님 적용(1분)** → 구현 → 검증 9~15 → 테스트 상품으로 시연 스크린샷
3. 커밋 2개(A/B 분리). push 는 별도 결정(현재 74커밋 미push 상태).
