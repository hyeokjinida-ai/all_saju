# 계획 — 메타 픽셀 이벤트 마무리 (2026-08-24)

> 전제: 픽셀 `983160017940090` 라이브 배선 완료(`/tr/` PageView 200 실측).
> 이 계획은 「어떤 상품이든 광고로 오면 추적되나」에 대한 답을 **예**로 만드는 것.
> 실행 주체: Opus. 검증은 진짜 브라우저 + 네트워크(`pixel-verify-needs-browser` 메모리).

---

## 0. 검토 결과 — 현재 상태 실측

| 이벤트 | 메타 표준 | 발화 위치 | 상품 동적? | 상태 |
|---|---|---|---|---|
| page_view | PageView | `Analytics.tsx` 전역 + SPA | — | ✅ |
| product_view | ViewContent | `SangunWebtoon.tsx:282`, `JiknyeoStory.tsx:96` | ❌ slug 하드코딩, **게이트 있는 2종만** | ⚠ |
| teaser_view | AddToCart | `SajuWizard.tsx:461` | ✅ `productSlug` | ✅ |
| begin_checkout | InitiateCheckout | `SajuWizard.tsx:593` | ✅ slug + value | ✅ |
| purchase | Purchase | `checkout/success/page.tsx:74` | ❌ **slug 없음** | ⚠ |

**현재 파는 2종(산군·직녀)은 5단계 전부 붙는다.** 빌더로 만든 새 상품은 ViewContent 한 칸이 빈다.

### 검토 중 새로 발견 — 가짜 Purchase (P0)

`success/page.tsx:117` — `res.ok` 이면 무조건 `sendPurchase()`.
그런데 `confirm/route.ts:80` 은 토스 상태가 `DONE` 이 아닐 때(**가상계좌 입금 대기**)도 200 + `pending: true, reason: "awaiting_deposit"` 을 돌려준다.
`TossWidget` 은 `variantKey: "DEFAULT"` 라 토스 콘솔에서 켜 둔 결제수단이 다 뜬다 — 가상계좌가 켜져 있으면 **돈을 안 받았는데 Purchase 가 나간다.**

결과: 메타가 「입금 안 한 사람」을 구매자로 학습한다. ROAS 부풀고 타깃이 틀어진다. **광고 켜기 전에 막아야 할 유일한 항목.**
(가상계좌를 고르고 입금 안 하는 손님이 실제로 얼마나 되느냐와 무관하게, 신호 자체가 거짓이다.)

---

## 1. 역산표

| 광고 켠 뒤 보고 싶은 것 | 필요한 신호 | 지금 | 이 계획 뒤 |
|---|---|---|---|
| 소재별 ROAS | Purchase + value | ✅ (단, 가상계좌 오염) | ✅ 정확 |
| 어느 상품이 팔리나 | Purchase + content_ids | ❌ | ✅ (맞춤 전환으로 분리 가능) |
| 새 상품 퍼널 | ViewContent 전 상품 | ❌ 2종만 | ✅ |
| 게이트 이탈 vs 본문 이탈 | ViewContent = 게이트 탈출 시점 | ✅ | ✅ **유지** |

---

## 2. 작업

### P0 — 가짜 Purchase 차단 (`checkout/success/page.tsx`)

```ts
// 전: if (!res.ok) throw ...; sendPurchase(orderId, amount);
// 후:
if (!res.ok) throw ...;
if (json.reason !== "awaiting_deposit") sendPurchase(orderId, amount, json.productSlug);
```

- `alreadyPaid: true` 응답은 **그대로 보낸다** — 두 겹 중복방지(ref + localStorage 키)가 이미 막는다.
- 가상계좌 입금 완료는 토스 웹훅 → 크론이 처리하는 경로라 **클라이언트가 Purchase 를 쏠 기회가 없다.** 이건 받아들인다(서버 CAPI 없음 — §5).

### P1 — Purchase 에 상품 싣기

**`api/orders/confirm/route.ts`** — 주문 조회에 slug 조인 한 줄:

```ts
.select("id, amount, status, product_id, user_id, guest_email, products(slug)")
```

성공 응답 3곳(`resultId` / `pending` / `alreadyPaid`)에 `productSlug` 추가. `products.slug` 는 0001 부터 있던 컬럼이라 마이그레이션 위험 없음.

**`success/page.tsx`** — `sendPurchase(orderId, amount, slug)` → `track("purchase", { value, currency: "KRW", slug })`.
`analytics.ts:41` 이 `slug → content_ids` 로 이미 변환한다. **`analytics.ts` 는 안 건드린다.**

효과: 메타 「맞춤 전환」에서 `content_ids contains sangun-sinjeom` 규칙으로 상품별 전환을 가른다. URL 에는 slug 가 없어 이 방법뿐이다.

### P2 — ViewContent 를 모든 상품으로

**원칙: 게이트 있는 랜딩의 발화 시점은 건드리지 않는다.** `SangunWebtoon`·`JiknyeoStory` 의 `product_view` 는 「게이트를 벗어난 순간」이고, 그게 게이트 이탈률을 재는 유일한 자다(두 파일 주석이 그 이유를 적어 놨다). 페이지 로드로 올리면 그 자가 없어진다.

**게이트 없는 분기만** 마운트 1회 비콘을 단다:

```tsx
// src/components/analytics/ProductViewBeacon.tsx ("use client")
export function ProductViewBeacon({ slug }: { slug: string }) {
  useEffect(() => { track("product_view", { slug }); }, [slug]);
  return null;
}
```

`products/[slug]/page.tsx` 에서 붙이는 자리 — 게이트가 없어 **페이지 로드 = 상품 열람**인 곳:

| 분기 | 게이트 | 조치 |
|---|---|---|
| `isSangunStory` | 있음 | 그대로 (컴포넌트 내부 발화) |
| `isJiknyeoStory` | 있음 | 그대로 |
| `isMarriage` (판매 중단) | 없음 | 비콘 |
| `Story` (wealth 등 WEBTOON 레지스트리) | 없음 | 비콘 |
| **일반 템플릿 (else)** — **빌더 상품 전부 여기** | 없음 | **비콘** |

`?view=detail` 로 산군 세일즈 페이지 직행 시 `initialStage="main"` → 기존 effect 가 `stage !== "gate"` 로 즉시 발화. 이미 맞다.

---

## 3. 검증 (판정선)

curl 금지. 브라우저 + `read_network_requests`(도구 먼저 부르고 **새로고침**).

| 시나리오 | 기대 `/tr/` 요청 |
|---|---|
| 일반 템플릿 상품 열기 (예: `premium-saju` 를 verify 서버에서 활성) | `ev=ViewContent&cd[content_ids]=["premium-saju"]` |
| 산군 게이트 화면만 보고 멈춤 | ViewContent **없음** (게이트 이탈 = 미열람, 의도) |
| 산군 게이트 통과 | ViewContent 1회 |
| 결제 성공 페이지 (카드) | `ev=Purchase&cd[value]=…&cd[content_ids]=["sangun-sinjeom"]` |
| 결제 성공 페이지 새로고침 | Purchase **추가 없음** (localStorage 키) |
| 가상계좌 선택 → 입금 대기 화면 | Purchase **없음** |

가상계좌 시나리오는 토스 테스트 키에서 재현 가능. 안 되면 `confirm` 응답을 `awaiting_deposit` 으로 목킹해 분기만 확인.

메타 쪽: 데이터 세트 → **「테스트 이벤트」** 탭(실시간)에서 이벤트명·파라미터 확인. 개요 집계는 15~20분 지연 — 0 이라고 실패 아님.

---

## 4. 범위 밖 (출구 목록)

- **서버 CAPI** — iOS·광고차단 유실 10~30% 회수 + 가상계좌 입금완료 Purchase 도 서버에서 쏠 수 있다. 주 50전환 쌓인 뒤 따로. 지금은 클라이언트만.
- **PageView 이중 발화 가능성** — `Analytics.tsx:39` 초기 스크립트 PageView + `RouteChangeTracker` 마운트 `sendMeta("page_view")` 가 로드 순서에 따라 둘 다 나갈 수 있다(실측 1회 — `fbq` 가 아직 없을 때 effect 가 먼저 돌아 스킵됨). PageView 는 최적화 이벤트가 아니라 해롭지 않다. 기록만.
- **`content_type`·카탈로그** — 카탈로그 없음. 불필요.
- **모렐르 픽셀** — 별건.

---

## 5. 변경 파일

| 파일 | 변경 |
|---|---|
| `src/app/api/orders/confirm/route.ts` | select 에 `products(slug)`, 응답 3곳에 `productSlug` |
| `src/app/checkout/success/page.tsx` | `awaiting_deposit` 가드, `sendPurchase` 에 slug |
| `src/components/analytics/ProductViewBeacon.tsx` | 신규, ~8줄 |
| `src/app/products/[slug]/page.tsx` | 게이트 없는 3분기에 `<ProductViewBeacon slug={product.slug} />` |

**안 건드리는 것**: `analytics.ts`(매핑 정확), `SangunWebtoon`·`JiknyeoStory`(발화 시점이 자산), `Analytics.tsx`, `SajuWizard`.

커밋 1개. 검증 후 푸시 → Vercel 자동 배포(환경변수 변경 아니라 캐시 무관).
