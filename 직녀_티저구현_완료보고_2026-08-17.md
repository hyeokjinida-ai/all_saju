# 직녀 티저 구현 — 완료 보고 (2026-08-17)

> 지시서 `다음세션_계획_티저구현_2026-08-17.md` 작업 ①~⑤ **전부 완료**. 로컬 커밋 4개, **push 안 함**.

## 커밋

| 커밋 | 작업 |
|---|---|
| `d85ca9c` | ① 티저 카피 패치 3 — 죄책감 해제·다리문장·잠금 문구 |
| `b50f385` | ② 티저 구조물 4 — 기회 카운트·열두 달 달력·하지 말 것·타이머 |
| `2b41c47` | ③ 결과지 챕터명 개명 — 한 커밋 동기화 |
| `2cac6d0` | ④ 위저드 직녀화 — 보이스·이메일 화면·로딩 체크리스트 |

⑤ 게이트: `tsc --noEmit` 0 · `npm run build` 통과 · 375px 실브라우저 완주 · 산군 대조 무변.
(이 저장소의 `npm run lint`는 `next lint`인데 ESLint 설정이 없어 대화형 프롬프트로 빠집니다 —
실제 게이트는 typecheck·build 둘입니다.)

## 지시서와 달라진 것 — 형님 판단 필요 두 가지

### 1. 「앞으로 3년」 → 「앞으로 열두 달」 (B2)
만세력 월운 창이 **정확히 12개월**입니다(실측: 2026-08~2027-07). 3년치 달 데이터는 존재하지
않아서 「3년, N번」은 지어내야만 나옵니다. 지시서의 "지어내기 금지"에 걸려, 상품이 이미 모든
화면에서 약속한 **「앞으로 12개월」**에 맞췄습니다. 세운(연 단위)은 13년치가 있으니, 굳이 3년
프레임을 쓰려면 **달이 아니라 해로** 세는 문구여야 합니다.

### 2. 열두 달 달력의 등급 기준
새 기준을 만들지 않고 **결과지가 쓰는 분류를 그대로** 옮겼습니다 — 티저가 ●라고 한 달을
결과지가 안 짚으면 그 자리에서 신뢰가 끝나기 때문입니다.
`● = top3(결과지 4장과 같은 세 달) · △ = shaky·대흉 · ◎ = 짝·합·같은 결·도화·귀인 신호 · ○ = 나머지`
검증 명식(지수)에서 ● 3 · ◎ 4 · △ 3 · ○ 2, 문 열리는 달 7번, 가장 가까운 달 2026년 12월.

## 곁들여 잡은 것 (지시서에 없던 직녀 금기 위반)

- **전환점 카드**가 인연 화면에서 「장부에 붉게 표시된 해」 + 붉은 먹을 쓰고 있었습니다
  (산군 어휘 + 빨강 0 위반). → 「달력에 하나, 크게 표시된 해」 + 은사 농담. 산군은 그대로 붉게.
- **잠긴 줄**에 연애 상품인데 「돈이 들어오는 달」이 섞여 있었고, 「인연이 들어오는 달」은 바로 위
  달력과 같은 말이었습니다. → 달력이 못 하는 것만 남긴 인연 전용 목록으로.
- **결제 시트**의 「장부 한 권 더」(산군 어휘)와 40% 이상 할인 시 빨간 글씨. → 인연만 교체.
- **InyeonWebtoon 샘플 카드**의 장 번호가 목차와 어긋나 있었습니다(3·4 → 실제 4·5). → 같이 정정.

## 실측으로 배운 함정 (다음에 또 밟지 말 것)

1. **dev 서버가 떠 있는데 `npm run build` 를 돌리면 안 됩니다.** 같은 `.next` 를 덮어써서
   돌던 dev 서버가 회복 불가로 500을 뱉습니다(이번에 다른 세션 서버를 죽였고, 재시작으로 복구).
   빌드는 **서버를 내리고** 돌립니다.
2. **타이머를 rAF 로 굴리면 안 됩니다.** 탭이 가려지면 rAF 가 멈춰 첫 값조차 안 잡힙니다.
   `setInterval` + 매 틱 `Date.now()` 재측정으로 둬야 백그라운드에서 돌아와도 값이 안 밀립니다.
3. **위저드 안에서는 `position: fixed` 가 뷰포트 기준이 아닙니다.** 감싼 `.svc-fade` 의 스크롤
   연출 transform 이 기준 상자를 가로챕니다(실측: 고정바가 화면 밖 top 2657px). → `createPortal` 로 body 에.
4. **STEPS 배열에 단계를 추가하면 안 됩니다.** 건너뛰기 계산(skipped)이 밀려 산군·존댓말 4종이
   통째로 어긋납니다. 이메일 화면은 boolean 화면으로 얹었습니다.
5. **polite 렌더 경로는 인연 전용이 아닙니다** — love·wealth·premium·monthly 4종과 공유합니다.
   인연만 바꾸려면 반드시 `productSlug === "inyeon-saju"` 가드가 필요합니다.

## 다음 수 (권고)

1. **에셋 13컷** — 지금 티저·랜딩은 글만으로 성립하지만, 「글·사진 교차」 규칙상 새 구조물
   사이에 컷이 없어 글 덩어리가 이어집니다. `j3`·`j1`·`t4`(달 이름 가린 12칸 달력) 셋이 먼저입니다.
2. **B2 프레임 확정** — 위 1번, 열두 달로 갈지 해 단위 3년으로 갈지 형님 한마디.
3. **타이머 30분** — 표시광고법 판단은 형님 몫이라 요청대로 넣었습니다. 껐다 켜는 건 한 줄입니다
   (`SajuWizard.tsx` 의 `TeaserTimerBar` 렌더 한 줄).
4. **`scripts/_probe-inyeon-range.ts`** — 등급 분포·달 배정표를 LLM 0회로 다시 재는 도구입니다.
   명식을 바꿔가며 달력이 어떻게 나오는지 볼 때 씁니다.

## 그대로 둔 것

가격·slug·번들 산수·`/api/orders/create` 무변 · push 안 함 · 산군 전 구간 무변
(직녀 문구 유출 0 · 산군 고유 카피 5종 그대로) · 만세력 API 캐시 명식만 사용(신규 호출 0).
