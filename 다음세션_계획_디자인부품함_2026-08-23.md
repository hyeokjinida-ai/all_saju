# 디자인 부품함 실행계획 — 이후 상품 추가를 반나절로 (2026-08-23 · Fable 작성 · 실행은 Opus)

> **범위(형님 확인 8/23): 이후 상품을 빠르게 디자인하는 것.** 기존 직녀·산군 정리는 범위 밖 → 보류 절로.
>
> 문제: 「이 레퍼런스처럼」 1건 = 5일(청월당→직녀 8/18~22, 커밋 16개) + 베낀 티·날티.
> 원인: 상품마다 전용 컴포넌트를 새로 씀(직녀 3,200줄·산군 1,450줄). 스킨 클래스(`.world-*`)가 있는데
> 안 쓰고 색을 손으로 박음(인라인 색값 ~700, SajuWizard 134). slug 분기 SajuWizard 60곳.
> 목표:
> ```
> 지금:  레퍼런스 → 전용 컴포넌트 10개 신규 → 페이지 3곳 if 분기 → 5일
> 이후:  흡수표 1장(구조만) → 데이터 파일 3개(세계관 1줄·문구·컷 자리) + 그림 → 반나절
> ```
> 확신도 — 원인 90% · 시간 60%.

## ★ 교통정리 (8/23 저녁, 형님 「ㄱㄱ」) — 같은 날 다른 세션의 「홈 메인 + 상품 빌더」 계획과 하나의 로드맵으로

| | 칸 | 산출 | 세션 |
|---|---|---|---|
| ✓ | A 자·스킨·질감 | 완료 f4cbf32 | — |
| 1 | **B 부품함** `kit/` + `kit/lettering.tsx` | 부품 | Opus 1 |
| 2 | **C worlds.ts + 돈달 실증** | 상품 추가 = 데이터 3개 | Opus 1 |
| 3 | **홈**(홈 계획 Part A, 개정판) — 검정 앱셸, 부품으로 조립, 카드·행은 `worlds`에서 | `/` | Opus 1 |
| 4 | **빌더**(홈 계획 Part B, 개정판) — 1차 = 관리(기본·홈 카드·그림·순서·활성). 카피 폼은 2차 | 어드민 | Opus 1 + 형님 SQL 1분 |

**한 세션에 한 칸. 두 세션 동시 진행 금지** — 공유 파일 7개가 겹친다:
`[slug]/page.tsx` `SajuWizard.tsx` `results/[resultId]/page.tsx` `generate-result.ts` `prompt.ts` `globals.css` `seed-products.ts`.
(실제로 8/23 저녁, 홈 계획서를 두 세션이 동시에 고치다 한쪽 패치가 깨졌다.)

**홈 계획서에 얹을 교통정리 블록** (그 세션이 끝난 뒤 머리에 붙인다 — 바뀐 것 6곳):
1. **판정선** — 「청월당 ±6px 일치」 폐기. 청월당은 **순서·요소 목록**만(흡수표), 치수·글자는 우리 눈금(`--fs-*`), 두 회사 실측 px 그대로 쓰기 0.
2. **D7** — 범용(보라 공용) 템플릿 → 부품함 템플릿(`WORLDS[slug]` 기본값). 빌더가 만든 상품도 전용급으로.
3. **§2 실측표** = 요소 목록·순서 참고용. px 는 스펙이 아님. 글자 눈금 스냅(10→11 · 12→13 · 14→15 · 18→19).
4. **§2-4 색** — 타이트 값은 Tailwind zinc 기본 팔레트라 그대로 써도 됨. **알약 탭바만 타이트 시그니처 → 형태 변경**(하단 고정 바 + 헤어라인).
5. **HOME_ART·HOME_ROWS** → `worlds.ts`·`config/slots/<slug>.ts`(그림 등록부 하나). `HeroLettering` → `kit/lettering.tsx`.
6. **빌더 1차 = 관리만.** ③랜딩 카피·④결과지 설계 폼은 2차.
검토 2판 R1~R15(RLS·sharp·운영DB·4MB 등)는 전부 타당 — 그대로 둔다.

---

## 0. 선행 한마디
- 3번째 상품 = **돈달**(wealth-saju) 가정. 대안 신점.
- dev 서버 켠 채 `npm run build` 금지(기존 함정).

## 1. 판정선·정지 규칙

**완성 판정**: 돈달 티저·결과지가 **데이터 파일 3개 + 그림**만으로 선다(부품 신규 코드 0) + 형님 나란히 판정 30초(「같은 집」이되 「같은 페이지」 아님, 어느 경쟁사인지 못 맞힘).

**정지 규칙**
- 부품은 **두 번째 쓰일 때** 만든다 — 돈달이 안 쓰는 부품은 꺼내지 않는다.
- `src/components/kit/**` 안 `#[0-9a-f]{6}`·`rgba(` **0개**(grep 게이트). 색은 `var(--…)`만.
- 글자 크기는 눈금만: **11·13·15·17·19·23 / 28·34·44**.
- 기존 직녀·산군 화면 **변화 0** — 옛 import 경로는 재-export로 살려둔다.
- 한 세션 한두 단계. 끝마다 커밋 + build 통과.

## 2. 작업 4개 (Opus 2세션)

### A. 자·스킨·질감 — `src/app/globals.css` (0.3세션)
- 타이포 토큰 `--fs-11 … --fs-44`(눈금 9개) + `--lh-tight/--lh-body`. 기존 `.world-sangun` `.world-jiknyeo` `.teaser-light` `.teaser-pink` `.wizard-immersive` 블록은 그대로.
- `.world-wealth` 신설: 먹바탕 + 금 + 주홍 POINT. 값은 `WealthWebtoon.tsx`의 `#a4552c`·`INK_BG`를 토큰(`--wealth-point` 등)으로 승격.
- 질감 4종 `.tx-hanji`(← `jiknyeo-comic-kit.HANJI_BG`) `.tx-grain`(← `직녀/가격카드/가격카드.html` feTurbulence) `.tx-gloss` `.tx-ink`(← `InkFade`). 날티 제거용.
- 추가만 → 화면 변화 0. build.

### B. 부품함 `src/components/kit/` — 꺼내기 (0.7세션) — ✅ 완료 `02aa818` (2026-08-23)
**실제는 아래 표와 둘이 다르다** (열어 보고 바꿈):
- 재-export 껍데기 **폐기** — 원자 부품들이 스킨별 하드코딩 색(직녀 달빛 rgba·명패 「직녀」)을 물고 있어
  토큰화하면 직녀 화면이 바뀜 → 옛 파일 무접촉, kit 을 **병렬**로 세움. 이식 전까지 중복이 대가.
- 섹션 부품(Toc·Points·PriceCard·BuyCard·Grid12·Faq·Evidence)은 **C 로** — 돈달이 요구할 때 짓는다.
- 기준: **눈금에 있으면 이동, 눈금 밖(청월당 16/24/12/30/14)이면 우리 자로 새로.**
- 추가로 생긴 것: `kit/scale.ts`(자) · `kit/lettering.tsx` · `lib/product-assets.ts` · `/dev/kit` 카탈로그 ·
  세계관별 `--night/--night-edge` · 측정 도구 `직녀/가격카드/tools/probe.mjs`.
- 검사(8/23 Fable)에서 잡힌 잔여 결함 5건 → C 착수 전 수정: ① 흰 면 위 글자 `--ink-mu`(teaser-light 에서
  말풍선 글자가 흰색) ② `/dev/kit` 운영 가드 ③ HanjiCard 종이 컨텍스트(--bone* 재정의) ④ `.tx-gloss`
  self overflow:hidden ⑤ DarkBand 「밝은 판 전용」 주석.

**두 부류로 다르게 다룬다** (8/23 형님 질문으로 잡은 함정):
- **원자 부품**(말풍선·효과음·한지·잉크·슬롯·타이포 조각) = 8/20에 우리가 코드로 만든 것 → **이동 + 이름 일반화만**, 옛 파일은 `export { X } from "@/components/kit/…"` 껍데기로. 동작 변경 0.
- **섹션 부품**(목차·POINT·가격카드·격자) = 직녀 것은 **청월당 실측값(±6px)**으로 지은 것 → 그대로 넣으면 이후 모든 상품이 청월당 치수를 물려받는다(베낀 티가 시스템에 박힘). **짜임새(데이터 구조·정점 prop)만 가져오고 치수·여백·비율은 우리 눈금으로 새로 박는다.** 직녀 옛 파일은 손대지 않고 그대로 둔다(재-export 아님).

| 새 파일 | 꺼내올 것 (출처) |
|---|---|
| `kit/cut.tsx` | `SlotCut` ← `products/jiknyeo-ui.tsx` — `dir`·슬롯표를 인자로(직녀 고정 해제) |
| `kit/comic.tsx` | `Sfx` `TiltCut` `InkFade` `ThoughtBubble` ← `jiknyeo-comic-kit.tsx` · `ComicSay` `Narration` ← `jiknyeo-ui.tsx` |
| `kit/type.tsx` | `BrushHead` `T` `Cap` `P` `Rule` `BigNum` `Val` `Chip` ← `jiknyeo-teaser-kit.tsx` · `Hi` ← jiknyeo-ui |
| `kit/panel.tsx` | `HanjiCard` `BarCard` `DarkBand` `LockRow` `ChatLine` ← teaser-kit · `GlowBand` `NeonMask` ← jiknyeo-ui |
| `kit/ink.tsx` | `InkMark` ← `saju/InkMark.tsx` · `ScribbleLine/Star` ← jiknyeo-ui · `ink-circle-path.ts` |
| `kit/sections/Toc.tsx` | `JiknyeoTeaserToc` + `SangunSalesBlocks.TocCard` → `chapters[]` 하나로 |
| `kit/sections/Points.tsx` | `JiknyeoTeaserPoints` → `points[{title, body, peak}]` — **정점 줄 prop 강제** |
| `kit/sections/PriceCard.tsx` | `JiknyeoTeaserPrice` + `ValueSpecCard` |
| `kit/sections/BuyCard.tsx` | `JiknyeoBuyCard` |
| `kit/sections/Grid12.tsx` | `JiknyeoForecast` 12칸 → `cells[{label, grade, locked}]` — 돈달 좋은달/나쁜달 |
| `kit/sections/Faq.tsx` | `SangunFaq` → `items[]` |
| `kit/sections/Evidence.tsx` | `MyeongsikTable` + 인연 프로필표 → 행 데이터 |
| `kit/lettering.tsx` | SVG `<text>` + `stroke` + `paint-order` + `textLength` ← `직녀/가격카드/가격카드.html` 108~124행 (CSS text-shadow 는 8방향뿐이라 모서리가 각진다) — **홈 히어로 카드 + 가격카드 공용**(두 번째 쓰임 충족) |

- 돈달이 안 쓰는 것(`Vs` 등)은 이번에 안 꺼냄(두 번째 쓰일 때 규칙).
- 옮기며 만나는 인라인 hex는 같은 값의 토큰으로만 치환.
- 슬롯 일반화: `src/config/slots/<slug>.ts`(← `lib/jiknyeo-slots.ts` 패턴), 파일 유무는 `lib/product-assets.ts(slug)`(← `lib/jiknyeo-assets.ts`).
- 검증: build + 직녀·산군 `?demo=` 티저·`/dev/*-result` 눈으로 1회(원자는 재-export라 바뀔 수 없고, 섹션은 직녀 파일을 안 건드렸으니 바뀔 수 없음).
- 섹션 부품 치수 게이트: kit/sections 안에 청월당 실측표(`직녀/가격카드/*.html`)의 px 값 **0개** — 눈금 토큰과 cqw 비율만.

### C. 세계관 표 `src/config/worlds.ts` + 돈달 실증 (0.8세션)
slug → 한 줄. **돈달만 추가(additive)** — 기존 `isSangun/isJiknyeo` 분기 무변경.
```ts
export const WORLDS: Record<string, World> = {
  "wealth-saju": {
    skin: "world-wealth", accent: "var(--wealth-point)", voice: "seonsaeng",
    story: "WealthStory",                                   // 착지 — 기존 WealthWebtoon 유지
    teaser: ["toc","grid12","points","lock","price","faq"], // 순서 = 흡수표 산출물
    result: ["evidence","grid12","chapters","crosssell"],
    slots: "wealth",
  },
};
```
- 붙이는 자리 3곳: `app/products/[slug]/page.tsx` · `components/saju/SajuWizard.tsx`(`WORLDS[slug]` 있으면 kit 조립, 없으면 기존 분기) · `app/results/[resultId]/page.tsx`.
- 돈달 데이터 3파일: `config/worlds.ts` 1행 · `config/copy/wealth.ts`(섹션별 문구 + **정점 한 줄씩**) · `config/slots/wealth.ts`(컷 자리·라벨·노트 — 그림 0장으로 패널 성립).
- 엔진은 있음: `buildWealthFactsBlock`(saju-api.ts:444) 구조체 판 → Grid12 `cells`.
- 미리보기 `/dev/wealth-teaser` · `/dev/wealth-result`(← `/dev/jiknyeo-result` 복제). `is_active:false` 유지 — 활성화는 런칭 때 별도 승인.
- **돈달 흡수표**(구조만, 표면·치수는 우리 자): 잠금·페이월=타이트 / 증거 스태킹=운세위키 / POINT 계단·가격카드=청월당(실측 있음, 표면 폐기) / 12칸=직녀 예보판(우리 것) / 착지 3컷=음양관 첫 구간.
- 형님 게이트 ① 스킨 시안 1장 픽(먹바탕+금 vs 한지 밝은바탕+금) 1분 ② 티저 완성 나란히 판정 30초.

### D. 흡수표 양식 (0.2세션)
- `docs-private/디자인/흡수표_양식.md`: 거리 점검 3축(시장·카테고리·스킨) · 훔칠 것 O/X(구조 O / 색·폰트·비율·문장 X) · 표면 0 · 치수는 우리 자 · 섹션별 출처 지도(착지=음양관 · 잠금=타이트 · 증거=운세위키 · POINT·가격=청월당 · 공유=사주도령 · 완독 앵커=산군).
- `docs-private/직녀_디자인/레퍼런스_복제_작업법.md`에 한 줄: 「복제 모드는 레터링·로고 같은 **원자**에만. 섹션·페이지는 흡수표 + 부품함」.
- **부품도 레퍼런스에서 가져온다 — 단 「종류·역할」만, 생김새는 우리 자·스킨으로 부품함에 한 번 짓는다.** 아카이브에서 이미 보이는 후보: 사주도령 친구 순위표·공유카드(받는 사람 주어) / 운세위키 추가질문권 카드·카톡 대화 증거 블록 / 타이트 잠금 공개·가림 비율·선착순 쿠폰코드 / 음양관 개발자 서사형 첫 구간.
- **규칙: 새 부품은 상품 파일이 아니라 `kit/`에만 짓는다.** 상품 파일에 새 컴포넌트가 생기면 5일짜리로 되돌아간 것. (grep 게이트: `src/components/products/<새상품>/` 안 `function [A-Z]` 컴포넌트 정의 0)

## 3. 보류 — 범위 밖 (기존 상품 정리, 형님이 시키면)
- 기준선 촬영(직녀·산군 PNG + 숫자) → 이식(인라인 색 700개를 토큰으로, `isSangun/isJiknyeo` 60분기 → `WORLDS` 행) → 눈금 밖 값 스냅. 산군 광고 데이터 본 뒤.
- 효과: SajuWizard 2,756줄 축소, 재-export 껍데기 삭제. **이후 상품 속도와는 무관.**

## 4. 형님 몫 (합계 ~2분)
돈달 스킨 시안 픽 1분 · 나란히 판정 30초 · 그림은 나중(슬롯 패널로 먼저 성립).

## 5. 출구
홈(로드맵 3)·빌더(로드맵 4)가 이 부품함의 첫 소비자다 — 홈 카드·레터링·후기 카드 전부 `kit/`에서.
부품함 그대로 → 아기팔자(mypalja)·신점·번들. 자·흡수표만 → 모렐르 메인(카페24 HTML). 흡수표 3축 → 광고 소재 벤치마킹.
