# 결과지 개편 계획 — 산군 신점 (2026-08-08 확정)

타이트 플래그십 결과지 PDF(약 24,000자, 21p) 실측 비교에서 나온 결론을 실행하는 계획.
**Fable이 설계, Opus가 실행.** 실행 순서는 **4 → 1 → 2 → 3** (자 먼저, 그다음 구조).

## 배경 한 줄

우리가 지는 지점은 모델이 아니라 **설계**다 — 9챕터를 병렬로 따로 뽑아 서로 뭘 썼는지 몰라서
같은 달을 3~4번 반복하고, 고민은 8장에서만 답하고, 과거 서사가 없다. 조판은 우리가 이긴다.

목표 수치 (현재 → 목표):
- 분량: 4,400자 → **9,000~12,000자**
- 같은 연월이 3개 이상 챕터에 등장: 다수 → **0~1건**
- 표에 없는 달을 본문이 말함(지어낸 달): → **0건**
- 헷지 ≤5 · 나이오류 0 · 사극어미/존댓말/가족단정 0 (현행 유지)
- 건당 원가: ₩32~34 → **₩100 안팎 허용**

## 선행 — 형님이 할 것

1. **DeepSeek 충전**: https://platform.deepseek.com/top_up (API 키 있는 계정으로 로그인 → Top up).
   테스트+당분간 운영이면 $5 권장. 키는 이미 `.env`에 있으므로 충전만 하면 바로 산다.
2. 충전 확인 후 Opus가 `npx tsx scripts/compare-models.ts` 재실행 (SAMPLE_OFFSET 올려서 —
   같은 명식 재사용 시 캐시히트가 부풀려짐, 스크립트 주석 참고).

---

## 4단계 (먼저) — 검사기 보강: `scripts/lint-result.ts`

모델·구조를 바꾸기 전에 자를 만든다. 이 자로 개편 전/후, 모델 A/B를 같은 기준으로 잰다.

### 추가할 검사 3종

| id | 무엇을 세나 | 통과 기준 |
|---|---|---|
| `반복달` | 챕터별로 `20\d\d년 ?\d{1,2}월`(연월)과 `20\d\d년`(연 단독)을 추출, 같은 값이 등장하는 **챕터 수** | 연월 기준 3개 챕터 이상 = 위반. 리포트에 "2027년 5월 → 4·7·8장" 식으로 출력 |
| `지어낸달` | 본문의 연월이 확정값(돈 top/bad + 인연 top3/shaky + topYears)에 있는지 대조 | 확정값 밖 연월 = 위반 (단, 대운 시작년 등 [확정 사실] 블록의 연도는 허용 목록에 넣을 것) |
| `고민관통` | concern 키워드(예: "이직")가 등장하는 챕터 수 | 검사라기보다 계측 — 리포트만. 목표는 3장 이상 |

### 구현 메모

- 확정값 대조에는 명식이 필요하다. `--analysis <json>` 옵션 추가, 미지정 시
  `os.tmpdir()/analysis-<slug>.json` 자동 탐색 (sample-results.ts가 쓰는 캐시와 같은 경로).
  분석 JSON에서 `computeWealthFacts`/`computeInyeonFacts`를 **다시 계산**해 허용 연월 집합을 만든다
  (저장된 값 재사용 금지 — 계산이 곧 정답지다).
- 허용 집합에 넣을 것: wealth top/bad, inyeon top3/shaky/topYears, [확정 사실]의 대운 전환년,
  `computePastEvent`(teaser.ts)의 과거 연도. 이 밖의 연월은 전부 위반.
- 주의: **lint-result.ts:111 근처에 챕터 인덱스 고정 규칙이 있다** (`i !== 2 && i !== 3` — 점수
  언급을 돈·인연 장에만 허용하는 류). 3단계에서 장 순서가 바뀌면 이 인덱스들이 전부 어긋난다.
  → 지금 단계에서 **인덱스 하드코딩을 "챕터 제목 매칭"으로 바꿔 둘 것** (예: `/돈이 들어오는/`).
- 기준선 측정: 현행 샘플(`sample-sangun-sinjeom-gpt4omini.md`)에 돌려 위반 수를 계획서 하단
  "기준선" 표에 기록하고 커밋.

### 완료 판정
- 현행 샘플에서 `반복달` 위반이 실제로 검출된다(우리가 눈으로 본 그 반복).
- `지어낸달`이 8장의 "2026년 4월"(확정값에 없는 달, 이번 샘플에서 발견)을 잡는다.

---

## 1단계 — 설계도 패스: 신규 `src/lib/saju/blueprint.ts`

### 원리 (중요)

반복 제거의 핵심인 **달 배정표는 LLM이 아니라 코드가 만든다** — 결정론이고 공짜다.
LLM 설계도는 코드가 못 하는 것만 맡는다: 고민 판정, 인생 서사, 과거 검증 문장, 챕터별 핵심 주장.

### 1-a. 코드 배정표 `buildMonthPlan()` (LLM 0회)

```
챕터(신규 11장 기준)      배정                                 다른 장은 언급 금지
3장 돈이 들어오는 달       wealth.top 전부 + wealth.bad[0]
4장 인연이 들어오는 달     inyeon.top3[0..1] + 짝 프로필
6장 일과 자리             wealth.top[0] 재인용 1회만 허용(이직 시점 근거) + wealth.bad 나머지
7장 조심할 달             inyeon.shaky 전부
8장 크게 바뀌는 해         inyeon.topYears (연 단위는 여기 독점)
9장 네 물음               고민 관련 달 — monthPlan에서 concern 태그가 붙은 달만
```
각 챕터 user 프롬프트에 두 줄을 주입한다:
`[이 장의 달] …` / `[금지] 다른 달·해는 이 장에서 언급하지 마라. 위 달만 쓴다.`

### 1-b. LLM 설계도 `generateBlueprint()`

- 입력: keyFacts 3블록(이미 있는 것) + concern + 챕터 제목 목록. 만세력 전문은 **넣지 않는다**(토큰 절약, 설계에 불필요).
- 출력(JSON, zod 검증):
```ts
type ResultBlueprint = {
  verdict: string;          // 고민 최종 판정 한 문장 (예: "올해 이직, 하되 6월에")
  thread: string;           // 결과지 전체를 관통할 한 줄 (각 챕터 프롬프트에 주입)
  arc: { period: string; label: string; line: string }[];   // 대운별 인생 서사 3~5개
  pastEvents: { year: number; claim: string }[];            // 과거 검증 2~3건 (2장 전용)
  chapterClaims: string[];  // 챕터별 핵심 주장 1문장 × 11 (겹치면 안 됨을 프롬프트에 명시)
};
```
- 모델: env `BLUEPRINT_PROVIDER` / `BLUEPRINT_MODEL` 신설, 없으면 `LLM_PROVIDER`/`LLM_MODEL` 폴백.
  `llm.ts`의 provider switch를 `generateWithProvider(req, provider, model)`로 빼서 재사용
  (현 `generateInterpretation`은 그걸 env로 호출하는 껍데기로).
- JSON 안정화: OpenAI 호환이면 `response_format: {type:"json_object"}` (DeepSeek 지원), 파싱 실패 시 1회 재시도.
- **실패 시 폴백 필수**: blueprint가 null이어도 결과지는 나가야 한다 (1-a 배정표만으로도 반복은 죽는다).
  결제 손님을 설계도 실패로 막는 일은 절대 없어야 한다.
- 타임아웃 25초. 현 생성 8~19초 + 설계도 = 총 60초 이내 유지.

### 1-c. 주입 지점

`generate-result.ts`: `buildChapterPrompts` 호출 전에 blueprint + monthPlan 생성 →
`buildChapterPrompts`에 인자 추가(옵셔널) → 각 챕터 user에
`[장부 설계도] 판정/한 줄/이 장의 주장/이 장의 달` 블록을 붙인다.
`sample-results.ts`·`compare-models.ts`도 같은 경로를 타게 수정 (안 그러면 자가 가짜가 된다).

### 완료 판정
- 샘플 재생성 → lint `반복달` 위반 0~1, `지어낸달` 0.
- blueprint를 강제로 실패시켜도(BLUEPRINT_MODEL=없는모델) 결과지가 정상 생성된다.

---

## 2단계 — 결정론 표 3종 (LLM 0원)

partner-face.ts와 같은 방식: 코드가 계산, 컴포넌트가 그린다. **표 값은 프롬프트에도 주입**해
본문이 표와 다른 말을 못 하게 한다(얼굴 카드에서 검증된 원칙).

### 2-a. 용신 처방표 — 신규 `src/lib/saju/prescription.ts`

- 입력: `gyeokguk.yongsin.오행` + `희신오행` (이미 computeInyeonFacts가 쓰는 필드).
- 오행별 조견표(5종): 방향 / 색 / 소지품 / 환경(맞는 일·공간) / 피할 것 / 운동.
  문구는 산군 톤 반말. 타이트처럼 "해라/피해라" 2열 구조.
- 렌더: `SangunResult.tsx`에 `PrescriptionTable` — 신규 10장(산군의 처방) 머리에.

### 2-b. 대운 연대기 — `computeDaeunTimeline()` (saju-api.ts에 추가)

- `analysis.daeun`의 대운 배열 → {나이구간, 간지, 십성(간/지 카테고리), 한줄}.
- 한줄은 십성 카테고리 × 용신 유불리 조견표로 결정론 생성 (result-view.ts의 대운 곡선
  유불리 로직 재사용 — 거기 이미 격국 용신 대비 판정이 있다).
- 렌더: 신규 2장(네가 걸어온 길) 머리에 `DaeunTimeline` 표. 과거 대운엔 "지나온", 현재에 "지금" 배지.

### 2-c. 대박 타이밍표 — `computeWealthYears()` (saju-api.ts에 추가)

- 인연 topYears와 같은 패턴으로 **세운의 재물 점수 상위 2~3개 해**를 뽑는다
  (`rowOf` 패턴 재사용, 재성 십성 가점 방식은 computeWealthFacts의 달 로직을 연 단위로).
- 렌더: 二章 머리 `MonthTable`(돈의 달력) 아래 "크게 벌리는 해" 2~3행 추가 —
  새 컴포넌트 말고 MonthTable rows에 kind로 합류시키는 게 최소 변경.
- **주의**: 8장(크게 바뀌는 해)의 인연 topYears와 겹칠 수 있다 — 겹치면 재물 쪽 문구로 통일하고
  1-a 배정표에서 연 단위 언급 독점권을 조정할 것.

### 완료 판정
- 세 표가 dev 미리보기에서 렌더되고, 본문이 표의 값과 다른 방향/색/해를 말하지 않는다(lint 대조에 연도 추가).

---

## 3단계 — 장 구성 9 → 11 + 분량

### 새 목차 (prompt.ts `STYLE_BY_SLUG["sangun-sinjeom"].outline`)

```
0  네 그릇부터 보자            (기존 1)
1  네가 걸어온 길 ★신규        — blueprint.arc + pastEvents 로 대운별 과거 서사.
                                "그때 이랬을 거다" 검증 문장 2개 이상, 연도 명시.
                                티저 computePastEvent와 같은 연도를 쓰게 배정표에 포함
2  올해 네게 오는 것, 떠나는 것 (기존 2)
3  ★돈이 들어오는 달           (기존 3)
4  ★인연이 들어오는 달         (기존 4)
5  일과 자리의 시기            (기존 5)
6  조심할 달                  (기존 6)
7  ★네 인생이 크게 바뀌는 해    (기존 7)
8  ★네 물음에 답한다           (기존 8)
9  산군의 처방 ★신규           — 처방표 해설. 방향·색·소지품을 생활 장면으로 풀기.
                                표 값과 다른 말 금지(배정표에 처방값 주입)
10 산군의 마지막 당부          (기존 9)
```

### 분량

- 챕터당 목표 700~1,000자를 각 챕터 프롬프트에 명시 (현행 평균 490자).
- `llm.ts` max_tokens 2048 확인 — 1,000자면 여유 있으나 2장(서사)·9장(물음)은 3072로.
- lint-result 분량 기준을 총 9,000~12,000자로 갱신.

### 연쇄 수정 목록 (하나라도 빠지면 들통)

| 파일 | 바꿀 것 |
|---|---|
| `teaser.ts` SANGUN_JANG | chapterIdx 재배열: 一[0,1,2] · 二[3,5] · 三[4,6] · 四[7,8,9,10]. 티저 목차 items에 신규 2장 불릿 추가("네가 걸어온 길 — 지나온 해까지 맞춰 보인다" / "산군의 처방 — 방향·색까지 적어 준다" 톤) |
| `SangunResult.tsx:207` | `useJang = chapters.length === 9` → **9와 11 둘 다 지원**. 9장용 구(舊) 매핑을 상수로 보존 — 기존 결제 고객 결과지가 9장이다. 재열람 깨지면 사고 |
| `normalize-voice.ts` | 형광펜 챕터당 1개 규칙 — 챕터 수 하드코딩 없는지 확인(정규식 분할이라 무관할 것) |
| `lint-result.ts` | 4단계에서 제목 매칭으로 바꿔뒀으면 자동 적응. 분량 기준만 갱신 |
| `prompt.ts` 길이 명세 | length 필드 "약 1,800~2,400자" → "약 9,000~12,000자" 갱신 |
| UI 문구 grep | `9장|9챕터|아홉` 전역 grep — 티저 locked/목차·결제 페이지 재확신 요소·상품 상세에 "9장" 카피가 있으면 11로 |

### 완료 판정
- 신규 샘플: 11챕터, 9,000자+, lint 전 항목 통과.
- **회귀**: 기존 9장 md(temp에 보관된 구 샘플)를 SangunResult에 물려 4章 폴백이 그대로 렌더됨.
- verify-money-path 1회 완주(실 파이프라인 E2E).

---

## 모델 결정 (충전 후, 1단계 전에)

- `compare-models.ts`에 blueprint 후보 평가 추가: 각 후보로 설계도 JSON 생성 → 파싱 성공률(3회) +
  verdict 품질(사람이 읽고 판단).
- 예상 구도: 설계도 = deepseek-v4-pro 또는 gemini(키 있음) / 챕터 = deepseek-v4-flash vs gpt-4o-mini
  중 lint 통과하는 싼 쪽.
- 결정 기준은 lint 신지표(반복달·지어낸달)가 1순위, 문체(사극·존댓말·헷지) 2순위, 원가 3순위.

## 커밋 계획 (단계별 1커밋, typecheck 통과 확인 후)

1. `lint-result — 반복달·지어낸달·고민관통 + 인덱스 하드코딩 제거` (+기준선 수치를 이 파일 하단에 추가)
2. `blueprint — 코드 배정표 + LLM 설계도 패스 (실패 시 폴백)`
3. `결정론 표 3종 — 용신 처방·대운 연대기·대박 타이밍`
4. `산군 결과지 11장 — 걸어온 길·산군의 처방 + 9장 하위호환`

푸시는 형님 지시가 있을 때만. 각 단계 후 `npx tsx scripts/sample-results.ts`로 실측치를 커밋 메시지에 기록.

## 형님 결정 대기 (기본값으로 진행, 바꾸려면 말해 줄 것)

| # | 결정 | 기본값 |
|---|---|---|
| 1 | 타이트식 평생 자산 액수 단정("그릇 O억") | **넣지 않음** — 맞으면 바이럴, 틀리면 환불 각. 원하시면 8장에 넣는 실험안 별도 |
| 2 | 성적 코드 콘텐츠(타이트 3부 3절류) | **제외** — 3040 여성 타깃과 안 맞다고 판단 |
| 3 | 설계도 모델 | 충전 후 A/B로 결정, 잠정 deepseek-v4-pro |
| 4 | 티저 목차에 신규 2장 노출(약속 추가) | **노출** — 약속이 늘수록 티저가 세진다. 단 결과지에 반드시 존재해야 함(들통 규칙) |

## 기준선 (2026-08-08 · gpt-4o-mini · 지수 1993-05-15 여 · 후처리 적용본)

린터 보강 후 처음 잰 값. **개편 후 이 표와 비교한다.**

| 항목 | 기준선 | 목표 |
|---|---|---|
| 분량 | 4,036자 / 9챕터 | 9,000~12,000자 / 11챕터 |
| 치명(FAIL) 위반 | **0종** | 0 유지 |
| 헷지 | 11건 | ≤5 |
| 점수중복 | 1건 (5장에서 재물그릇 재인용) | 0 |
| 달도배 | 1건 (2027-6 → 3·7·9장) | 0 |
| 표밖의달 | 1건 (2026-7) | 0 |
| 지어낸달 | 0건 | 0 |
| 고민관통 | 통과 | 통과 |

**측정 중 알게 된 것 둘:**
1. `sample-results.ts` 가 `normalizeResultVoice` 를 안 거치고 있었다 — 실제 파이프라인은 거친다.
   자가 손님이 보지도 않을 문장(하라체 4건·"대흉" 1건)을 FAIL 로 잡아 실제보다 나쁜 값을 냈다. 고침.
2. **모델 변동성이 크다.** 같은 프롬프트·같은 명식인데 직전 생성에서는 `지어낸달` 2건(2026-4·2026-6,
   월운에도 없는 진짜 환각)이 나왔고 이번엔 0건이다. → **A/B 는 1회 생성으로 판정하지 말 것.
   같은 조건 3회 이상 돌려 평균으로 본다.**

## Opus 실행 시 주의

- 순서 4→1→2→3 엄수. 자 없이 구조부터 바꾸면 개선을 증명할 수 없다.
- `npx tsc --noEmit` 통과 **확인 후** 커밋 (typecheck와 commit을 한 명령에 묶지 말 것 — 인수인계 함정 2번).
- dev 서버 켠 채 `npm run build` 금지 (인수인계 함정 1번).
- 기존 9장 결과지 하위호환은 협상 불가 — 결제한 손님의 재열람이 걸려 있다.
- 샘플 재생성은 명식 캐시를 쓰므로 사주 API 콜 0. LLM 비용만 발생(건당 수십 원).
