# 업셀·상품확장 실행계획 (2026-08-11 형님 컨펌본)

> 작성: 기획 세션(Fable). 실행: Opus 세션이 이 문서를 읽고 착수한다.
> 실행 규칙: **계획→보고→컨펌→실행**. 코드 반영·커밋·배포는 각 단계 형님 OK 후.
> 마이그레이션 SQL 은 파일로 만들되 **형님이 Supabase SQL Editor 에서 직접 Run** (Claude 는 DB 접근 불가).

## 0. 형님 컨펌 완료된 결정사항

| # | 결정 | 값 |
|---|---|---|
| 1 | 정가 앵커 | 산군 정가 29,900 → 판매 19,900 (33%↓) |
| 2 | 패키지 2종 | 산군+인연 26,900 (정가 47,800, 44%↓) / 산군+재물 24,900 (정가 44,800, 44%↓). 추천 뱃지는 위저드 伴(연애상태) 답변으로 분기: 솔로·썸→인연 패키지, 그 외→재물 패키지 |
| 3 | 추가질문권 | 5,000원. **리뷰 작성 시 1회 무료**(후기 12건 게이트를 뚫는 엔진 겸용) |
| 4 | 신규 캐릭터 | 가칭 **직녀 아씨** — 홍실 장부(이미 만난 그 사람), 존대 공감 톤, 영물=원앙 한 쌍, 로판 화풍(까치 아씨와 동일). 상품: 재회 27,900 → 궁합 39,900 순차 출시 |
| 5 | 순서 | P0-1 리뷰+질문권 → P0-2 정가+패키지 → P1 → P2 → 직녀 |

전략 배경(요약): 타이트 4층 업셀 구조 이식(상세 `타이트_MZ무당사주_완전해부.md` §4·6·7·9), 질문권은 운세위키 발명, 캐릭터 확장은 청월당 모델. 크로스셀 원칙 = **자극을 바꾼다**(산군 반말 팩폭 ↔ 직녀 존대 공감).

## 1. 코드 현실 (2026-08-11 실측 — 계획의 전제)

1. **주문 구조는 1:1:1.** `orders.product_id` 단일 FK, `saju_inputs.order_id UNIQUE`, `saju_results.order_id UNIQUE` (0001_init.sql). → 패키지의 최대 공사 지점은 **saju_results 의 unique 제약**.
2. **리뷰는 이미 있으나 회원 전용.** `reviews.user_id not null` + `api/reviews/route.ts` 가 401. **비회원 결제가 주 경로인 지금 이대로면 후기 엔진이 안 돈다.** 게스트 소유 증명 패턴은 `api/orders/generate/route.ts:38-40` 에 이미 있음(**paymentKey capability** — 게스트는 toss_payment_key 일치로 본인 확인). 이걸 재활용.
3. **가격은 서버 단독 산정** (`api/orders/create/route.ts:37-53`). 번들을 products 행으로 만들면 금액 검증 로직 무수정 통과. 회원 할인 1,900 은 번들에도 자동 적용됨(의도된 동작으로 유지).
4. **생성 파이프는 멱등 공유 함수** `generateResultForOrder(orderId)` (`src/lib/saju/generate-result.ts`). confirm·폴링(generate)·크론·토스 웹훅 4곳이 호출. `maxDuration=300` 확보됨. slug 분기(buildPlanForSangun 등)가 이미 있어 번들 분기 추가 가능.
5. **products 에 정가 컬럼 없음** — `compare_at_price` 신설 필요.
6. 결제 화면 = `SajuWizard.tsx` TEASER_STEP(9) 하단 결제 버튼(라인 ~878-928). 패키지 옵션 카드는 이 버튼 위에 삽입.

## 2. P0-1 — 리뷰 개통 + 추가질문권

### 2-1. 마이그레이션 `0010_reviews_questions.sql`

```sql
-- (a) 게스트 리뷰 개통
alter table public.reviews alter column user_id drop not null;
alter table public.reviews add column if not exists guest_email text;
alter table public.reviews add column if not exists is_approved boolean not null default true; -- 노출 관리(어드민 숨김 토글)
alter table public.reviews add constraint reviews_owner check (user_id is not null or guest_email is not null);

-- (b) 애드온 상품 구분 (홈 라인업에서 제외, 주문 생성은 허용)
alter table public.products add column if not exists is_addon boolean not null default false;

-- (c) 추가질문
create table public.extra_questions (
  id uuid primary key default gen_random_uuid(),
  parent_order_id uuid not null references public.orders(id) on delete cascade, -- 원 결과지 주문
  order_id uuid unique references public.orders(id) on delete set null,          -- 유료 질문의 결제 주문(보상 질문은 null)
  source text not null check (source in ('paid','review_reward')),
  status text not null default 'credited' check (status in ('credited','pending','answered')),
  question text,
  answer_md text,
  created_at timestamptz not null default now(),
  answered_at timestamptz
);
create index extra_questions_parent_idx on public.extra_questions(parent_order_id);
```

RLS: 기존 0002 관행 따라 추가(읽기는 소유자, 쓰기는 service_role 라우트만 — webtoon_pages 방식과 동일).

### 2-2. 게스트 리뷰 API (`api/reviews/route.ts` 수정)

- body 에 `paymentKey?: string` 추가. 비로그인 시: order 가 게스트 주문(user_id null + guest_email 존재)이고 `paymentKey === order.toss_payment_key` 면 통과 → `guest_email` 로 insert. generate/route.ts:38-40 패턴 복붙.
- insert 성공 시 **보상 크레딧 발급**: `extra_questions` 에 `{parent_order_id: order.id, source:'review_reward', status:'credited'}` 1행. 리뷰 1주문 1건(unique) 이므로 중복 발급 원천 차단.
- 응답에 `creditGranted: true` 포함 → UI 가 "질문권이 생겼어요" 안내.

### 2-3. 리뷰 작성 UI

- **조사 항목**: 결과지 페이지에 리뷰 폼이 이미 있는지 확인(없으면 신설). 결과지 하단, 별점+텍스트, 산군일 땐 반말 카피("장부 값을 했는지 적어라. 답례로 한 번 더 봐주마"), 그 외 해요체.
- 게스트는 세션에 paymentKey 가 있는 결제 직후/결과지 URL 접근 경로에서만 작성 가능(이미 결과지 접근이 그 구조).

### 2-4. 질문권 구매·사용 흐름

- 시드: products 에 `extra-question` 5,000원, `is_addon=true`, is_active true. **홈/라인업 쿼리에서 `is_addon=false` 필터 추가**(노출 지점 전수 확인: 홈, 상품 목록, sitemap, 퍼널 추천).
- **유료 경로**: 결과지 페이지 "하나 더 묻기" → 질문 입력 → `POST /api/questions/create` (parent 주문 소유 검증: 회원 user_id / 게스트 paymentKey) → orders 행(product=extra-question, amount 5,000) + extra_questions 행(source='paid', status='pending', question) → 기존 `/checkout/[orderId]` 재사용 → confirm → 생성.
- **보상 경로**: 크레딧(status='credited') 보유 시 "무료 질문 1회" 버튼 → `POST /api/questions/redeem` → question 채우고 status='pending' → 즉시 생성(결제 없음).
- **생성**: `generateResultForOrder` 에 slug=`extra-question` 분기 — parent_order_id 의 saju_inputs + 만세력 캐시 재사용, 고민확답 프롬프트(산군 惑 챕터 스타일 재활용) 1콜 → `extra_questions.answer_md` 저장(saju_results 에 저장하지 않음). **말투는 parent 상품을 따른다**(산군=반말 직언, 그 외=해요체).
- 결과지 페이지에 답변 섹션 렌더(질문·답변 목록).

### 2-5. 검증 체크리스트 (P0-1)

- [ ] tsc·next build (dev 서버 끄고 빌드, 빌드 후 `.next` 정리 — 기존 교훈)
- [ ] 시드 재실행(`npm run seed:products`, .env 수동 로드) 후 홈에 extra-question 미노출 확인
- [ ] 게스트 플로우 실주행: 비회원 결제(리뷰모드) → 결과지 → 리뷰 작성 → 크레딧 발급 → 무료 질문 → 답변 생성
- [ ] 회원 플로우 동일 + 유료 질문 결제 왕복
- [ ] 답변 말투 샘플 검증(산군 1건: 사극어미 0·존댓말 0 — `scripts/sample-results.ts` 활용)
- [ ] 어드민에서 리뷰 숨김 토글 동작

## 3. P0-2 — 정가 앵커 + 패키지 2종

### 3-1. 마이그레이션 `0011_bundles.sql`

```sql
-- (a) 정가 앵커
alter table public.products add column if not exists compare_at_price integer check (compare_at_price is null or compare_at_price >= price);

-- (b) 번들 정의 (구성품 슬러그 배열 — 별도 테이블보다 단순)
alter table public.products add column if not exists bundle_slugs text[];

-- (c) 한 주문 여러 결과지: unique(order_id) → unique(order_id, product_slug)
alter table public.saju_results add column if not exists product_slug text;
update public.saju_results r set product_slug = p.slug
  from public.orders o join public.products p on p.id = o.product_id
  where r.order_id = o.id and r.product_slug is null;      -- 기존 행 백필
alter table public.saju_results alter column product_slug set not null;
alter table public.saju_results drop constraint saju_results_order_id_key;
alter table public.saju_results add constraint saju_results_order_slug_key unique (order_id, product_slug);
```

⚠ unique 제약 이름은 실 DB 에서 확인 후 drop (`\d saju_results` 또는 pg_constraint 조회를 형님 Run SQL 에 포함).

### 3-2. 시드 (`products.seed.ts`)

| slug | name | price | compare_at | bundle_slugs | 비고 |
|---|---|---|---|---|---|
| sangun-sinjeom | (기존) | 19,900 | **29,900** | — | 정가 앵커만 추가 |
| bundle-sangun-inyeon | 산군+인연 장부 묶음 | 26,900 | 47,800 | [sangun-sinjeom, inyeon-saju] | is_addon=true(라인업 비노출, 결제시트 전용) |
| bundle-sangun-wealth | 산군+재물 장부 묶음 | 24,900 | 44,800 | [sangun-sinjeom, wealth-saju] | 〃 |

기존 교훈 — **상품은 4곳에 흩어진다**(products 테이블/seed/product-pitch/prompt STYLE_BY_SLUG/WEBTOON). 번들은 LLM 생성이 구성품 slug 로 돌므로 prompt.ts 신규 불요. product-pitch(결제 화면 문구)만 번들 슬러그 대응 확인.

### 3-3. 결제 시트 UI (`SajuWizard.tsx` TEASER_STEP)

- 산군 slug 일 때만 옵션 카드 3장: 단품 / 패키지A(인연) / 패키지B(재물). 각 카드 = 정가 취소선 + 판매가 + 할인% + 한 줄 구성. **할인율 역전이 눈에 보이게**(33% vs 44%).
- 추천 뱃지: 위저드 伴 답변이 솔로·썸이면 A, 그 외(연애중·기혼 등)면 B. (伴 답변은 이미 위저드 state 에 있음 — [프로필] 태그 파이프)
- 선택 시 주문 생성 productId 교체(서버 페이지에서 번들 상품 3종 조회해 props 전달). 이중결제 방지(30분 pending 재사용)는 product_id 기준이라 갈아타기 시 자연히 새 주문 — 무수정.
- 중복 반론 카피(산군 대사): "겉장은 내가 봤다. 장부 통째는 다른 얘기지."
- 산군 외 상품 위저드에는 어떤 변화도 없어야 함.

### 3-4. 생성 파이프 (`generate-result.ts`)

- slug 에 bundle_slugs 있으면: 구성품별 생성을 **Promise.all 병렬**(순차 금지 — 폴링 창 64초, 평균 생성 26초 실측. 순차 2건이면 창을 뚫는다). saju_results 에 (order_id, 구성품 slug) 행 2개.
- 멱등 체크를 (order_id, product_slug) 기준으로 교체. 한쪽만 성공한 부분실패 시 성공분 보존, 실패분만 재시도(기존 '미완성' 정책 그대로).
- 폴링 응답(`api/orders/generate`)은 **전 구성품 완료 시에만** resultId 반환(부분완료는 pending 유지 — 프론트 수정 최소화).
- `sendResultEmail` 번들 대응(링크 1개: 주문 결과지 URL이면 무수정일 가능성 — 조사).

### 3-5. 결과지 페이지

- **조사 항목**: 결과지 URL 이 orderId 기준인지 resultId 기준인지 → 번들이면 결과 2부를 탭("산군 장부 | 인연 장부") 또는 상하 순차 렌더. 상품별 스킨 분기(웹툰 컷·voice)가 slug 로 도는지 확인 후 구성품 slug 로 각각 렌더.

### 3-6. 검증 체크리스트 (P0-2)

- [ ] 마이그레이션 후 기존 결과지 전건 정상 열람(백필 확인 쿼리 포함)
- [ ] 번들 결제 왕복(리뷰모드): 결제 → 병렬 생성 → 폴링 창 내 완료 → 결과 2부 렌더
- [ ] 단품 플로우 회귀 무결(산군·인연·재물·옛 템플릿 각 1)
- [ ] 부분실패 시나리오: 한쪽 LLM 실패 → 재폴링으로 완성
- [ ] 정가 취소선이 랜딩·결제시트 표기 일관
- [ ] 모바일 375 넘침 0 · 콘솔 에러 0

## 4. P1·P2 — 씨앗·큐레이션·배너 (P0 배포 후)

1. **P1-a 결제 후 세계관 유지**: AnalyzingScreen 산군 분기(먹빛+반말 "장부를 펴는 중이다"). 현재 보라 나경반+존댓말 = 세계관 단절(해부 §6-2).
2. **P1-b 대기 화면 씨앗**: 폴링 64초 동안 타이트식 보관함 목업 — 잠긴 장부 3종(돈/인연/홍실) 노출. 텍스트/CSS 만, 이미지 불요.
3. **P1-c 결과지 말미 큐레이션**: "○○님의 다음 장부" 1칸 — [프로필] 태그 분기(솔로→인연, 기혼→부부·자녀, 직업고민→재물).
4. **P2 챕터 핸드오프 배너**: 결과지 md 의 챕터 경계(outlineTitles 기반 파싱)에 컴포넌트 삽입 — 돈 챕터 끝→재물("여기서부턴 금두꺼비 장부다"), 인연 챕터 끝→인연. 감정 최고조 지점 삽입(타이트 §9-4 공식).

## 5. 직녀 Phase (P0 배포 후 착수, ~9월 초 사양서)

- 산출물: `직녀_사양서.md` — ①작명 확정(가칭 직녀 아씨, 후보 3개 추가 제시) ②비주얼 스펙(로판 화풍·원앙 한 쌍·오작교 세계관 연결, 산군 바이블 포맷 재활용) ③재회 27,900 챕터 7(그 사람의 지금 마음/끊어진 진짜 이유/**다시 이어지는 달**/연락의 시기/달라져야 할 것/끝 모양/당부) ④궁합 39,900 챕터(두 원국 겹쳐보기/겉·속 점수/싸우는 지점/채우는 것/위험한 달·좋은 달/오래가는 법) ⑤엔진: computeInyeonFacts 확장 + 2인 대조(일지 합충 표 재활용, 만세력 2콜) ⑥30대 여성 페르소나 3인 모의판정(인연 상품 때 검증된 방법).
- 포지션 카피: **"몇 년이 아니라 몇 월인지 짚는다"** — 타이트 실측 구멍(연도 70회 vs 달 1회, 해부 §7-4). 재회에서 최강.
- 출시: 재회 먼저(독립 콜드 광고 가능) → 궁합(엔진 공유, 산군 업셀 종착). 이후 패키지를 이종 자극으로 재편(산군+재회).
- 데드라인: **9월 중 착수** — 10~11월 신년 리뉴얼(월별캘린더→2027판+먹빛)과 겹치면 안 됨.

## 6. Opus 실행 세션 주의사항

1. 착수 전 이 문서 + `타이트_MZ무당사주_완전해부.md` §4·6·7 정독.
2. 단계별 형님 보고·컨펌 후 진행. 커밋·배포·시드 실행 타이밍도 형님 지시.
3. 결제 실험은 PG 심사 대기 상태 고려(PG_REVIEW_MODE 확인 후 리뷰모드/테스트 결제로).
4. 프롬프트를 만지면 반드시 샘플 재생성 검증(`scripts/sample-results.ts` — 새 임시 스크립트 만들지 말 것).
5. `next build` 는 dev 서버 끄고. 라이브 API 점검 시 브라우저 UA 필요(봇필터).
6. 조사 항목(§2-3 리뷰폼 존재, §3-4 이메일, §3-5 결과지 URL 구조)은 구현 전 확인하고 계획과 다르면 보고.
