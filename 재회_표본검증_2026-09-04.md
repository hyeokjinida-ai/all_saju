# 견우의 재회예보 — 표본 결과지 실측 검증 (2026-09-04)

> 대상: `reunion-saju` 표본 2건(같은 명식·같은 고민, **「지금 마음」만** 다름).
> 생성 경로는 운영과 같은 함수다 — `fetchSajuAnalysis` → `computeReunionFacts` →
> `buildReunionFactsBlock` → `buildMonthPlan` → `generateBlueprint` → `buildChapterPrompts`
> → `generateByChapters` → `normalizeResultVoice`. 스크립트는 `scripts/sample-results.ts` 하나만 늘렸다.

## 0. 자체 검증에서 잡은 것 (정정 목록)

| # | 잡은 것 | 내용 |
|---|---|---|
| 1 | **린터의 「표밖의달」은 재회에서 오탐이다** | 4건 WARN 이 떴는데 **전부 정상이다.** `lint-result.loadMonthSets` 는 표에 실리는 달을 `computeInyeonFacts().top3/shaky` 로만 만든다 — 재회 표는 12칸 **전부**가 실린다(다리·연락OK·연락NO). 지적된 2026-11 · 2026-12 · 2027-2 · 2027-7 은 넷 다 「연락해도 되는 달」이다(아래 §4 12칸 실측표와 대조). **린터는 안 고쳤다** — 지시 범위 밖이고 게이트(치명)는 이미 통과라서, 여기 기록만 남긴다. |
| 2 | **「낮음」 판정은 이 표본으로는 못 봤다** | 서윤 명식은 다리 달의 질이 좋아 상대를 누구로 바꿔도 높음/보통만 나온다(15쌍 실측: 높음 11 · 보통 4 · 낮음 0). 그래서 부정 판정은 **본문이 아니라 계산으로** 검증했다(§5). 낮음 본문이 실제로 어떻게 쓰이는지는 아직 **안 본 것**이다. |
| 3 | **DB 에 상품 행이 없다 → dev 는 시드 모드로 재현했다** | 운영 DB `products` 에 `reunion-saju` 가 없다(REST 실측, 14행 전수). 시드는 승인 게이트라 안 돌렸고, `NEXT_PUBLIC_SUPABASE_URL` 을 더미로 덮어 **코드 표(products.seed.ts)** 로 화면을 세웠다. 가격·번들 값은 시드 값이라 운영과 같지만, **로그인·주문·결제 경로는 이 측정에 안 들어갔다.** |
| 4 | **표본은 「상대 생일을 받은」 경우 하나뿐** | 둘 다 준호 생일을 넣었다. 상대 없는 1인 판(3장을 통째로 다르게 쓰는 분기)은 **본문으로는 안 봤다** — 계산으로만 확인했다(1인 판 19건: 보통 12 · 낮음 7 · 높음 0, 천장 60 이 실제로 걸린다). |
| 5 | 「그대」 1건 · 「아내」 6건은 **오탐** | 실물은 「있는 그대**로**」 · 「받아**내려**」 · 「쏟아**내기**」다. 검사 정규식이 어절 안쪽을 문 것 — 사람 부르는 말도 가족 단정도 0건이다. |
| 6 | 나이 「32·35」 경보도 **오탐이었다** | 스크립트 `expectAges` 가 산군 표본용 값을 물려받고 있었다. 실물은 「만 32세」(2026-09-04 기준 만 나이) · 「2028년, 35세가 되는 해」(세는나이) — 둘 다 맞다. 기대값을 `[32,33,34,35]` 로 고쳐 0건이 됐다. |

---

## 1. 표본 두 건

| | 재회 트랙 | 환승 트랙 |
|---|---|---|
| 파일 | `%TEMP%\sample-reunion-saju-서윤-재회-gpt56luna.md` | `…-서윤-환승-gpt56luna.md` |
| 명식 | 서윤 1994-06-06 · 여 · 양력 · **시 모름** | 같음(캐시 공유) |
| 상대 | 준호 1992-03-15 · 남 (생일만) | 같음(캐시 공유) |
| 이별 | 2026년 6월 · 2~3년 연애 · 그 사람이 통보 · 잠수·통보 | 같음 |
| 지금 마음 | 「다시 만나고 싶어요」 → `track=reunion` | 「더 좋은 사람 만날 수 있을까요」 → `track=moveon` |
| 고민(자유) | 그 사람이 아직 저를 생각하는지, 연락해도 되는지 알고 싶어요 | 같음 |
| 모델 | openai / gpt-5.6-luna | 같음 |
| 분량·시간 | 10,506자 · 27.1초 | 11,632자 · 22.2초 |

만세력 호출은 **2콜**뿐이다(서윤 1 + 준호 1). 두 번째 표본은 둘 다 캐시를 썼다.

---

## 2. 검증 7항목 — 실측값

| # | 항목 | 재회 | 환승 | 판정 |
|---|---|---|---|---|
| ① | 10장 전부 생성 | 10 / 10 | 10 / 10 | **통과** |
| ② | 견우 voice | 존대어미 101회 · 반말 **0** · 사극어미 **0** · 느끼한 말 **0** | 117회 · **0** · **0** · **0** | **통과** |
| ③ | 확정값 숫자 누출 | **0**(린터 `내부점수노출` ✓) | **0** | **통과** |
| ④ | 부정 판정 규칙 | 4장이 등급 「높음」을 **정면 인용** · 흐리는 표현 0 | 같음 | **통과**(낮음 경로는 §5 계산 검증) |
| ⑤ | 볼드 20자 규칙 | 20자 초과 **0** · 한 문단 3곳↑ **0** | **0** · **0** | **통과** |
| ⑥ | 결과지 린터 | **치명 0종** · 경고 4종 | **치명 0종** · 경고 2종 | **통과** |
| ⑦ | `verify-result-gate.ts` | 전 항목 통과 (재회 핵심 장 `2,4,5,6,9`) | — | **통과** |

② 자세히 — 린터의 화법 규칙 `존대-어미 · 존대-당신 · 2인칭반말 · 반말혼입 · 하라체 · 3인칭-이름` 여섯이 두 표본 모두 통과.
1장 첫 마디도 지시대로 나왔다: 「저도 일 년에 하루, 강 건너를 바라보는 놈입니다. 기다리는 마음은 압니다.
그 사람 마음은, 장부를 열면 보입니다.」 — 두 표본이 **같은 순서**로 열었고 다른 장에서 되풀이하지 않았다.

### 린터 경고(치명 아님) 내역

| 규칙 | 재회 | 환승 | 판단 |
|---|---|---|---|
| 헷지 | 1건 (「일 수 있」) | 0 | 실화. 한 군데뿐이라 넘김 |
| 형광펜 | 1건 (9장에 `==` 없음) | 0 | 실화. 9장만 하이라이트가 빠졌다 |
| 달도배 | 2026-10 → 3개 챕터(4·5·10장) | 같음 | 배정표 설계대로다 — 10장은 「가장 가까운 연락의 달」을 도로 물어 닫으라고 시킨 자리 |
| 표밖의달 | 4건 | 4건 | **오탐**(§0-1). 넷 다 12칸 안의 「연락해도 되는 달」 |

---

## 3. 12칸 확정값 — 본문이 이 밖의 달을 말했는가

`computeReunionFacts` 재계산값(서윤 × 준호):

| 달 | 등급 | 달 | 등급 |
|---|---|---|---|
| 2026년 9월 | 그냥 지나가는 달 | 2027년 3월 | 연락해도 되는 달 |
| 2026년 10월 | **다리가 놓이는 달** | 2027년 4월 | 먼저 연락하면 안 되는 달 |
| 2026년 11월 | 연락해도 되는 달 | 2027년 5월 | 먼저 연락하면 안 되는 달 |
| 2026년 12월 | 연락해도 되는 달 | 2027년 6월 | 먼저 연락하면 안 되는 달 |
| 2027년 1월 | **다리가 놓이는 달** | 2027년 7월 | 연락해도 되는 달 |
| 2027년 2월 | 연락해도 되는 달 | 2027년 8월 | **다리가 놓이는 달** |

- 판정 **높음** · 이별 무렵 판독 **꺾임**(2026년 세운 종합판정 「대흉」 + 합충 + 그 달 눌림)
- 연적 근거 = **그 사람 흐름** · 세기 **강**
- 본문이 인용한 달: 재회 11종 · 환승 11종 — **12칸 밖 0건.** 손님이 적어 낸 이별 달(2026년 6월) 외에 새 달을 만들지 않았다.

---

## 4. 부정 판정 규칙 — 계산으로 검증

본문 표본이 「높음」이라 낮음 문장을 못 봤으므로, `judgeOdds` 가 실제로 갈리는지를 **캐시된 명식 19개**로 돌렸다(API 0콜).

| 조건 | 표본 수 | 높음 | 보통 | 낮음 |
|---|---|---|---|---|
| 2인 판(모든 쌍) | 342쌍 | 67 (19.6%) | 156 (45.6%) | **119 (34.8%)** |
| 1인 판(상대 생일 없음) | 19건 | **0** | 12 | **7 (36.8%)** |

- 1인 판에서 높음이 **0** 인 것은 설계대로다 — 상대 명식이 없으면 `s = min(s, 60)` 으로 천장을 내린다.
- 낮음 표본의 근거 문장이 실제로 붙는다: 「다리가 놓이긴 하는데 폭이 좁습니다 / 두 사람 곁자리가 서로 어긋납니다(긁히기 쉬움) / …」
- `buildReunionFactsBlock` 에 내부 점수 문자열이 새는지: **false**(정규식 `내부|score|N점|/12` 0건).

---

## 5. 환승 트랙 소견 — 9장이 두터워지는가, 톤이 갈라지는가

| 장 | 재회(자) | 환승(자) | 차이 |
|---|---|---|---|
| 4장 아직 이어져 있는 것 | 1,056 | 888 | −16% |
| 5장 다리가 놓이는 달 | 1,131 | 1,404 | +24% |
| 6장 연락의 달 | 1,757 | 2,665 | **+52%** |
| **9장 강을 건너지 않는다면** | **1,319** | **1,645** | **+25%** |
| 10장 견우의 배웅 | 539 | 475 | −12% |
| 전체 | 10,506 | 11,632 | +11% |

- **9장은 실제로 두꺼워졌다.** 장 평균 대비로도 1.26배 → **1.42배**로 벌어졌다.
- 9장 첫 소제목은 두 표본 모두 「그 사람이 아니어도 되는 이유」로 같은데, 환승 판은 **전 애인 얘기를 빼고**
  내 생활·나이대·다음 사람 쪽으로 먼저 돌린다(재회 판은 매달림 패턴 교정으로 시작).
- **톤은 안 갈라졌다.** 100자당 존대어미 0.96 → 1.01회. 반말·사극어미·느끼한 말 양쪽 0.
- ⚠ 예상 밖: 6장(연락의 달)이 환승에서 **가장 크게 늘었다**(+52%). 환승 손님에게 연락 지침이 제일
  길어지는 셈인데, 기획 의도(9장을 키운다)와 어긋나지는 않지만 **비중 설계는 다시 볼 값**이다.

---

## 6. 화면 실측 (dev 3457 · 390px · DOM)

시드 모드(§0-3)로 띄우고 `직녀/가격카드/tools/measure-dom.mjs` · `shoot-long.mjs` 로 쟀다.

| 화면 | 실측 |
|---|---|
| 랜딩 `/products/reunion-saju` | 390 × **4,273px** · 가로 오버플로 **0**(scrollWidth 390 = clientWidth 390, 넘치는 요소 0개) |
| 히어로 | 390×260 · y=0 · 원본 1080×720 로드 확인 |
| 첫 CTA | y=288 · h=58 → **폴드 안**(390×844 기준 상단 41%) |
| 위저드 `#start` | y≈2,700 |
| 티저(위저드 → `?demo=`) | 페이지 390 × **10,214px** · 티저 섹션 폭 **318px = 인연과 동일** |
| 결과지 `/dev/jiknyeo-result?reunion=1` | 390 × **24,800px** |
| 콘솔 에러 | 랜딩·티저 새 탭 로드에서 **0건** |

### 결과지 실물 확인

- 제목 「서윤님에게 **다리가 놓이는 달**」 · 어깨에 牽牛 붓자국 ✓
- 장 배지 「제 1 장」 + 은하수 판 ✓ · 목차 열 장(견우의 배웅까지) ✓
- 재회 예보 12칸 전부 열림 · 다리 3개(2026년 10월 · 2027년 1월 · 2027년 8월) = 확정값과 일치 ✓
- 명식 「1994.06.06 · 여 · 양력 · **시 모름**」 = 기둥 3개 ✓
- 낙관 `seal.png` 1개(문서 끝) ✓ · 마무리 편지가 牽 牛 로 닫힘 ✓
- **직녀 얼굴 0장** ✓ — `products/jiknyeo/` 참조 **0건**. 이 페이지의 이미지는 일곱뿐이다:
  헤더 워드마크 · 견우 컷 셋(g-greet · g-river · g-farewell) · 새 인연 얼굴 카드
  `p-m-earth-jeong-elder.webp` · 「멀리할 결」 카드 `w-m-wood.webp` · 낙관.

### 견우 컷 3장 (2026-09-04 입고)

원본 1024×1536 png → 폭 1080 webp(q90). 원본은 커밋하지 않았다.

| 파일 | 크기 | 자리 | 실측 위치(390px) |
|---|---|---|---|
| `g-greet.webp` | 1080×1620 · 197KB | 결과지 프롤로그 도입 + 위저드 위로 화면(慰) | 결과지 y=698 · 위저드 217×325 |
| `g-river.webp` | 1080×1620 · 229KB | 결과지 2장 「강이 갈라진 날」 + 티저 T2(이별 채점) 위 | 결과지 y=6,778 · 티저 318×477 |
| `g-farewell.webp` | 1080×1620 · 230KB | 결과지 10장 배웅 — 편지 바로 앞 | 결과지 y=24,980 |

- 셋 다 치수를 박고 lazy 로 붙였다(로드 중 레이아웃 점프 0).
- `CutInterlude` 에 `dir` 을 새로 뒀다(기본 `jiknyeo`). 폴더를 안 가르면 재회 결과지에 직녀 얼굴이 뜬다.
- 티저 T2 는 `?demo=` 로는 안 켜진다(프리셋에 이별 시기가 없다) — **위저드를 실제로 끝까지 몰아** 확인했다:
  2026년 6월 입력 → T2 「강이 갈라지던 무렵 / 2026년 6월, 그 무렵 흐름」 + g-river 렌더 ✓.
- **랜딩 히어로는 `gyeonu-hero.webp` 를 유지했다.** g-river 가 그림으로는 더 세지만 2:3 이라
  390px 에서 585px 을 먹어 첫 CTA(현재 y=288)가 폴드 밖으로 밀린다 —
  직녀 랜딩이 히어로를 62svh 로 죄어야 했던 그 실측과 같은 자리다. 히어로는 3:2(260px)로 둔다.

### 회귀 (화면 깨짐 0)

| 라우트 | 결과 |
|---|---|
| `/products/inyeon-saju` | 200 · 390px 정상 |
| `/products/sangun-sinjeom` | 200 · 390px 정상 |
| `/jiknyeo` | 200 · 390px 정상 · 푸터 y=10,020 |
| `/dev/jiknyeo-result`(인연) | j3·w3·w5·t3 + SD 컷 + 말풍선 그대로 · 낙관 y=41,332 |
| `/dev/jiknyeo-result?marriage=1` | w2·w5 + SD 컷 그대로 · 낙관 y=28,355 |
| `/` | 200 |
| `/products/marriage-saju` | 404 — **기존 상태**(시드 `is_active: false`), 이번 변경과 무관 |

콘솔 경고 `getServerSnapshot should be cached` 는 인연·산군·직녀 세 곳에 뜨는데, 출처가
`src/lib/jiknyeo-say-box.ts`(말풍선 `useSyncExternalStore`)로 **이번 작업이 안 건드린 파일**이다.
재회 랜딩에는 안 뜬다(그 부품을 안 쓴다).

---

## 7. 아직 안 본 것

1. **낮음 판정 본문** — 계산으론 34.8% 가 낮음인데, 그때 4장·9장이 실제로 어떻게 쓰이는지는 못 봤다.
2. **상대 생일 없는 1인 판 본문** — 3장이 통째로 다른 분기인데 표본이 없다.
3. **`?demo=` 로는 T2 가 안 켜진다** — 프리셋에 재회 여섯 답이 없다(설계상 정상 폴백). 화면 확인은
   위저드를 손으로 끝까지 몰아야 한다.
4. **결제·주문 경로** — 시드 모드라 DB 를 안 탔다.
5. 린터 `표밖의달` 의 재회 대응(§0-1).
